# 사주팔자 앱 리서치 자료

**이 디렉터리는 [Agent-Reach](https://github.com/Panniantong/Agent-Reach)로 수집한 원자료(raw material) 전용입니다.** 이 자료를 바탕으로 실제로 만든 UI 목업은 이 디렉터리가 아니라 루트의 [`/mockups`](../mockups/README.md)에 별도로 있습니다 — **`research/`는 자료 수집, `mockups/`는 그 자료를 소화해 만든 산출물**이라는 역할 구분입니다.

## 목적 (2026-07-13, 사용자 요청)

리서치의 목적은 "조사 일지를 쌓는 것"이 아니라 **사주팔자·궁합·MBTI 관련 개념에 대해 정확하고 최신이며 중복 없는 참고 데이터를 유지하는 것**이다. 각 개념(십신·용신·궁합 등)마다 "지금 기준으로 가장 정확한 내용" 딱 하나만 유지하고, 새 조사로 더 정확한 정보나 정정 사항이 나오면 그 자리에서 교체한다 — 날짜별 발견 이력을 겹겹이 쌓지 않는다. 과거에 뭘 언제 조사했는지 궁금하면 `git log -p`로 확인할 수 있다.

**이 데이터베이스는 나중에 실제 개발 단계에 그대로 적용된다** — 사람 승인이 나서 `saju-developer`가 십신·용신·십이운성 등을 구현하게 되면 이 문서들이 계산 로직·콘텐츠의 근거가 되어 앱 사용자에게 그대로 노출된다. 특히 `사주팔자/`는 다른 3개(궁합·MBTI·운세)보다 정확성 요구가 가장 높은 핵심 데이터베이스이므로, 매일 밤 리서치 사이클의 마지막 순서는 항상 `사주팔자/`를 마무리 점검하는 데 쓴다.

## 디렉터리 구성

| 폴더 | 성격 | 관리 방식 |
|---|---|---|
| [`사주팔자/`](사주팔자/) | 계산·이론(정답이 있는 개념) | 개념당 파일 하나, 현재-정확 상태로 교체 |
| [`궁합/`](궁합/) | 계산·이론 | 위와 동일 |
| [`MBTI/`](MBTI/) | 계산·콘텐츠 | 위와 동일 |
| [`운세/`](운세/) | 시장·트렌드(시점 자체가 의미 있는 관찰) | 관찰 기록 형태 유지 가능 — "2026년 O월 기준 MAU"처럼 시점 표기 |

각 폴더 안의 파일 목록:
- `사주팔자/`: 기초개념 · 십신 · 용신 · 신강신약 · 대운_세운 · 십이운성 · 신살 · 공망 · 납음오행 · 택일 · 계산_정확도_이슈 · 오픈소스_라이브러리
- `운세/`: 시장_현황 · UIUX_트렌드 · 결과화면_설계 · SNS_콘텐츠_전략 · 입력_온보딩_설계
- `MBTI/`: 현황
- `궁합/`: 궁합

## UI/UX 산출물 → [`/mockups`](../mockups/README.md)
이 자료들을 바탕으로 4가지 디자인 방향(파스텔 큐트/다크 프리미엄 미스틱/레트로 아케이드/에디토리얼 만세력) 목업을 이미 제작했다. 실제 개발 시 UI/UX 기준은 `research/`가 아니라 `mockups/`(채택: 01-pastel-cute.html)를 참고할 것.

## 조사 방법 메모
- 웹 검색(Exa 시맨틱 검색), GitHub 검색/API(`gh search repos`, `gh api`), Instagram 게시물 조회(`opencli instagram user`)로 수집
- Twitter/X는 브라우저 로그인 세션이 없어 조사 제외 — 필요 시 Chrome에서 x.com 로그인 후 재조사 가능
- Dribbble/Figma 등 JS 렌더링 기반 사이트는 텍스트 기반 조사로 한계 — 무드보드 작업 시 브라우저 직접 확인 권장

## Agent-Reach 채널 현황

`agent-reach doctor`가 🟢로 보고하는 건 "설정/바이너리 설치됨"일 뿐 실제 호출 성공을 보장하지 않는다. OpenCLI 백엔드 채널(Twitter/X·Reddit·Facebook·Instagram·B站·샤오홍슈) 중 로그인 세션(`[cookie]`)이 필요한 것들은 사람이 상시 켜둔 Chrome+OpenCLI 확장이 필요해, 무인 자동화 세션에서는 세션마다 성공 여부가 다르다. **매번 안정적으로 호출 가능한 채널은 GitHub(`gh`)·전역 시맨틱 검색(Exa)·임의 웹페이지(Jina Reader)·YouTube(yt-dlp)·RSS·V2EX(공개 API)뿐**이다(2026-07-15 재검증으로 V2EX 추가, 2026-07-17 재확인에서도 동일).

| 채널 | 실측 상태 | 비고 |
|---|---|---|
| GitHub 저장소/코드 | ✅ 안정적 | gh CLI, 읽기·검색·API 완전 지원 |
| 전역 시맨틱 검색 | ✅ 안정적 | Exa(mcporter 경유), API 키 불필요 |
| 임의 웹페이지 | ✅ 안정적(구조상) | Jina Reader, 브라우저 불필요 |
| YouTube | ✅ 안정적(구조상) | yt-dlp, 브라우저 불필요 |
| RSS/Atom | ✅ 안정적(구조상) | feedparser, 브라우저 의존 없음 |
| V2EX | ✅ 안정적(2026-07-15, 2026-07-17 실측 확인) | 공개 API(`hot`/`latest`/`topic`/`replies`/`nodes`), 브라우저 불필요. 단 풀텍스트 검색 명령이 없어 Exa 웹 검색(`site:v2ex.com`)과 병행 필요. 상세 사례는 [`운세/SNS_콘텐츠_전략.md`](운세/SNS_콘텐츠_전략.md) 참고 |
| Twitter/X, Reddit | ❌ 세션마다 다름 | OpenCLI, `BROWSER_CONNECT` 오류로 실패한 이력 다수 |
| Instagram | ⚠️ 세션마다 다름 | OpenCLI, 성공한 세션도 있었음(점신 계정 분석 등) — 매번 새로 시도해볼 가치 있음 |
| Facebook, B站, 샤오홍슈 | ❌ 재시도 실패 확정(2026-07-15, 2026-07-17 재확인 동일) | OpenCLI, `opencli doctor`가 `[FAIL] Browser Bridge extension not connected`로 진단 — `whoami`/검색 시도 시 `BROWSER_CONNECT` 오류(Facebook) 또는 무응답(B站·샤오홍슈, 강제 종료). 브라우저 확장이 상시 연결된 사람 세션이 아니면 무인 세션에서는 원천적으로 호출 불가 |
| 쉐추(雪球) | 🟡 경고 | 로그인 쿠키 필요 |
| LinkedIn, 샤오위저우 | ⚪ 비활성 | 미설치/`ffmpeg` 필요 |

**지침**: SNS 실 게시물이 필요하면 OpenCLI 채널을 먼저 시도해보되, `BROWSER_CONNECT` 오류가 나면 즉시 포기하고 Exa 웹 검색으로 대체한다. V2EX는 브라우저 없이 호출 가능하므로 우선 시도할 가치가 있지만, 키워드 검색이 필요하면 Exa 웹 검색으로 글타래를 먼저 찾은 뒤 `opencli v2ex topic <id>`로 본문을 조회하는 방식을 쓴다.
