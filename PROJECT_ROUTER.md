# PROJECT_ROUTER — 기능별 경로 안내

이 문서는 `COSMOS_BEOMBOO` 앱(Flutter, `project/`)에 구현된 비즈니스 로직의 조직도입니다.
작업 요구사항을 분석한 후, 이 표에서 관련 기능의 경로를 먼저 확인하고 해당 코드로 이동해
작업하세요. 새 기능/화면/모듈을 추가했다면 반드시 아래 표에 항목을 추가하세요.

**이 문서는 경로를 빠르게 찾기 위한 네비게이션 용도입니다 — 사이클별 발견/수정 이력을
누적해서 기록하는 곳이 아닙니다.** 각 행은 "현재 상태"만 한 줄로 유지하세요. 과거 이력이
궁금하면 `git log -p PROJECT_ROUTER.md`로 확인할 수 있습니다.

관련 규칙: [CLAUDE.md](CLAUDE.md)

## 기능별 경로

| 기능 | 경로 | 상태 | 설명 |
|---|---|---|---|
| 앱 진입점 | `project/lib/main.dart` | 🟢 | 앱 시작점, 강제 라이트 테마 초기화 |
| 공통 테마/디자인 토큰 | `project/lib/app/theme/` | 🟡 | 파스텔 큐트 컬러/타이포 토큰. accent 버튼 대비(WCAG)는 사람 결정 대기 |
| 라우팅 | `project/lib/app/router.dart` | 🟢 | 화면 간 라우트 정의 |
| 생년월일시 저장소 | `project/lib/core/storage/` | 🟢 | `BirthInfoStore`/`DeepDiveInfoStore` (SharedPreferences) |
| 온보딩 화면 | `project/lib/features/onboarding/` | 🟢 | 최초 진입 화면. 랜드스케이프처럼 세로 폭이 좁은 뷰포트에서 오버플로우 나지 않도록 `LayoutBuilder`+`SingleChildScrollView`로 감싸 필요할 때만 스크롤. 시작 버튼 `Text('시작하기 →')`는 장식용 화살표가 스크린 리더에 읽히지 않도록 `semanticsLabel: '시작하기'` 지정 |
| 생년월일시 정보 입력 화면 | `project/lib/features/birth_input/` | 🟢 | 생년월일시·이름·성별·혈액형·MBTI 입력. 목업(`.field-required`) 갱신에 맞춰 이름/태어난 날짜/태어난 시간/성별/혈액형 다섯 항목이 전부 필수다(2026-07-19) — `_canSubmit`은 날짜 non-null AND (시간 non-null 이거나 "태어난 시간을 몰라요" 체크) AND 이름(trim) 비어있지 않음 AND `_gender`(`Gender?`, 초기값 null) non-null AND `_bloodType`(`BloodType?`, 초기값 null, `birth_info.dart`) non-null일 때만 참—`_nameController`에 `_onNameChanged` 리스너를 달아 타이핑마다 `setState`가 갱신되도록 함(리스너 누락으로 제출 버튼이 갱신 안 되던 버그 수정). 혈액형은 성별과 같은 `PastelToggleRow<BloodType>` 패턴(A/B/AB/O형)이고, `PastelToggleRow`의 `value`는 nullable(`T?`)로 바뀌어 "아직 아무것도 안 고른" 상태를 표현할 수 있음(하위 호환, 기존 non-null 사용처는 그대로 동작). `_birthDate`/`_birthTime`도 여전히 `null` 초기값(과거 1998.08.15·14:30 기본값 버그 수정)이라 미선택 상태에서는 각각 "날짜를 선택해주세요"/"시간을 선택해주세요" 플레이스홀더 표시. 제출 시 `pushReplacementNamed`로 계산 중 화면으로 넘어가 스택에서 제거됨(뒤로가기로 재진입 불가). 날짜/시간 `PastelPillButton`은 `fieldLabel`('태어난 날짜'/'태어난 시간') 옵션 파라미터로 필드 맥락을 더한 시맨틱 라벨을 위젯 내부에서 조합(값이 라벨과 다를 때는 `semanticValue`로 override, 예: 시간 모름 상태의 "모름"). 자시(23시~01시) 경계 안내: 계산 로직(`four_pillars.dart`의 `midnight` 관법)은 그대로 두고, `_isJasiRange`(hour==23 또는 0)이면서 시간 모름 체크가 꺼져 있을 때만 시간 pill 아래에 순화된 안내 문구를 노출(리서치: `docs/research/운세/입력_온보딩_설계.md`). "태어난 시간을 몰라요" 체크박스 아래에는 체크해도 시간 선택 자체는 여전히 필요하다는(대략적인 시간대라도 골라야 그 안에서 최대한 정확히 봐준다는) 안내 문구가 항상 표시됨(2026-07-19). "태어난 시간을 몰라요"/"MBTI를 알고 있어요" 체크박스는 공용 `PastelCheckboxRow` 재사용. 제출 버튼 바로 아래에는 목업(`.input-hint`)과 같은 "이름·날짜·시간·성별·혈액형을 다 채워야 눌러져요" 안내 문구가 있음(2026-07-19). 제출 버튼 `Text('사주 보러가기 🔮')`는 이모지가 스크린 리더에 읽히지 않도록 `semanticsLabel: '사주 보러가기'` 지정. `_FieldLabel`은 `required` 파라미터로 목업(`.field-required::after`)처럼 라벨 뒤에 accent 색 `*` 접미사를 붙일 수 있음(이름/태어난 날짜/태어난 시간/성별/혈액형에 적용, 2026-07-19)—폰트 값(font-size 11px·font-weight 800·letter-spacing .02em)과 margin-bottom(7px)은 목업 그대로. 이름 필드는 더 이상 "(선택)"이 아니며 그 아래 "이름 없이도 괜찮아요 →" 안내 문구는 제거됐고, 태어난 곳(여전히 선택)은 `Text('건너뛰어도 괜찮아요 →')` 안내가 남아있음(장식용 화살표는 `semanticsLabel`로 스크린 리더에서 제외). `BirthInfo.bloodType`(`BloodType? {a,b,ab,o}`, `Gender`와 나란한 nullable 순수 저장 필드, 사주 계산과 무관)은 `BirthInfoStore`(`project/lib/core/storage/`)에 `Gender`와 동일한 enum-name 기반 키로 저장/조회/삭제됨. 날짜/시간 pill을 탭했을 때 뜨는 피커는 목업(`docs/mockups/01-pastel-cute.html` "생년월일 선택 UI — 스크롤 휠 피커로 교체")에 맞춰 Material `showDatePicker`/`showTimePicker`(팝업 달력/스피너 다이얼로그)에서 iOS 스타일 스크롤 휠 피커로 교체됨(2026-07-19)—공용 헬퍼 `showWheelDatePicker`/`showWheelTimePicker`(`project/lib/shared/widgets/wheel_date_picker_sheet.dart`)가 `showModalBottomSheet` 안에 `CupertinoDatePicker`(날짜는 `mode: date`, 시간은 `mode: time`)를 넣고 "확인"을 눌러야만 값이 반영, "취소"/바깥 탭은 기존과 동일하게 null 반환(값 미반영)되는 구조. 앱이 강제 라이트 테마(`main.dart`)이므로 시트 내부를 `CupertinoTheme(brightness: light)`로 감싸 다크 톤이 새어나오지 않게 함. `_pickDate`/`_pickTime`의 미래 날짜 방지 등 기존 제약(firstDate/lastDate)은 그대로 유지 |
| 명식 계산 중 화면 | `project/lib/features/calculating/` | 🟢 | 계산 중 애니메이션. 궤도(달+이모지)는 온보딩 마스코트와 같은 장식용이라 `ExcludeSemantics` 처리, 로딩 문구는 `Semantics(liveRegion: true)`로 갱신 안내 |
| 사주 결과 화면 | `project/lib/features/result/` | 🟢 | `widget.birthInfo`도 라우트 arguments도 없으면 결과를 그리지 않고 `addPostFrameCallback`으로 `BirthInputScreen`까지 `pushAndRemoveUntil`—그 사이엔 로딩 스피너만 표시. 목업 STEP4 리팩터(2026-07-19)로 페이지 제목은 개인화 문구(`'$displayName의 사주팔자'`) 대신 고정 문자열 `'내 사주결과'`이고, 연애·재물·건강·성격 카테고리 카드 그리드와 그 아래 건강운 면책 문구 상시 노출은 이 화면에서 완전히 빠졌다(카드가 참조하던 `ohaeng_readings.dart`의 `categoryReadingsForCombo` 등 함수 정의는 삭제하지 않고 그대로 유지—추후 관심사 선택 화면에서 재사용 예정). 이제 화면 구성은 4기둥·오행 밸런스(우세+2순위 오행 상생상극 콤보 콜아웃, `ohaeng_readings.dart`의 공용 함수 `dominantComboCallout`/`ohaengComboSuffix`—문구 자체는 2026-07-19에 오행 상징을 구체적 비유로 풀어써 더 상세하고 친절하게 확장됨, 결과·공유 카드·리포트·심층 분석이 공유)·공유 카드까지다. 공유하기 버튼 아래 CTA 문구는 `'🎬 광고 보고 상세 리포트 이어보기'`(과거 `'상세 리포트 보기 (무료)'`)로 바뀌었고, 목적지는 아직 광고 게이트/관심사 선택 화면이 없어 기존 `AppRoutes.report`(상세 리포트 화면) 그대로 연결(2026-07-19). 시주(`pillars.hour`)를 모르면 절기 디스클레이머 아래에 3주 계산 사실+재입력 유도 넛지 문구(AppBar "다시 입력하기"와 중복 안 되게 안내 텍스트만) 표시. 메타 라인/헤더 이름 폴백은 `meta_line.dart`의 공용 함수 `buildMetaLine`·`displayNameFor`를 결과·리포트·심층 분석 세 화면이 공유(displayName 자체는 메타 라인·공유 카드에 여전히 쓰임). 메인 콜아웃(결과 화면+공유 카드)은 목업 `.callout`과 여백·글자 크기까지 일치(padding 15/13·font-size 12.5·line-height 1.55). 공유 캡처(`_handleShare`→`shareCapturedCard`, 오프스크린 래퍼→`OffscreenShareCapture`, 둘 다 `shared/`)와 공유 카드 9:16 스캐폴드(`share_card.dart`→`shared/widgets/share_card_scaffold.dart`)는 심층 분석 화면과 공용 로직으로 통합. 오행 밸런스 바(`_OhaengBarRow`)는 한자 대신 한글 오행명으로 `Semantics(label: '$오행 비중 $퍼센트%')` 병합, 섹션 소제목("오행 밸런스")은 파일 내부 private `_SubsectionHeading` 위젯. 그라데이션 공유 버튼(`Text('📸 공유하기')`, `semanticsLabel: '공유하기'`)은 심층 분석 화면과 완전히 같은 구조라 공용 `GradientShareButton`으로 통합. 4기둥(년주/월주/일주/시주) 카드 사이 간격은 목업(`.pillars` gap:7px)에 맞춘 `SizedBox(width: 7)` |
| 상세 리포트 화면 | `project/lib/features/report/` | 🟢 | 오행별 상세 해석. 시주를 모르면 `_pillarRow` 안내 문구 뒤에 재입력 유도 넛지 문구 덧붙임. 심층 분석 진입 링크 `Text('MBTI·관심사로 심층 분석 받기 →')`는 장식용 화살표가 스크린 리더에 읽히지 않도록 `semanticsLabel: 'MBTI·관심사로 심층 분석 받기'` 지정. `_PillarBreakdownTable._pillarRow`의 년주/월주/일주/시주 라벨(`SizedBox(width:44)`)·`_OhaengMeaningCard`의 40x40 원형 배지 한자도 시스템 폰트 확대 시 조용히 잘리던 걸 `FittedBox(fit: scaleDown)`로 감싸 방지(기본 배율에선 시각 변화 없음, 2026-07-15 접근성 감사). 페이지 제목·"명식 한 글자씩 뜯어보기"·"오행 五行 완전 정복"·"오행별 오늘의 풀이 모음" 섹션 제목에 `Semantics(header: true)`를 추가해 TalkBack/VoiceOver 헤딩 단위 탐색을 지원(2026-07-16, 세 섹션 제목이 문구만 다르고 구조가 같아 파일 내부 private `_SectionHeading` 위젯으로 통합함—2026-07-17). 페이지 제목(`'$displayName의 상세 리포트'`)은 심층 분석 화면과 스타일이 겹쳐 공용 `shared/widgets/page_heading.dart`의 `PageHeading`으로 통합(2026-07-17). "오행별 오늘의 풀이 모음"(오행 5개 반복 `_AllReadingsSection`) 전체 끝에 건강운 면책 문구를 딱 1번만 노출 — 결과·심층 분석 화면과 공용 `HealthDisclaimerText`(`shared/widgets/health_disclaimer_text.dart`)로 통합(2026-07-17). "명식 한 글자씩 뜯어보기"의 각 기둥 행 아래에 `_PillarExtras` 위젯을 추가해 납음오행(`core/saju/ganzhi.dart`의 `nayinFor`, 60갑자 조회형 상수)과 공망(`voidBranchIndices`, 일주 기준 순중공망) 배지+짧은 이미지 연상 카피(`report_readings.dart`의 `nayinReadings`/`gongmangReadingCaption`)를 노출—일주 자신에는 공망 배지를 표시하지 않고 년/월/시주 중 지지가 일치하는 기둥에만 표시(2026-07-18, 사람 승인 후 게이트 해제된 콘텐츠 2종) |
| 심층 분석(MBTI·관심사) | `project/lib/features/deep_dive/` | 🟢 | 관심사 선택 + 심층 분석 결과. 직장운 콤보 접미사는 결과 화면과 같은 공용 함수(`ohaengComboSuffix`) 재사용. 관심사 입력 화면의 안내 문구+칩 Wrap도 `PastelToggleRow`와 같은 그룹 시맨틱스(`Semantics(container: true, label: '관심 있는 영역 선택')`) 적용. 결과 화면과 같은 패턴의 공유하기(`DeepDiveShareCard`+`buildDeepDiveShareText`, MBTI 코멘트/관심사 중 하나라도 있어야 버튼 노출, MBTI 박스는 오행색 대신 항상 `accentSoft` 고정) 지원 — 캡처/공유 로직·오프스크린 래퍼·9:16 스캐폴드는 결과 화면과 공용(`shared/share/share_capture.dart`, `shared/widgets/offscreen_share_capture.dart`, `shared/widgets/share_card_scaffold.dart`)으로 통합됨(2026-07-15). `deep_dive_input_screen.dart`의 `initState()`가 `_loadSaved()`를 fire-and-forget으로 던지던 걸 `late final Future<void> _loadFuture`로 바꿔 보관하고, 제출 콜백(`_saveAndContinue`)이 저장 직전 그 Future를 먼저 기다리게 해서 로드 완료 전 즉시 제출 시 birth_input에서 저장해둔 MBTI가 `null`로 덮어써지던 데이터 유실 버그 수정. 그라데이션 공유 버튼도 결과 화면과 완전히 같은 구조라 공용 `GradientShareButton`으로 통합됨(2026-07-15). 심층 분석 결과 화면의 페이지 제목에 `Semantics(header: true)`를 추가해 TalkBack/VoiceOver 헤딩 단위 탐색을 지원(2026-07-16). 같은 페이지 제목 `Text('$displayName의 심층 분석 ✨')`도 장식용 이모지가 스크린 리더에 읽히지 않도록 `semanticsLabel: '$displayName의 심층 분석'` 지정(2026-07-16, 상세 리포트 화면과 페이지 제목 스타일이 겹쳐 공용 `shared/widgets/page_heading.dart`의 `PageHeading`으로 통합함—2026-07-17). `deepDiveInfo.interests.contains(Interest.health)`일 때만 건강운 면책 문구를 조건부 노출(건강 카드 자체가 조건부라 문구도 조건부, 2026-07-17) — 결과·리포트 화면과 공용 `HealthDisclaimerText`(`shared/widgets/health_disclaimer_text.dart`)로 통합(2026-07-17). MBTI 강조 박스는 목업(`.mbti-box .type`)대로 상단에 코드+별칭(`mbtiNicknames`, `deep_dive_readings.dart`)을 `accentText` 색 작은 글씨로, 하단에 기존 코멘트를 별도 줄로 분리 표시 — 공유 카드(`DeepDiveShareCard`)·공유 텍스트(`buildDeepDiveShareText`)도 동일하게 "코드 · 별칭" 줄을 반영(2026-07-18). **2026-07-19 목업 STEP 5 리팩터(`/grill-me` 합의, W6)**: `deep_dive_input_screen.dart` 상단에 실제 광고 SDK 연동 없는 시각적 게이트 `_AdGateCard`(accentSoft→metalSoft 그라데이션, "광고 보고 계속하기")를 추가—탭하면(`_adWatched`) 그 아래 MBTI 확인 뱃지(`_MbtiKnownChip`, `_mbti` non-null일 때만 "✅ 코드 · 별칭")·관심사 선택·제출 버튼이 나타난다. 관심사(`Interest` enum)에 그동안 "항상 기본 노출이라 선택지에서 뺐던" `personality`(성격, 🎭)를 추가해 연애·직장·재물·건강·성격 5종이 모두 동등한 선택 대상이 됨(과거 카테고리 카드가 쓰던 `categoryReadingsForCombo`의 "성격" 항목과 `readingFor`가 그대로 매칭돼 별도 콘텐츠 추가 없음). 제출 버튼 문구는 `'결과 보기'`로 변경(과거 `'심층 분석 보기'`), 제출 목적지는 아직 통합 리포트 화면이 없어 기존 `DeepDiveResultScreen` 유지(W7·W8에서 재배선 예정) |
| 십신(十神) 콘텐츠 확장 | 아직 없음 — 만든다면 `project/lib/features/deep_dive/` 또는 `project/lib/features/ten_gods/` | ⚪ | 사람 결정 대기, 자동화 루프 임의 착수 금지 |
| 사주 계산 로직 | `project/lib/core/saju/` | 🟡 | `ganzhi.dart`(오행 한자 공용 상수 `ohaengHanja`, 오행 상생상극 관계 판별 `ohaengRelationOf`/`OhaengRelation`, `GanzhiPillar.ganzhiIndex60`로 stem/branch 인덱스에서 60갑자 인덱스 역산, 공망 `voidBranchIndices()`, 납음오행 `nayinFor()`+30개 조견표 포함)+`four_pillars.dart`(`dominantOhaeng`/`subDominantOhaeng`). 년주/일주의 `(x % 60 + 60) % 60` mod-60 보정 관용구는 private 헬퍼 `_wrap60()`으로 통합(월간지수/시간지수는 서로 다른 전통 공식이라 그대로 둠, 2026-07-16). 공망·납음오행은 2026-07-18 사람 승인으로 게이트 해제된 콘텐츠(계산 자체는 절기 무관 고정 조회형이라 정확도 이슈 없음, `docs/research/사주팔자/공망.md`·`납음오행.md` 참고). 절기 근사·자시 관법·진태양시·음력 변환 등 나머지 정확도 이슈는 여전히 사람 결정 대기 |
| 공용 위젯 | `project/lib/shared/widgets/` | 🟢 | `PastelPillButton`(`fieldLabel`/`semanticValue`로 필드 맥락 시맨틱 라벨 조합 지원)/`PastelToggleRow`(옵션 버튼 나열을 목업 `.pill-row`처럼 `Wrap(spacing/runSpacing: 8)`로 렌더링—과거엔 `Padding(right: 10)`이라 마지막 항목 뒤에도 불필요한 여백이 남았음, 2026-07-18 대조 발견 수정)/`PastelCard`(선택적 `color`/`showBorder` 파라미터로 배경색이 다른 톤 박스—우세 오행 콜아웃, MBTI 강조 박스, 기둥 칩 등—도 표현, 기본값은 기존 흰 배경+테두리와 동일, 2026-07-16 결과/심층 분석 화면 5곳 중복 Container 통합)/`PastelCheckboxRow`(체크박스+터치 가능 라벨 행)/`GradientShareButton`(accent→metal 그라데이션 공유 버튼, 결과·심층 분석 화면 공용)/`OffscreenShareCapture`(오프스크린 공유 캡처 래퍼)/`ShareCardScaffold`(공유 카드 9:16 공통 스캐폴드)/`HealthDisclaimerText`(건강운 면책 문구, 결과·리포트·심층 분석 3화면 공용, 2026-07-17 중복 통합)/`PageHeading`(페이지 제목 `fontWeight:w800`/`color:ink`/`fontSize:20` + `Semantics(header:true)`, `semanticsLabel` 파라미터로 이모지 섞인 제목도 지원, 상세 리포트·심층 분석 결과 화면 공용, 2026-07-17 중복 통합)/`showWheelDatePicker`·`showWheelTimePicker`(`wheel_date_picker_sheet.dart`—`showModalBottomSheet` 안에 `CupertinoDatePicker`(연/월/일·시/분)를 넣은 스크롤 휠 피커, "확인" 눌러야 값 반영·"취소"/바깥 탭은 null 반환, 생년월일시 입력 화면 전용, 2026-07-19) |
| 공유 캡처 로직 | `project/lib/shared/share/share_capture.dart` | 🟢 | `shareCapturedCard` — RepaintBoundary 캡처→SharePlus 공유→실패 스낵바를 결과·심층 분석 화면이 공용으로 사용. release/profile 빌드에서 `assert`가 제거되며 `debugNeedsPaint` 게터의 `LateInitializationError`가 항상 발생해 이미지 없이 텍스트만 공유되던 버그 수정(`boundary != null`만 확인 후 `toImage()` 직접 시도, 실패해도 기존 catch 폴백 유지) (2026-07-16) |
| 빌드 검증 스크립트 | `project/tool/check_build.sh` | 🟢 | 표준 빌드/린트 체크(pub get→analyze→test→build apk) 일괄 실행 |
| 앱 아이콘 | `project/assets/icon/icon.png` | 🟢 | 생성 도구: `project/test/tool/generate_app_icon.dart` |

상태 범례: 🟢 완료 · 🟡 진행 중/부분 완료(사람 결정 대기 항목 포함) · ⚪ 미착수

## 참고 자료 (원자료, 수정 지양)

| 자료 | 경로 |
|---|---|
| 사주팔자 계산·이론(십신/용신/대운세운/십이운성/신살/공망/택일/정확도 이슈/오픈소스) | `docs/research/사주팔자/` |
| 궁합 | `docs/research/궁합/` |
| MBTI | `docs/research/MBTI/` |
| 운세(시장·트렌드·UI/UX·SNS 전략) | `docs/research/운세/` |
| UI/UX 목업 4종 | `docs/mockups/` |
| 오버나이트 루프 재사용 프롬프트 | `COSMOS_LOOP_JOB.md` |
