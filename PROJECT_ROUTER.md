# PROJECT_ROUTER — 기능별 경로 안내

이 문서는 `COSMOS_BEOMBOO` 앱의 모든 기능이 어느 경로에 있는지 알려주는 네비게이션 문서입니다.
**작업을 시작하기 전, 관련 기능이 이미 이 표에 있는지 먼저 확인하고 해당 경로로 이동해 작업하세요.**
새 기능/화면/모듈을 추가했다면 반드시 아래 표에 항목을 추가하세요.

관련 규칙: [CLAUDE.md](CLAUDE.md) · 진행 로그: [PROGRESS.md](PROGRESS.md)

## 빠른 상태 요약 (2026-07-07 추가)

**이 문서는 매 사이클 누적되는 상세 이력 때문에 아주 커졌다 — 일반 `Read`로 열면 앞부분(이 요약 포함)만 보이고 아래 상세 표의 뒷부분(특히 "십신"·"사주 계산 로직" 행)은 잘려서 안 보일 수 있다.** 아래 15개 행 전체의 상태만 먼저 파악하고, 특정 행의 자세한 설명은 `grep -n "^| 행이름"`으로 찾을 것.

- 앱 진입점 — 🟢 완료
- 공통 테마/디자인 토큰 — 🟡 진행 중 (사람 결정 대기: `accent`+흰 텍스트 대비, `border` 비텍스트 대비 — 브랜드 팔레트 자체를 바꿔야 함)
- 라우팅 — 🟢 완료
- 생년월일시 저장소 — 🟢 완료
- 온보딩 화면 — 🟢 완료
- 생년월일시 정보 입력 화면 — 🟢 완료
- 명식 계산 중 화면 — 🟢 완료
- 사주 결과 화면 — 🟢 완료
- 상세 리포트 화면 — 🟢 완료
- 심층 분석(MBTI·관심사) — 🟢 완료
- **십신(十神) 콘텐츠 확장 (미착수) — ⚪ 미착수. 자동화 루프가 이 행을 임의로 골라 구현을 시작하면 안 됨(사람 결정 대기, 아래 해당 행 참고)**
- 사주 계산 로직 — 🟡 진행 중 (사람 결정 대기: 절기 근사·자시 관법·진태양시 미보정·음력 미변환·역사적 서머타임 미반영, 5가지 정확도 이슈 — 전부 구현 여부 미정)
- 공용 위젯 — 🟢 완료
- 빌드 검증 스크립트 — 🟢 완료
- 앱 아이콘 — 🟢 완료

## 채택된 디자인 컨셉

**파스텔 큐트** — [docs/mockups/01-pastel-cute.html](docs/mockups/01-pastel-cute.html) (흰 배경 + 파스텔 오행 컬러 + 귀여운 마스코트)

**2026-07-08 확인(코드 변경 없음)**: 목업 파일 안의 `<span>`(`"탭 한 번"`, `"짜잔!"`, `"평균 3초"` ×2)과 하단 `<section class="legend">`(오행 컬러 시스템 범례, "성장, 시작, 추진력. 새싹이 자라는 이미지" 등)는 STEP 1~4 실제 화면 콘텐츠가 아니라, 목업 갤러리를 보는 사람에게 단계 전환·색상 선정 이유를 설명하는 **메타 주석**이다 — `report_screen.dart`의 `_ohaengMeaning`(예: "새싹이 자라나는 이미지")과 문구가 살짝 다른 것도 실제 콘텐츠 불일치가 아니라 애초에 서로 다른 목적의 텍스트라 비교 대상이 아님을 명시적으로 확인해둔다(향후 사이클이 이 둘을 잘못 비교해 "불일치 수정"을 시도하지 않도록). STEP 1~4의 실제 화면 카피(온보딩/입력/계산 중/결과)는 전수 대조 결과 이미 정확히 일치함도 재확인.

## 기능 라우팅 표

| 기능 | 상태 | 경로 | 설명 |
|---|---|---|---|
| 앱 진입점 | 🟢 완료 | `project/lib/main.dart` | `CosmosSajuApp` — 시작 시 저장된 `BirthInfo`가 있으면 바로 결과 화면, 없으면 온보딩. OS 홈 화면 앱 이름도 "사주랑"으로 통일(`android/.../AndroidManifest.xml`, `ios/Runner/Info.plist`, `macos/.../AppInfo.xcconfig`). `flutter_localizations`로 로케일을 `ko_KR` 고정해 `showDatePicker`/`showTimePicker`가 한국어로 표시됨. `test/widget_test.dart`에 실제 `AppRoutes.routes` 배선을 그대로 타는 온보딩→입력→계산 중→결과→상세 리포트 전체 여정(end-to-end) 테스트, 저장된 값→"다시 입력하기"→새 값 재제출까지 실제 라우트로 잇는 테스트, 결과→상세 리포트→심층 분석 입력→심층 분석 결과까지 실제 라우트로 잇는 테스트도 포함(개별 화면 스텁 테스트만으로는 못 잡는 라우트/인자 전달/데이터 교체 회귀를 잡기 위함). **2026-07-07 추가(사용자 요청)**: 온보딩부터 시작해 birth_input에서 MBTI를 실제로 체크·선택하고 결과→상세 리포트→심층 분석까지 이어지면 그 MBTI가 정확히 반영되는지 확인하는 테스트도 추가(아래 "생년월일시 정보 입력 화면"/"심층 분석" 행 참고). **2026-07-08 정리(코드 변경, `pubspec.yaml`)**: 더 엄격한 린트 규칙들을 임시로 켜서 점검하던 중(값을 채택하진 않음 — 대부분 상수 관련 스타일이라 실질 이득 없이 30곳 가까이 고쳐야 해서 되돌림) `cupertino_icons` 의존성이 Flutter 기본 템플릿에서 그대로 남아있을 뿐 `lib/`·`test/`·iOS/macOS 러너 어디에서도 실제로 쓰이지 않는 것을 발견 — 제거. `flutter pub get`+`flutter analyze`+`flutter test`(204개 전체 통과)+`./tool/check_build.sh`(APK 194,291,004 bytes, 제거 전보다 118바이트 작음)로 회귀 없음 확인. **2026-07-08 사용자 요청**: "기기에 다크/화이트 테마가 설정되어있다면 저희 앱에서는 무조건 화이트 테마를 따릅니다" — Flutter `MaterialApp` 자체는 `darkTheme:`을 안 주면 시스템이 다크여도 `theme:`을 그대로 쓰긴 하지만 그 암묵적 동작에만 기대고 있었고, 정작 세 곳의 실제 위험을 발견함: (1) `android/app/src/main/res/values-night/styles.xml`이 `Theme.Black.NoTitleBar`를 써서 시스템 다크 모드 기기에서 네이티브 윈도우 배경(`NormalTheme`, 화면 전환·리사이즈 중 Flutter UI 뒤로 잠깐 보임)이 검은색이었음 — 파일 삭제(값 없으면 Android가 항상 `values/styles.xml`(라이트)로 폴백). (2) `ios/Runner/Info.plist`에 `UIUserInterfaceStyle`이 없어 키보드·공유 시트 등 네이티브 UIKit 크롬이 시스템 다크 모드를 따를 수 있었음 — `Light`로 명시 추가. (3) `macos/Runner/Info.plist`에도 `NSRequiresAquaSystemAppearance` true 추가(같은 이유). Flutter 쪽도 암묵적 동작에 기대지 않도록 `main.dart`에 `darkTheme: AppTheme.light`+`themeMode: ThemeMode.light`를 명시하고, `AppBar` 없는 화면(온보딩 등)이 시스템 다크 설정에 따라 상태 표시줄 아이콘 색을 자체 판단하지 않도록 `AnnotatedRegion<SystemUiOverlayStyle>`로 앱 전체에 밝은 배경 기준 스타일(어두운 아이콘)을 강제. 시스템을 다크로 설정한 상태에서 렌더링해도 `Theme.of(context).scaffoldBackgroundColor`가 `AppColors.bg`(라이트)이고 강제된 `SystemUiOverlayStyle` 값이 그대로인지 확인하는 회귀 테스트 추가(`widget_test.dart`, 코드를 되돌려 실제로 실패하는 것까지 확인 후 복원). `flutter analyze`+`flutter test`(223개 전체 통과, +1)+`./tool/check_build.sh`(APK 194,291,004 bytes) 검증 완료. iOS/macOS 쪽 Info.plist 변경은 이 환경에서 빌드 검증이 안 되는 플랫폼이라(CLAUDE.md 참고) 코드 리뷰 수준으로만 확인함. **2026-07-08 커버리지 공백 발견 및 보강**: `flutter test --coverage`로 확인해보니 `lib/` 전체에서 `main.dart`만 라인 커버리지가 66.7%였음 — `main()` 자체는 `runApp()`으로 실제 엔진 바인딩에 붙어 `flutter test`로 직접 실행할 수 없는 게 정상이지만, 그 안에 있던 "저장된 값을 읽다가 실패하면 null로 간주" try/catch 폴백 로직까지 덩달아 한 번도 값으로 검증되지 못하고 있었음 — 이 로직만 `loadInitialBirthInfo()`라는 별도 공개 함수로 분리해 테스트 가능하게 만들고, `SharedPreferencesStorePlatform.instance`를 `getAll()`이 항상 실패하는 가짜 저장소로 바꿔치기해 실제로 null로 폴백하는지 확인하는 테스트 추가(코드를 되돌려 예외가 그대로 새는 것까지 확인 후 재적용) — 분리 후 `main.dart` 커버리지는 76.9%(10/13)로, 남은 3줄은 `main()` 함수 자체(진짜 엔진 진입점이라 원천적으로 단위 테스트 불가능한 최소 잔여분)뿐임을 확인. `flutter analyze`+`flutter test`(227개 전체 통과, +1)+`./tool/check_build.sh`(APK 194,291,004 bytes) 검증 완료 |
| 공통 테마/디자인 토큰 | 🟡 진행 중 | `project/lib/app/theme/` | `app_colors.dart`(파스텔 큐트 팔레트+오행 5색), `app_theme.dart`(ThemeData). `inkSoft` 조정 완료. 오행색을 텍스트로 쓸 때는 `ohaengColors`(배경/아이콘 전용) 대신 `ohaengTextColors`(WCAG AA 4.5:1 통과하도록 진하게 조정, `test/app/theme/app_colors_contrast_test.dart`로 검증)를 쓴다. 마찬가지로 `accent`를 텍스트로 쓸 때는 `accentText`(같은 방식으로 조정, 온보딩 워드마크에서 사용)를 쓴다. **`accent`(#FF6B8A) 배경 위 흰(`accentInk`) 텍스트만 여전히 WCAG AA 미달(2.72:1, `test/app/theme/app_colors_contrast_test.dart`로 값 고정됨) — 브랜드 버튼 색 자체를 바꿔야 해 사람 결정 대기 중.** 범위: 주요 CTA 버튼("시작하기 →" 등, 목업 `.btn-primary`도 동일하게 accent+흰 글자를 써서 의도된 조합)만 해당(2026-07-05엔 `PastelToggleRow`의 선택된 토글·심층 분석의 선택된 관심사 칩도 같은 조합을 잘못 재사용해 동일하게 미달이었으나, 2026-07-06에 목업(`.pill.is-active`)과 다시 대조해 실제로는 accentSoft+accentText 조합임을 발견하고 바로잡음 — 아래 "공용 위젯" 행 참고). **2026-07-06 추가 발견**: 지금까지는 WCAG 1.4.3(텍스트 대비, 4.5:1) 기준만 확인했는데, `border`(#F1E4DC)가 `PastelCard`/`PastelPillButton`/`PastelToggleRow`(비선택)/`ShareCard` 등 카드·버튼 테두리 전반에 쓰이는 **비텍스트** 요소라 WCAG 1.4.11(Non-text Contrast, 3:1 기준) 대상인데, `bg`/`bgCard` 위에서 실측 약 1.2:1로 거의 안 보이는 수준임을 확인 — 저시력 사용자에게 카드/버튼 경계 자체가 안 보일 수 있음. 선택된 토글의 `accent` 테두리도 2.72:1로 3:1 미달(배경·글자색은 바로잡았지만 목업이 테두리는 그대로 accent를 쓰므로 이 부분은 여전히 미달). `accent`/`accentInk`와 같은 브랜드 팔레트 결정 사항이라 별도 항목이 아니라 같은 사람 결정 대기에 편입, 값 고정 회귀 테스트 추가(`test/app/theme/app_colors_contrast_test.dart`). **2026-07-06 목업 대조로 발견한 별개 문제(모서리 반경)**: `app_theme.dart`의 CTA 버튼이 `BorderRadius.circular(999)`(완전히 둥근 알약)를 썼는데, 목업의 `.btn-primary`는 실제로 16px 모서리 반경만 씀 — 값을 16으로 수정(같은 문제가 `PastelPillButton`/`PastelToggleRow`/`_InterestChip`에도 있었음, "공용 위젯"/"심층 분석" 행 참고, 그쪽은 `.pill`의 12px 기준). **2026-07-06 목업 대조로 발견한 더 큰 누락**: `app_theme.dart`에 `inputDecorationTheme`이 아예 없었음 — 이름/출생지 `TextField`(birth_input_screen.dart, "생년월일시 정보 입력 화면" 행 참고)가 목업(`.text-input`: `border:1.5px solid var(--app-border)`, `border-radius:12px`, `padding:10px 13px`)과 전혀 무관하게 Flutter 기본 Material 텍스트 필드 스타일을 그대로 쓰고 있었던 것 — 다른 위젯(버튼·토글·카드)은 전부 커스텀 위젯/테마로 파스텔 큐트 스타일을 입혀뒀는데 유일하게 텍스트 필드만 이 적용에서 빠져 있었음. 전역 `inputDecorationTheme`을 추가해 목업 값(보더색·굵기·반경·패딩)을 그대로 적용, 목업엔 명시돼 있지 않은 포커스 상태는 접근성을 위해 `accent` 색 테두리로 별도 추가. `grep`으로 전수 확인한 결과 이 두 필드가 앱 전체에서 유일한 `TextField` 사용처라 영향 범위가 정확히 그 둘로 한정됨을 확인. **2026-07-07 죽은 설정 제거**: `ThemeData`에 있던 `cardTheme`(배경 `bgCard`, 반경 24px, `border` 테두리)이 실제로는 한 번도 적용된 적 없는 설정이었음을 발견 — `grep`으로 `lib/` 전체를 확인한 결과 Flutter 내장 `Card` 위젯은 앱 어디서도 안 쓰이고, "카드"처럼 보이는 UI는 전부 커스텀 `PastelCard`/`ShareCard`/`_PillarCard`/`_CategoryCard` 등 `Container` 기반 위젯이 담당함(각자 자기 `BoxDecoration`을 직접 지정). `cardTheme`을 참조하는 곳도 `app_theme.dart` 자기 자신뿐이었어 실질적으로 죽은 코드였음 — 향후 개발자가 이 설정을 고치면 카드 스타일이 바뀔 거라고 착각하지 않도록 제거. **2026-07-07 죽은 설정 추가 조사(textTheme)**: `cardTheme` 제거를 계기로 같은 `ThemeData`의 `textTheme`(`headlineMedium`/`titleLarge`/`bodyLarge`/`bodyMedium`)도 죽은 설정인지 의심했으나(코드 어디서도 `Theme.of(context).textTheme.xxx`로 명시 참조하지 않음), 실제로 위젯 테스트를 작성해 렌더링 스타일을 직접 확인한 결과 셋은 진짜 살아있었음 — `titleLarge`는 모든 화면의 `AppBar` 제목(굵게+ink색), `bodyLarge`는 `TextField` 입력 텍스트(birth_input의 이름/출생지 필드, ink색), `bodyMedium`은 `AlertDialog` 본문·스타일 없는 일반 `Text`(inkSoft색)에 Flutter Material 3가 암묵적으로 적용해주고 있었음. `headlineMedium`만 이 앱이 실제로 쓰는 어떤 위젯의 기본값도 아니어서(`AlertDialog` 제목은 커스터마이즈 안 한 `headlineSmall`을 씀) 진짜 죽은 설정으로 확인돼 제거함. 이 착각하기 쉬운 암묵적 의존(명시적 코드 참조가 없어 자칫 또 죽은 설정으로 오인하기 쉬움)이 재발하지 않도록 `test/app/theme/app_theme_test.dart`를 신설해 세 항목의 실제 렌더링 값(색상·굵기)을 고정하는 회귀 테스트 3개 추가(각각 값을 깨뜨려 실패 확인 후 복원. **2026-07-07 확인(코드 변경 없음)**: 같은 흐름으로 `ColorScheme.fromSeed(...)`도 `cardTheme`처럼 죽은 설정인지 의심했으나(코드 어디서도 `Theme.of(context).colorScheme.xxx`로 명시 참조하지 않음), `surface`/`onSurface`/`primary`/`onPrimary`는 `AlertDialog` 배경·`Checkbox` 활성 색 등 Material 3 여러 위젯의 기본 색 해석에 광범위하게 관여해 `textTheme`과 마찬가지로 살아있는 설정으로 판단 — 실제로 위젯 테스트로 `AlertDialog` 배경색을 찍어본 결과 순수 흰색(`bgCard`)이 아니라 `surface`에 Material 3 엘리베이션 틴트(seed color인 `accent` 기반)가 옅게 섞인 핑크빛 오프화이트(약 `#F6E4E5`)였음 — 목업엔 다이얼로그 자체가 없어 대조할 기준값은 없지만, 파스텔 핑크 톤 앱 전체 분위기와 자연스럽게 어울려 별도 수정 없이 현재 상태를 유지하기로 함(`dialogTheme.surfaceTintColor`를 명시적으로 끄면 순수 흰색이 되지만, 그럴 근거가 없어 보류). **2026-07-07 목업 대조로 발견한 누락(그림자)**: 목업의 `.btn-primary`가 `box-shadow:0 10px 22px -10px accent(70%)`(브랜드색이 은은하게 번지는 글로우)를 쓰는데, `elevatedButtonTheme`엔 그림자 관련 설정이 아예 없어 지금까지 CTA 버튼("시작하기 →"·"사주 보러가기"·"심층 분석 보기")에 아무 그림자도 없었음 — CSS box-shadow의 offset/blur/spread를 Flutter의 elevation 모델로 그대로 옮길 수는 없어(서로 다른 그림자 모델), `elevation: 6`/`shadowColor: accent(70% 불투명도)`로 같은 "브랜드색 글로우" 느낌을 근사했다(정확한 픽셀 재현이 아니라 근사임을 명시). "공유하기" 버튼은 자체 그라데이션 `Container`로 감싸며 이미 `elevation: 0`/`shadowColor: Colors.transparent`를 명시적으로 덮어쓰고 있어 영향 없음. `test/app/theme/app_theme_test.dart`에 이 값을 고정하는 회귀 테스트 추가(제거해서 테스트가 실제로 실패하는 것까지 확인 후 복원)). **2026-07-08 확인(Explore 서브에이전트, 코드는 문서화만 보강)**: `ohaengColors`(오행 원색 맵)와 그 안에서만 쓰이는 `wood`/`fire`/`water`가 실제 앱 화면 어디서도 참조되지 않는다는 걸 재확인 — `cardTheme` 제거 사례와 비슷해 보였지만, 유일한 소비처가 `test/tool/generate_app_icon.dart`(앱 아이콘 생성 도구)라 완전히 죽은 코드는 아님(제거하면 아이콘 생성이 깨짐) — 향후 사이클이 같은 의심으로 재조사·오삭제하지 않도록 `app_colors.dart`의 `ohaengColors` doc-comment에 현재 유일한 소비처를 명시해둠(로직 변경 없음). `flutter analyze`+`flutter test`(220개 전체 통과)+`./tool/check_build.sh`(APK 194,291,004 bytes) 검증 완료. **2026-07-08 위 기록 오류 정정**: 방금 위 항목에서 "매 `flutter test`마다 실행됨"이라고 적었는데, 이 파일은 `_test.dart`가 아니라 `generate_app_icon.dart`로 끝나(파일 자체 doc-comment에도 "일반 flutter test 실행 시 자동으로 수집되지 않는다"고 명시돼 있음) `flutter test`의 기본 파일 탐색 대상이 아님을 재확인(`flutter test 2>&1 \| grep -c generate_app_icon` → 0으로 실측) — `flutter test test/tool/generate_app_icon.dart`처럼 직접 지정해야만 실행되는 수동 스크립트임을 정정. 즉 이 원색 맵은 실제로는 CI/자동 테스트 어디서도 값이 검증되지 않고, 아이콘을 수동으로 재생성할 때만(그리고 그 결과물을 사람이 육안으로 볼 때만) 값 오류가 드러남 — 앞서 내린 "완전히 죽은 코드는 아니다"라는 결론 자체는 유효하지만(제거하면 그 수동 재생성 자체가 깨짐), 자동 검증 커버리지는 이 결론이 시사하는 것보다 더 낮다는 점을 바로잡음(코드 변경 없음, 기록 정정만). **2026-07-08 textTheme/cardTheme과 같은 종류의 미검증 항목 추가 발견**: `inputDecorationTheme`(2026-07-06에 목업 `.text-input` 대조로 추가된, 이름/출생지 TextField 전역 스타일)도 코드 어디에도 `Theme.of(context).inputDecorationTheme`으로 명시 참조되지 않고 TextField가 암묵적으로 주워 쓰는 구조인데, 지금까지 이 값 자체(테두리색·두께·반경·채움색·포커스 시 테두리색)를 확인하는 테스트가 하나도 없었음 — `app_theme_test.dart`에 textTheme/elevatedButtonTheme과 같은 방식(`Theme.of(context).inputDecorationTheme` 직접 읽기)으로 값 고정 테스트 추가(focusedBorder 색을 일부러 accent→border로 바꿔 실제로 실패하는 것까지 확인 후 복원). `flutter analyze`+`flutter test`(221개 전체 통과, +1)+`./tool/check_build.sh`(APK 194,291,004 bytes) 검증 완료. **2026-07-08 같은 종류의 미검증 항목 3건 추가 발견**: `elevatedButtonTheme`의 그림자 색/elevation은 이미 값으로 고정돼 있었지만 같은 스타일 안의 모서리 반경(16px, 2026-07-06 대조 발견)·패딩(28/16)은 값으로 확인한 적이 없었고, `appBarTheme`(backgroundColor/foregroundColor/elevation/centerTitle)과 `scaffoldBackgroundColor`도 모든 화면이 `Theme.of(context)`로 명시 참조 없이 암묵적으로 주워 쓰는데(모든 화면이 `AppBar(title: Text(...))`만 쓰고 색을 직접 지정하는 곳이 하나도 없음) 지금까지 값 자체를 확인한 테스트가 전혀 없었음 — `app_theme_test.dart`에 반경·패딩 assertion 추가 + `appBarTheme`/`scaffoldBackgroundColor` 새 테스트 추가(각각 값을 일부러 바꿔 실패하는 것까지 확인 후 복원). `flutter analyze`+`flutter test`(222개 전체 통과, +1)+`./tool/check_build.sh`(APK 194,291,004 bytes) 검증 완료 |
| 라우팅 | 🟢 완료 | `project/lib/app/router.dart` | `AppRoutes` — birthInput/calculating/result 등록. 온보딩은 `routes`에 없고 `main.dart`의 `home:`에서만 결정(이유는 파일 상단 doc-comment 참고). 신규 화면 추가 시 이 표에 등록. **2026-07-08 테스트 커버리지 보강**: `router_test.dart`가 지금까지 각 라우트를 `initialRoute`로 곧장 도달해 개별 화면만 확인했을 뿐, 실제 앱처럼 birthInput→(pushNamed)→calculating→(pushReplacementNamed)→result로 진짜 스택을 쌓아본 적이 없었음 — 이 체이닝이 정확히 "BirthInputScreen이 스택에 남아 ResultScreen에 자동 뒤로 가기 버튼이 생기는" 실제 버그(위 "생년월일시 정보 입력 화면" 행 참고)의 원인이라, 그 스택 모양 자체를 명시적으로 검증하는 통합 테스트 추가(뒤로 가기 버튼 존재 확인 + 탭하면 BirthInputScreen으로 정확히 돌아가는지까지). 이 테스트가 실제로 `SharedPreferences.setMockInitialValues({})`(기존 그룹은 저장을 거치지 않아 필요 없었음) 없이는 저장 호출에서 멈추는 것도 확인 후 `setUp()`에 추가. |
| 생년월일시 저장소 | 🟢 완료 | `project/lib/core/storage/` | `BirthInfoStore` — `SharedPreferences`로 마지막 입력값(성별·분(`minute`) 포함) 저장/조회/삭제. 앱 재실행 시 온보딩 스킵에 사용. `DeepDiveInfoStore`도 같은 파일 위치·패턴으로 MBTI·관심사 저장(아래 "심층 분석" 행 참고). **2026-07-06 버그 수정**: `DeepDiveInfoStore.load()`가 MBTI 네 축을 `MbtiEi.values.asNameMap()[eiName]!`처럼 null 단언(`!`)으로 복원하고 있어, 저장된 문자열이 현재 enum 값과 하나라도 안 맞으면(예: 향후 MBTI 축 enum 이름 변경) null-check 예외를 던지는 실제 버그를 발견 — `BirthInfoStore.load()`의 성별 복원(`asNameMap()[]` 결과를 그대로 확인, null 단언 없음)과 달리 저장소 자체가 안전하지 않았고, 유일한 호출부(`deep_dive_input_screen.dart`)가 try/catch로 감싸고 있어 화면은 안 깨졌지만 다른 호출부가 생기면 위험한 상태였음. null 단언을 제거해 안 맞으면 mbti 전체를 null로 처리하도록 고침 — 실제로 원래 코드로 되돌려 예외가 재현되는 것까지 확인 후 다시 수정. `BirthInfoStore`에도 성별 문자열이 enum과 안 맞을 때 안전하게 null로 복원되는지 확인하는 회귀 테스트, `DeepDiveInfoStore`에도 관심사·MBTI 각각의 안전한 폴백을 확인하는 회귀 테스트 추가 |
| 온보딩 화면 | 🟢 완료 | `project/lib/features/onboarding/` | 워드마크("사주랑") + 헤드라인("내 안의 오행, 3분이면 알 수 있어요") + 설명 + "시작하기 →"(→ birthInput 실제 이동) + "어떻게 계산되나요?"(60갑자 변환·절기 근사 한계를 설명하는 다이얼로그, `report_screen.dart` 안내 문구와 같은 취지) — 목업 STEP 1 카피와 실제로 맞춤. 저장된 정보가 없을 때만 보임. 장식용 마스코트 아이콘은 `ExcludeSemantics`로 스크린 리더에서 제외. **2026-07-05 수정**: 다이얼로그 안내 문구에 자시(23시~1시)·지역 시차 보정 한계도 추가 — 절기 근사만 안내하고 있던 것을 리서치로 새로 밝혀진 두 가지 정확도 이슈까지 정직하게 알리도록 확장(아래 "사주 계산 로직" 행 참고). **2026-07-06 수정**: "시작하기" 버튼 카피가 목업(docs/mockups/01-pastel-cute.html STEP 1)에는 "시작하기 →"로 화살표가 붙어 있는데 실제 구현엔 화살표가 빠져 있던 걸 발견해 목업과 일치하도록 수정(`widget_test.dart`/`onboarding_screen_test.dart`의 텍스트 검증도 함께 갱신). **2026-07-06 추가 발견**: 다이얼로그 안내 문구에 음력 입력이 양력으로 변환되지 않고 그대로 계산에 쓰인다는 한계도 새로 추가(아래 "사주 계산 로직" 행 참고, 절기 근사·자시·진태양시에 이은 네 번째 정확도 이슈). **2026-07-06 안전성 확인(코드 변경 없음)**: result_screen.dart/share_card.dart에서 실제로 겪은 "시스템 글자 크기 2배 확대 시 RenderFlex overflow" 위험이 이 화면에도 있는지 점검 — 온보딩은 세로로 쌓이는 단순 Column 레이아웃이라 실제로는 재현되지 않음을 확인, 회귀 방지용 테스트로 고정. **2026-07-06 목업 대조로 발견한 마스코트 불일치 수정**: 지금까지 장식용 마스코트가 그냥 원(`accentSoft` 배경)에 반짝임 아이콘(`Icons.auto_awesome_rounded`)을 얹은 형태였는데, 목업(`.mascot .blob`)은 완전한 원이 아니라 CSS `border-radius: 44% 56% 58% 42% / 48% 44% 56% 52%`(모서리별로 가로/세로 반경이 다른 비대칭 "블롭" 도형)에 `accentSoft`→`woodSoft` 그라데이션, 옅은 `accent` 테두리(30% 불투명도), 반짝임 아이콘 대신 점 두 개짜리 "눈"이 있는 캐릭터였음 — Flutter의 `Radius.elliptical(가로, 세로)`가 CSS의 `/`로 구분된 가로/세로 분리 반경과 정확히 대응돼 `BorderRadius.only()`로 그대로 포팅, 점 두 개(눈)로 교체. 목업의 `float`/`twinkle` CSS 애니메이션과 블롭 주위를 도는 별도 반짝이(`​.spark`) 요소는 이번엔 스코프 밖으로 남겨둠(순수 장식 요소라 낮은 우선순위, 애니메이션 추가는 별도 작업으로 고려 가능). **2026-07-06 목업 대조로 발견한 스타일 불일치 수정**: "어떻게 계산되나요?" 링크가 목업(`.link-quiet`)은 밑줄 있는 링크(12px, font-weight 700)인데 우리 구현은 밑줄이 아예 없고 크기·굵기도 달랐음(기본 크기, w600) — 같은 사이클 계열의 "상세 리포트 보기"(`.report-link`) 링크에서 겪은 것과 똑같은 종류의 누락이라 같은 방식(`TextDecoration.underline`, 밑줄 색 `inkSoft`, 목업 값 12px/700)으로 수정. **2026-07-07 목업 대조로 발견한 마스코트 크기·여백 불일치 수정**: (1) 마스코트 블롭이 목업(`.mascot`: 104x104)과 달리 120x120이었음 — 2026-07-06에 계산해둔 모서리 반경(퍼센트 기반 `Radius.elliptical` 값)도 잘못된 기준(120px)으로 계산돼 있어서, 104px 기준으로 다시 계산(46,50/58,46/60,58/44,54)해 함께 수정. 눈(점) 크기도 목업(`.face i`: 6x8)과 달리 8x10이었던 걸 6x8로 수정(가로 여백 7은 `.face{gap:14px}`와 이미 일치해 그대로 둠). (2) 목업의 `.screen-body`는 자식 사이에 균일한 `gap:18px`를 쓰는데(마스코트만 자기 `margin-bottom:4px`가 더해져 22px) — 지금까지 마스코트→워드마크 20px, 워드마크→헤드라인 12px, 헤드라인→설명문 12px이던 것을 각각 22/18/18로 수정. 화면 하단 버튼을 밀어내는 `Spacer(flex:)` 기반 배치는 목업의 고정 레이아웃과 다른 반응형 전략(실기기 화면 높이 대응)이라 의도적으로 그대로 둠. **2026-07-07 목업 대조로 발견한 화면 바깥 패딩 불일치 수정**: 목업(`.screen-body`)의 좌우 padding은 20px인데 이 화면은 32px이었음 — 20으로 수정(`flutter test` 전체로 회귀 없음 확인). **2026-07-07 목업 대조로 발견한 누락 요소 추가**: 목업의 마스코트 반짝이 장식(`.mascot .spark`, ✨/⋆ 두 개)이 지금까지 아예 구현돼 있지 않았음 — `float`/`twinkle` CSS 애니메이션은 순수 장식용 낮은 우선순위 항목이라 이전과 같은 이유로 여전히 스코프 밖으로 남겨두되, 정적 위치·크기(top:-6/right:6 14px, bottom:2/left:-10 10px)만 추가. 기존 마스코트 `Container`를 `Stack(clipBehavior: Clip.none)`으로 감싸 블롭 경계 밖으로 살짝 나가는 위치를 표현(`onboarding_screen_test.dart`/`widget_test.dart` 및 전체 테스트로 회귀 없음 확인). **2026-07-08 안내 문구 최신화 공백 발견**: 2026-07-07에 "사주 계산 로직" 행에 다섯 번째 정확도 이슈(한국의 역사적 서머타임 1948~1960·1987~1988년 미반영)를 리서치로 새로 문서화했는데, 계산 로직 자체의 산술 버그(날짜 차이 계산의 DST 오프셋 문제)만 그날 바로 고쳤을 뿐, 앞선 네 가지 이슈(절기·자시·진태양시·음력)와 달리 이 다이얼로그 안내 문구엔 반영하는 걸 놓쳤던 공백을 발견 — 서머타임 한계 안내 문장 추가(report_screen.dart도 동일, 아래 "상세 리포트 화면" 행 참고), 회귀 테스트 추가(문구 제거해 실제로 실패하는 것까지 확인 후 복원). `flutter analyze`+`flutter test`(222개 전체 통과, 기존 테스트에 assertion만 추가)+`./tool/check_build.sh`(APK 194,291,004 bytes) 검증 완료 |
| 생년월일시 정보 입력 화면 | 🟢 완료 | `project/lib/features/birth_input/` | 이름(선택, 20자 제한), 생년월일(datePicker), 양력/음력(`PastelToggleRow`에 `semanticLabel: '양력 또는 음력'` 적용 — 목업 `aria-label`과 동등), 태어난 시간(timePicker+`CheckboxListTile`로 전체 행 탭 가능한 모름 체크), 성별(`Gender` enum, `semanticLabel: '성별 선택'` 적용, `BirthInfo.gender`로 실제 전달됨), 출생지(선택, 30자 제한, `BirthInfo.birthPlace`로 실제 전달됨) 구현 완료 — timePicker가 고른 분(`TimeOfDay.minute`)까지 `BirthInfo.minute`로 실제 전달됨(2026-07-05 수정: 예전엔 `hour`만 있고 `minute` 필드 자체가 없어 목업 STEP 4 "오후 2시 30분生"과 달리 분이 통째로 사라지던 문제, `meta_line.dart`/`BirthInfoStore`까지 함께 갱신). 글자 수 제한은 `share_card.dart`의 폭 고정 레이아웃이 아주 긴 입력에 깨지는 걸 막기 위함. 이름/출생지 `TextField`는 `Semantics(label:)`로 감싸 스크린 리더가 hintText뿐 아니라 필드 용도도 함께 읽도록 처리. datePicker/timePicker는 "확인" 확정뿐 아니라 "취소"해도 원래 값이 유지되는지도 검증됨. "사주 보러가기" → `BirthInfoStore.save()` 후 `BirthInfo`를 담아 calculating으로 실제 이동. **2026-07-06 관례 확인**: "태어난 시간을 몰라요" 체크박스를 체크했다 다시 해제해도 시간값(`_birthTime`)이 엉뚱하게 초기화되지 않고 그대로 유지되는지(오후 2시 30분 기본값 기준) 지금까지 직접 값으로 확인한 적이 없었음 — 심층 분석의 MBTI 체크박스에서 발견한 것과 같은 종류의, 이미 성립하던 관례를 여기서도 회귀 테스트로 못박음. **2026-07-06 버그 수정**: `_submit()`이 `BirthInfoStore.save()`를 await하는 async 함수라, "사주 보러가기"를 빠르게 두 번 누르면 calculating 라우트가 중복 push되는 실제 버그를 재현·확인 — 재진입을 막는 `_isSubmitting` 플래그로 수정(`tester.tap()`의 프레임 타이밍이 화면마다 달라 재현이 불안정해, `onPressed`를 직접 두 번 동기 호출하는 결정론적 테스트 기법으로 확정 검증). **2026-07-06 테스트 커버리지 보강**: 이름 필드는 "공백만 입력→null", "20자 초과 입력→20자까지만 반영" 두 경계값이 이미 테스트돼 있었는데, `_submit()`에서 정확히 같은 트리밍/길이 제한 로직(`trimmedBirthPlace.isEmpty ? null : trimmedBirthPlace`, `maxLength: 30`)을 쓰는 출생지(`birthPlace`) 필드는 지금까지 이 두 경계값을 값으로 확인한 적이 없었던 비대칭을 발견 — 이름 필드와 대칭되는 테스트 2건 추가(공백만 입력 시 null 전달, 35자 입력해도 30자까지만 반영). **2026-07-06 버그 수정(2)**: 더블탭 가드용 `_isSubmitting` 플래그를 `true`로 바꾸는 지점만 있고 다시 `false`로 되돌리는 지점이 전혀 없던 실제 버그를 발견 — `_submit()`은 `Navigator.pushNamed()`로 화면을 교체가 아니라 그 위에 쌓기만 해서 `BirthInputScreen` 인스턴스가 스택에 그대로 남는데, 사용자가 calculating 화면에서 뒤로가기로 이 화면에 돌아오면 그 인스턴스의 `_isSubmitting`이 계속 `true`라 "사주 보러가기"를 아무리 눌러도 반응이 없는(영구 먹통) 상태가 됐다. `pushNamed()`가 반환하는 Future가 완료되는 시점(뒤로가기로 돌아왔을 때)에 맞춰 플래그를 되돌리도록 수정(`await Navigator...pushNamed(...); if (mounted) _isSubmitting = false;`) — 수정을 일부러 되돌려 새 회귀 테스트("한 번 제출한 뒤 뒤로가기로 돌아와도...")가 실제로 실패하는 것까지 확인한 뒤 재적용. **2026-07-06 목업 대조 감사(코드 변경 없음)**: docs/mockups/01-pastel-cute.html의 STEP 2(정보 입력)를 다시 자세히 대조해보니, 목업에는 실제로 "이름" 입력 필드가 아예 없다(태어난 날짜·양력음력·태어난 시간·성별·태어난 곳 순서만 있음) — 처음엔 목업에 없는 필드를 임의로 추가한 게 아닌지 의심했으나, 목업 STEP 4(결과 화면)의 헤더가 "민지님의 사주팔자 ✨"처럼 실제 이름을 쓰는 걸 보면 그 이름을 어디서 받는지는 목업 자체가 답을 안 주는 정적 데모의 한계였을 뿐임을 확인 — 이름 필드는 목업의 그 데모 카피를 실제 사용자 데이터로 구현 가능하게 만들기 위해 STEP 2에 의도적으로 추가한 것(신규 기능)이라 목업과의 불일치가 아니다. 향후 사이클이 같은 의심으로 재조사하지 않도록 여기 명시해둠. **2026-07-06 안전성 확인(코드 변경 없음)**: result_screen.dart/share_card.dart에서 실제로 겪은 "시스템 글자 크기 2배 확대 시 RenderFlex overflow" 위험을 점검 — 이 화면은 ListView + PastelPillButton/PastelToggleRow(고정 높이 없이 내용에 맞춰 늘어남)라 실제로는 재현되지 않음을 확인, 회귀 방지용 테스트로 고정. **2026-07-06 추가 감사(코드 변경 없음)**: 이 화면과 심층 분석 입력 화면에서 실제로 겪은 "_isSubmitting을 true로만 바꾸고 다시 false로 되돌리지 않는" 버그 패턴이 `lib/features/` 다른 곳에도 더 있는지 전수 점검 — `calculating_screen.dart`는 `Navigator.pushReplacementNamed`로 화면 자체를 교체(destroy)해버려 사용자가 같은 인스턴스로 "돌아올" 경로 자체가 없어 이 버그 클래스가 애초에 적용되지 않음을 확인했고(`_orbitAnimationDecided`는 종류가 다른 1회성 판단 플래그일 뿐 UI를 막는 재진입 가드가 아님), `_messageTimer`도 `dispose()`에서 동기적으로 `cancel()`되어 누수 없음을 확인. `result_screen.dart`(`_handleShare`, `_resetAndReenter`)는 애초에 이런 플래그 자체가 없어(더블탭을 막는 가드가 없다는 뜻이지 "멈춘 가드"가 있는 건 아님, 별도 행에서 이미 이 부분은 실제로 안전함을 재현 테스트로 확인해둠) 해당 없음. 다른 화면들(`ResultScreen`/`DeepDiveResultScreen`/`ReportScreen`/`OnboardingScreen`)은 전부 `StatelessWidget`이라 애초에 이런 상태 플래그를 가질 수 없음 — 이 버그 클래스는 이번 세션에서 발견된 두 곳(이 화면, 심층 분석 입력 화면)이 전부였고 둘 다 수정 완료된 것으로 결론. **2026-07-06 목업 대조로 발견한 스타일 누락**: 이름/출생지 `TextField` 둘 다 목업(`.text-input`)과 무관하게 Flutter 기본 텍스트 필드 스타일을 쓰고 있었음 — `app_theme.dart`에 전역 `inputDecorationTheme`을 새로 추가해 해결(자세한 내용은 "공통 테마/디자인 토큰" 행 참고), 이 화면의 코드 자체는 변경 없음(테마 적용만으로 해결). **2026-07-06 목업 대조로 발견한 스타일 불일치 수정**: "태어난 시간을 몰라요" 체크박스 문구가 목업(`.check-row`)의 12px과 달리 기본 글자 크기였음 — `fontSize: 12`로 수정(전체 커버리지 재확인 결과 최근 사이클들의 추가 코드는 모두 100% 커버 유지 중임도 함께 확인). 위젯 테스트 32개(`test/features/birth_input_screen_test.dart`). **2026-07-07 변경(사용자 요청)**: MBTI 입력(원래 심층 분석 입력 화면, `deep_dive_input_screen.dart`에 있던 "MBTI를 알고 있어요" 체크박스 + 4개 축 `PastelToggleRow`)을 이 화면으로 옮겨왔다 — 생년월일시와 함께 한 번에 받는 편이 자연스럽다는 요청. 기본은 꺼둔 채(체크해야 축 4개가 보임), 제출 시 `BirthInfoStore.save()`와 별도로 `DeepDiveInfoStore.save()`도 호출해 MBTI(체크 안 했으면 null)와 관심사(항상 전체 선택 기본값)를 함께 저장한다 — 관심사는 이 화면에서 고르지 않고, 나중에 상세 리포트의 "MBTI·관심사로 심층 분석 받기"에서 고르면 여기서 저장해둔 MBTI를 그대로 이어받는다(아래 "심층 분석" 행 참고). **2026-07-07 테스트 커버리지 보강**: 성별 토글에 이미 있던 "탭하면 스크린 리더용 selected 시맨틱스가 바뀐다" 테스트와 같은 패턴을 새로 옮겨온 MBTI 축 토글에도 추가 — `PastelToggleRow` 자체의 위젯 테스트와 별개로, 이 화면에 실제 연결된 `onChanged`/`semanticLabel` 배선이 스크린 리더 기준으로도 정확한지 확인한다(`onChanged`를 임시로 no-op으로 바꿔 테스트가 실제로 실패하는 것까지 확인 후 원복). **2026-07-07 커버리지 갭 발견**: `flutter test --coverage`로 `coverage/lcov.info`를 직접 분석해보니, MBTI 4축 중 T/F·J/P 축의 `onChanged` 콜백(`_tf`/`_jp` 갱신)이 지금까지 단 한 번도 실행된 적이 없었음을 발견 — MBTI 질문이 `deep_dive_input_screen.dart`에서 이 화면으로 옮겨오면서 그쪽에 있던 "네 축 전부 바꿔보는" 테스트가 새 화면 기준으로 다시 작성되지 않아 생긴 공백이었음(옮겨온 테스트는 E/I·S/N 두 축만 확인). 네 축을 전부 뒤집어 "INFP"로 저장되는지 확인하는 테스트를 추가해 커버리지 갭을 닫음(추가 후 `flutter test --coverage`로 재확인, 우연이 아니라 실제로 그 두 줄이 히트로 바뀌는 것까지 확인). **2026-07-07 목업 대조로 발견한 스타일 불일치 수정**: "이름 없이도 괜찮아요 →"·"건너뛰어도 괜찮아요 →" 안내 문구가 목업(`.skip`: 11px, font-weight 700)과 달리 13px에 기본 굵기였음 — 두 문구 모두 11px/w700으로 수정해 통일(태어난 곳 문구만 목업에 실제로 있고 이름 문구는 목업에 없는 신규 필드용이지만, 같은 UI 패턴이라 같은 스타일로 맞춤). **2026-07-07 테스트 완전성 보강**: `deep_dive_input_screen.dart`에 있던 "MBTI를 알고 있어요를 껐다가 다시 켜도 그 사이에 고른 축 선택은 그대로 유지된다" 테스트가 MBTI 질문이 이 화면으로 옮겨오면서 새 화면 기준으로 다시 작성되지 않았던 걸 발견 — `onChanged`에 축 값을 기본값으로 되돌리는 리셋 버그를 임시로 주입해 테스트가 실제로 실패하는 것까지 확인한 뒤 재작성하고 원복함. **2026-07-07 목업 대조로 발견한 필드 간격 불일치 수정**: 목업의 `.field`는 `margin-bottom:14px`인데, 필드 그룹 사이(태어난 날짜→양력/음력→태어난 시간→성별→태어난 곳→MBTI, 총 6곳)가 전부 20px이었음 — 14px로 수정. 라벨→필드 내용 간격(목업 `.field label` margin-bottom 7px, 실제 8px)은 1px 차이뿐이라 무시할 만해 그대로 둠. 목업이 날짜를 년/월/일 3개로 분리된 `pill` 버튼(`.date-row`)으로 보여주는 구조적 차이(현재는 네이티브 `showDatePicker`를 여는 단일 버튼)는 발견은 했지만, 이미 확립된 "AppBar vs 커스텀 헤더" 판단과 같은 종류(플랫폼 네이티브 패턴이 접근성·사용성 면에서 더 나음)의 사람 결정 대기 사안으로 분류하고 건드리지 않음. **2026-07-07 목업 대조로 발견한 화면 바깥 패딩 불일치 수정**: 목업(`.screen-body`)의 기본 padding은 `14px 20px 18px`인데 이 화면의 `ListView` padding은 `24/8/24/24`였음 — `20/14/20/18`로 수정(패딩이 줄어드는 방향이라 콘텐츠 폭이 넓어져 오버플로우 위험 없음, `flutter test` 전체로 회귀 없음 확인). **2026-07-08 버그 수정(Explore 서브에이전트가 발견)**: 사주 결과 화면은 이 화면이 스택 아래에 그대로 남아 있어(계산 중 화면만 `pushReplacementNamed`로 교체되고 이 화면은 안 지워짐) Flutter가 결과 화면 AppBar에 자동으로 뒤로 가기 버튼을 붙여준다(위젯 테스트로 재현 확인) — "다시 입력하기"(명시적으로 `BirthInfoStore`·`DeepDiveInfoStore` 둘 다 지움)를 거치지 않고 이 자동 뒤로 가기로 이 화면에 돌아와 재제출하면, `_submit()`이 `DeepDiveInfoStore`의 관심사를 무조건 "전체 선택"으로 덮어써서 이미 심층 분석에서 좁혀뒀던 관심사(예: 연애운만 남기고 해제)가 조용히 초기화되는 실제 데이터 유실 버그였음. `_submit()`에서 저장 전에 `DeepDiveInfoStore.load()`로 기존 값을 먼저 읽어, 있으면 그 `interests`를 그대로 유지하고 없을 때만(첫 방문) 전체 선택으로 채우도록 수정 — 수정 전 코드로 새 회귀 테스트가 실제로 실패하는 것까지 확인한 뒤 재적용. |
| 명식 계산 중 화면 | 🟢 완료 | `project/lib/features/calculating/` | 궤도 애니메이션(달+🌿⭐💫) + 진행 바 + 로딩 문구 3종 1.8초 순환(목업 STEP 3) 구현 완료 — 궤도 회전과 문구 순환 둘 다 `MediaQuery.disableAnimations`(동작 줄이기)일 때는 멈춘다(궤도 쪽은 `didChangeDependencies()`에서 판단 — `initState()`에서 `MediaQuery.of(context)`를 쓰면 예외가 나서 옮김, CLAUDE.md 참고). 3초 뒤 `BirthInfo`를 그대로 들고 result로 실제 이동 — `initState()`의 `Future.delayed(3초)` 콜백은 `if (!mounted) return;`으로 방어돼 있는데, 3초가 되기 전에 사용자가 뒤로 가기로 화면을 벗어나도 예외 없이 안전하게 무시되는지 실제로 pop을 재현해 검증하는 테스트 포함. **2026-07-06 목업 대조로 발견한 시각적 불일치 2건 수정**: (1) "달"(`.orbit .moon`)이 지금까지 단색 `accentSoft` 원이었는데, 목업은 `radial-gradient(circle at 32% 30%, #fff, earthSoft 55%, earth 130%)`(흰색 중심에서 흙(土) 톤으로 번지는 방사형 그라데이션)에 `accentSoft` 톤의 은은한 링 섀도(`box-shadow:0 0 0 10px`)까지 두른 "달무리" 효과였음 — `RadialGradient`+`BoxShadow(spreadRadius: 10)`로 수정(CSS의 130% 스톱은 Flutter `Gradient.stops`가 1.0을 넘길 수 없어 1.0으로 근사). (2) 진행 바(`.progress-fill`)도 단색 `accent`였는데, 목업은 공유 버튼(`.share-btn`)과 같은 `accent`→`metal` 그라데이션을 씀 — `LinearProgressIndicator.valueColor`는 단색만 받아서 `ShaderMask`로 그라데이션을 입히되, 배경 트랙까지 같이 물드는 걸 막기 위해 배경 트랙(`AppColors.border`)은 별도 `Container`로 먼저 그리고 그 위에 진행 막대만(배경을 투명하게) `Stack`+`ShaderMask`로 감싸는 구조로 재구성. **2026-07-06 테스트 커버리지 보강**: "달"의 방사형 그라데이션 색상이 실제로 `[Colors.white, AppColors.earthSoft, AppColors.earth]`와 일치하는지 확인하는 테스트 추가 — `colors`를 `[white, accentSoft, accent]`로 일부러 되돌려 테스트가 실제로 실패하는 것까지 확인한 뒤 재적용. 진행 바의 `ShaderMask` 그라데이션은 `shaderCallback`이 `Rect`를 받는 함수(클로저) 안에 있어 위젯 트리에서 직접 값으로 꺼내 검증하기 어려움을 확인 — 억지로 깨지기 쉬운 테스트를 추가하는 대신 이번엔 보류(달의 `RadialGradient`는 `Container.decoration`에 그대로 저장돼 있어 검증 가능했던 것과 대조적). **2026-07-07 목업 대조로 발견한 스타일 불일치 수정**: "평균 3초 소요돼요" 캡션이 목업(`.loading-caption`: 11px, font-weight 600)과 달리 13px에 기본 굵기였음 — 11px/w600으로 수정. 같은 화면의 로딩 문구(`.loading-msg`, 목업 13px)는 실제로 17px로 더 큰 차이가 있지만, 결과 화면 헤더(22px vs 15.5px)·메타 라인(13px vs 10.5px)과 같은 이유(모바일 실기기 가독성을 위한 의도적 확대일 가능성)로 이번엔 건드리지 않음. **2026-07-07 목업 대조로 발견한 여백 불일치 수정**: 결과·온보딩·생년월일시 입력 화면 여백 감사에 이어 이 화면도 점검 — 목업(`.loading .screen-body`)은 자식(궤도 애니메이션→로딩 문구→진행바→캡션) 사이에 균일한 `gap:22px`를 쓰는데, 실제로는 32px/20px/12px로 제각각이었음 — 세 곳 모두 22로 통일. 궤도 컨테이너 자체 크기(목업 120x120 vs 실제 200x200)는 궤도 반경(90) 검토 시 이미 "비례상 크게 어긋나지 않아 의도적 확대일 가능성"으로 판단해뒀던 것과 같은 이유로 이번에도 그대로 둠. **2026-07-07 이전 판단을 뒤집은 궤도·달·이모지 크기 수정**: 결과 화면 카드 패딩 판단을 뒤집은 것과 같은 근거로 재검토 — 궤도 컨테이너(200→120)·달(72→64)·궤도 반경(90→58)·이모지 폰트 크기(22→13)의 실제 비율을 계산해보니 각각 1.67배·1.13배·1.55배·1.69배로 서로 안 맞아 일관된 의도적 확대로 보기 어렵다고 결론 — 넷 다 목업 값 그대로 수정(`calculating_screen_test.dart`의 달 그라데이션 색상 테스트 등 6개로 회귀 없음 확인, box-shadow spreadRadius:10은 CSS/Flutter 둘 다 요소 크기와 무관한 절대값이라 그대로 둠). **2026-07-07 목업 대조로 발견한 진행바 폭 불일치 수정**: 목업(`.progress-track`)은 180px인데 지금까지는 160px이었음 — 180으로 수정. 같은 화면에서 궤도 회전 애니메이션 주기(`_orbitController`의 `Duration(seconds: 3)`)도 목업(`spin 5s`)과 다르지만, 이 3초는 화면이 떠 있는 전체 시간(`Future.delayed(seconds: 3)` 뒤 result로 이동)과 정확히 일치해 "궤도가 정확히 한 바퀴 도는 동안 로딩이 끝난다"는 의도적 설계로 판단해 그대로 둠. 달의 pulse(숨쉬기, `scale(1)→scale(1.06)`) 애니메이션은 아예 구현돼 있지 않은데, 이건 완전히 새로운 `AnimationController`가 필요한 신규 기능이라(값 하나를 고치는 수준이 아님) 온보딩 마스코트의 float/twinkle 애니메이션과 같은 이유로 스코프 밖에 남겨둠. **2026-07-08 대조로 발견한 공백(Explore 서브에이전트)**: 로딩 문구 순환 타이머(`_messageTimer`)도 궤도 회전과 똑같이 `MediaQuery.disableAnimations` 가드(`if (...) return;`)를 갖는데, 지금까지 이 분기를 실제로 통과시켜 검증한 테스트가 없었음 — 궤도 회전 테스트는 1초만 흘려보내 문구 타이머의 1.8초 문턱을 아예 안 넘었고, 문구 순환 테스트는 반대로 disableAnimations를 켜지 않아 이 가드 자체를 안 탔음. "동작 줄이기" 테스트에 0.8초를 더 흘려보내(총 1.8초 경과, 문구 타이머가 처음 발화하는 시점) 문구가 첫 문구 그대로인지 확인하는 assertion 추가(가드를 일부러 제거해 테스트가 실제로 실패하는 것까지 확인 후 복원). `flutter analyze`+`flutter test`(217개 전체 통과, 기존 테스트에 assertion만 추가)+`./tool/check_build.sh`(APK 194,291,004 bytes) 검증 완료 |
| 사주 결과 화면 | 🟢 완료 | `project/lib/features/result/` | 이름 있으면 헤더/공유에 실제 이름 반영(없으면 "회원님"), 성별·출생지 있으면 메타 라인·공유 텍스트에 순서대로 표시(날짜 · 시간 · 양/음력 · 성별 · 출생지 — 메타 라인 조립은 `meta_line.dart`의 `buildMetaLine`, report 화면과 공유 — 12시간제 변환의 자정/정오 경계값 및 분(`minute`, 목업처럼 "오후 2시 30분生" 형식 — `minute`이 없으면 기존처럼 분 없이 표시해 하위 호환 유지)까지 `test/features/meta_line_test.dart`로 검증됨), 4기둥 카드(한자·오행 밸런스 % 모두 실제 계산값과 정확히 일치하는지 값으로 검증됨, `_PillarCard`는 "년주 무인"처럼 라벨+값이 하나로 병합된 시맨틱스 제공 — `container: true`로 형제 카드와 안 섞이게 함), 카테고리 2x2(연애·재물·건강·성격 — `ohaeng_readings.dart` 규칙 기반, 아직 실제 AI 해석은 아님. 어떤 오행이 "우세"한지 뽑는 선택 로직 자체를 실제 값·동률 상황까지 포함해 검증한 테스트 있음. **2026-07-05 수정**: 원래 `GridView.count(childAspectRatio: 1.3)`로 셀 높이를 고정해뒀는데, 시스템 글자 크기를 키우면(접근성 큰 텍스트 — 일부 기기는 기본 "큼" 설정만으로도 1.3배) 카드 텍스트가 고정 높이를 넘겨 실제로 `RenderFlex overflow`가 재현되는 걸 확인함 — `_PillarCard`와 같은 `Row`-of-`Expanded`+`IntrinsicHeight`(내용에 맞춰 늘어나는 높이) 패턴으로 교체해 어떤 배율에서도 안 잘리게 고침, 2배 배율 렌더링 회귀 테스트 추가). 공유 버튼은 `share_card.dart`(9:16 카드 위젯, 이름 최대 2줄·메타 라인 1줄로 말줄임 처리해 20자/30자 입력 제한 최대치에서도 고정 높이를 넘치지 않게 함. **2026-07-05 수정**: 공유자의 시스템 글자 크기 설정을 그대로 물려받으면 2배 배율에서 551px `RenderFlex overflow`가 나던 것도 확인함 — 받는 사람은 어차피 보내는 사람의 접근성 설정과 무관하게 이미지를 보므로 `MediaQuery.withClampedTextScaling(1.0 고정)`으로 카드 내부 배율을 항상 디자인대로 고정, 회귀 테스트 추가)를 `RepaintBoundary.toImage()`로 캡처하고 `share_text.dart`(`buildShareText`, 테스트 가능한 순수 함수 — 오행 밸런스 퍼센트 줄이 `ohaengCount` 실제 분포·반올림까지 정확히 일치하는지 값으로 검증됨. 2026-07-05 수정: 예전엔 날짜/양음력/성별/출생지를 `meta_line.dart`와 별개로 자체 조립해 태어난 시간이 공유 텍스트에서만 통째로 빠져 있었음 — 특히 이미지 캡처 실패로 텍스트만 단독 공유되는 폴백 상황에서 시간 정보가 누락된 채 나가던 실제 결함. `buildMetaLine`을 그대로 재사용하도록 고쳐 화면과 동일하게 시간·분까지 포함되도록 통일)로 만든 텍스트와 함께 `share_plus`로 공유, 캡처 실패 시 텍스트 전용으로 자동 폴백. `SharePlus.instance.share()` 자체가 실패해도 try/catch로 잡아 스낵바로 사용자에게 알림(무반응처럼 보이지 않게 — 위젯 테스트 환경은 플랫폼 채널 목 핸들러가 없어 이 실패 경로가 실제로 재현되므로, "공유하기" 버튼을 탭해 스낵바까지 뜨는지 확인하는 테스트로 검증됨). AppBar의 "다시 입력하기"(확인 다이얼로그 거침)로 저장값 초기화 후 재입력 가능. "상세 리포트 보기" → report 화면으로 실제 이동, **2026-07-07 변경(사용자 요청)**: "MBTI·관심사로 심층 분석 받기 →" 버튼을 이 화면에서 제거하고 상세 리포트 화면으로 옮겼다 — 이 화면엔 이제 "상세 리포트 보기 (무료)" 링크만 남는다(자세한 내용은 아래 "상세 리포트 화면"/"심층 분석" 행 참고). **2026-07-06 수정**: 우세 오행 콜아웃 문구가 목업(STEP 4, "목(木) 기운이 강한 타입이에요 🌿\n새로운 걸 벌이는 힘이 넘쳐요")과 실제 다르게 구현돼 있던 걸 발견 — 순서가 한자(한글)로 뒤바뀌어 있었고(木(목)), 이모지가 첫 줄이 아닌 둘째 줄에 있었고, 둘째 줄에 "타입이에요"가 불필요하게 반복돼 있었음. `_ohaengCallout`을 (한자, 이모지, 짧은 설명) 3튜플로 재구성해 목업과 정확히 일치하도록 고침 — 이 문구는 `share_card.dart`/`share_text.dart`도 그대로 재사용하므로 세 파일 모두 함께 갱신, 관련 테스트(result_screen_test.dart/share_card_test.dart/share_text_test.dart) 전부 새 문구로 갱신. **2026-07-06 버그 수정**: "다시 입력하기"의 `_resetAndReenter`가 `BirthInfoStore`만 지우고 `DeepDiveInfoStore`(MBTI·관심사)는 그대로 남겨두던 실제 버그를 발견 — 확인 다이얼로그가 "저장된 생년월일시 정보가 삭제되고, 처음부터 다시 입력하게 돼요"라고 안내하는데도, 완전히 다른 사람이 새로 생년월일시를 입력한 뒤 심층 분석 화면에 들어가면 이전 사람이 골랐던 MBTI·관심사가 그대로 다시 나타나고 있었음. `_resetAndReenter`에 `DeepDiveInfoStore.clear()`도 함께 호출하도록 수정 — 실제로 되돌려 새 회귀 테스트가 실패하는 것까지 확인한 뒤 다시 적용. **2026-07-06 감사(코드 변경 없음)**: birth_input/deep_dive_input의 "제출" 버튼에서 실제로 겪은 더블탭 재진입 버그(별도 행 참고)를 계기로, 이 화면의 다른 비동기 버튼 핸들러들도 같은 취약점이 있는지 직접 재현 테스트로 점검함 — "공유하기"(`_handleShare`)를 `onPressed`를 두 번 동기 호출해 재현해봐도 예외·중복 스낵바 없이 안전했고(캡처·공유가 두 번 일어나도 사용자에게 보이는 결과는 동일), "다시 입력하기" 확인 다이얼로그의 확인 버튼도 두 번 눌러봐도 `BirthInputScreen`이 한 번만 push됨(두 번째 `Navigator.pop`이 이미 닫힌 다이얼로그에 대해 안전하게 무시됨)을 확인 — 이 두 곳은 가드 없이도 이미 안전해 코드 변경하지 않음(불필요한 방어 코드 추가 지양). 날짜/시간 피커(`showDatePicker`/`showTimePicker`)도 같은 방식으로 더블탭해봤지만 다이얼로그가 한 번만 열림을 확인. **2026-07-06 추가 발견(회귀 고정, 코드 변경 아님)**: `calculateFourPillars()`가 `isLunar`를 아예 받지 않아 음력으로 입력해도 양력으로 취급해 그대로 계산한다는 것을 발견 — 지금까지 이 화면의 모든 테스트가 `isLunar: false`만 써서 이 사실이 값으로 확인된 적이 없었음. `isLunar: true`로 넣어도 4기둥이 `isLunar: false`와 완전히 동일하게 나오고 메타 라인의 "음력" 라벨만 바뀐다는 것을 값으로 고정하는 회귀 테스트 추가(아래 "사주 계산 로직" 행 참고, 절기 근사·자시·진태양시보다 영향 범위가 큰 네 번째 정확도 이슈 — 구현 여부는 마찬가지로 사람 결정 대기). **2026-07-06 커버리지 갭 발견**: `flutter test --coverage`로 `coverage/lcov.info`를 직접 분석해보니, `_handleShare()`의 이미지 캡처 실패 시 텍스트 전용으로 폴백하는 분기(캡처 성공 시 도는 분기와 갈라지는 지점, 발견 당시 기준 `result_screen.dart:348-349`, 2026-07-07 여러 차례의 목업 대조 수정으로 파일이 늘어나 지금은 대략 374~375번 줄 부근으로 이동함)가 지금까지 단 한 번도 실행된 적이 없었음을 발견 — 기존 "공유 시트가 실패하면 스낵바로 알려준다" 테스트는 매번 카드 캡처 자체는 성공하고(`imageBytes != null`) `SharePlus.instance.share()` 호출만 실패하는 경로였을 뿐, "캡처 자체 실패" 경로는 재현된 적이 없었다(라우터의 기존 설명이 이 둘을 뭉뚱그려 마치 폴백 분기까지 검증된 것처럼 읽혔던 것도 부정확했음). `pumpWidget(phase: EnginePhase.layout)`로 build·layout만 하고 paint는 아직 안 돈 상태로 멈춰(`RenderRepaintBoundary.debugNeedsPaint`가 아직 true) 버튼의 `onPressed`를 직접 동기 호출하는 방식으로 이 분기를 실제로 실행시키는 테스트를 추가 — 코드 자체에는 두 분기의 사용자 관찰 가능한 차이가 없어(둘 다 이후 SharePlus 실패로 같은 스낵바가 뜸) 런타임 assertion만으로는 증명 불가능하므로, 테스트 실행 후 `coverage/lcov.info`에서 348~349번 줄이 실제로 히트(hit count 2, 1)로 바뀌는 것까지 확인해 검증. **2026-07-06 확인(코드 변경 없음)**: 위젯 테스트 환경은 플랫폼 채널이 없어 `SharePlus.instance.share()`가 항상 실패하는 경로로만 검증되는데, 실제 Android 기기에서 이미지 파일 공유가 되려면 보통 앱 쪽 `AndroidManifest.xml`에 `FileProvider` 선언이 필요하다는 점이 걱정돼 `project/android/app/src/main/AndroidManifest.xml`과 `share_plus` 패키지(`~/.pub-cache/hosted/pub.dev/share_plus-13.2.0/android/src/main/AndroidManifest.xml`)를 직접 대조해봄 — `share_plus`가 자체 `ShareFileProvider`(`authorities="${applicationId}.flutter.share_provider"`)와 전용 `@xml/flutter_share_file_paths` 리소스를 플러그인 자신의 매니페스트에 이미 선언해두고 있어, 앱 쪽에서 별도로 `<provider>`를 추가할 필요가 없음(Gradle 매니페스트 병합으로 자동 반영). 실제 기기 검증은 이 환경에 연결된 Android 기기가 없어 여전히 불가능하지만(CLAUDE.md 참고), 적어도 이 설정 누락으로 인한 실패 가능성은 배제됨 — 향후 사이클이 같은 걱정으로 재조사하지 않도록 여기 기록. **2026-07-06 목업 대조로 발견한 시각적 불일치 수정**: 목업의 "공유하기" 버튼(`.share-btn`, 실제 마크업 문구는 "📸 인스타 스토리로 공유하기")은 다른 CTA 버튼과 달리 단색이 아니라 `accent`→`metal` 그라데이션 배경(`linear-gradient(120deg, var(--app-accent), var(--app-metal))`)을 쓰는데, 우리 구현은 그냥 일반 단색 `ElevatedButton`이었음 — 이 화면에서 유일한 그라데이션 버튼이라 다른 CTA와 똑같이 구현하기 쉬웠던 것으로 보임. `Container`+`LinearGradient(colors: [accent, metal])`로 감싸고 버튼 자체는 투명 배경으로 위에 얹는 방식으로 수정, border-radius도 목업 값(15px)에 맞춤. 버튼 문구("📸 공유하기")는 목업의 "인스타 스토리로"를 그대로 따르지 않음 — 실제 기능은 `share_plus`로 여는 범용 OS 공유 시트(카톡·메시지·인스타 등 사용자가 직접 고름)라 "인스타 스토리로"라고 특정하면 실제로 없는 기능을 있다고 오해시킬 수 있어 기존의 정확한 범용 카피를 유지함. **2026-07-06 목업 대조로 발견한 색상 로직 불일치 수정(가장 눈에 띄는 발견)**: 우세 오행 콜아웃 박스(예: "목(木) 기운이 강한 타입이에요")가 지금까지 어떤 오행이 우세하든 항상 고정된 `accentSoft`(핑크) 배경+`ink` 글자색이었는데, 목업(`.callout`)을 다시 보니 실제로는 그 화면의 우세 오행 색으로 물드는 디자인이었음(목업 데모가 "목(木)"이 우세한 예시라 `--app-wood-soft` 배경으로 보였을 뿐, 화/토/금/수가 우세하면 각각 그 색이어야 함) — `AppColors.ohaengSoftColors[dominant]`/`ohaengTextColors[dominant]`로 수정(`ohaengTextColors`는 이미 `ohaengSoftColors` 배경 위에서 WCAG AA 통과하도록 설계돼 있어 안전), border-radius도 목업 값(16px, 기존 20px)에 맞춤. `result_screen.dart`뿐 아니라 같은 콜아웃 박스를 그대로 복제해 그리는 `share_card.dart`(공유 이미지)도 똑같은 문제가 있어 함께 수정. **2026-07-06 목업 대조로 발견한 라벨 표기 불일치 수정**: 오행 밸런스 바 차트의 한 글자 라벨(`_OhaengBarRow`/`share_card.dart`의 `_balanceRow`)이 지금까지 한글(목/화/토/금/수)이었는데, 목업(`.bar-row .tag`)은 한자(木/火/土/金/水)로 표시함 — `report_screen.dart`의 오행 뜻풀이 배지가 이미 같은 이유(짧게 보여줄 땐 한자, 풀어 쓰는 문장엔 한글)로 한자를 쓰고 있던 기존 관례와도 일치시킴. `result_screen.dart`는 이미 있던 `_ohaengCallout`의 한자 값을 재사용, `share_card.dart`는 같은 5개 값을 담은 새 `_ohaengHanja` 맵을 추가 — 색상/오행 집계(`ohaengCount`) 등 실제 로직은 그대로 한글 키를 쓰고, 화면 표시만 한자로 바꿈(로직 변경 없음, 테스트 회귀 없음). **2026-07-06 목업 대조로 발견한 스타일 불일치 수정**: "상세 리포트 보기 (무료)" 링크가 목업(`.report-link`)은 밑줄 있는 링크(`text-decoration:underline`, 11px, font-weight 700)인데 우리 구현은 밑줄이 아예 없고 크기·굵기도 달랐음(기본 크기, w600) — `TextDecoration.underline`(밑줄 색은 `inkSoft`)을 추가하고 목업 값(11px/700)에 맞춤. 문구 자체는 이미 목업과 정확히 일치했어서 이번엔 스타일만 수정. **2026-07-06 테스트 커버리지 보강**: 우세 오행 콜아웃 박스의 배경색을 `ohaengSoftColors[dominant]`로 고친 뒤에도, 그 실제 색상 값을 값으로 확인하는 테스트가 없었음(텍스트 내용만 검증돼 있었음 — 줄 커버리지만으로는 색이 다시 `accentSoft`로 되돌아가도 못 잡음). 기존 "우세 오행 선정" 테스트에 `find.ancestor`로 콜아웃 `Container`를 찾아 `decoration.color`가 `AppColors.ohaengSoftColors['금']`(이 테스트의 dominant)과 정확히 일치하는지 확인하는 assertion 추가 — 색을 `accentSoft`로 일부러 되돌려 테스트가 실제로 실패하는 것까지 확인한 뒤 재적용. **2026-07-06 판단**: 헤더(22px vs 목업 15.5px)·메타 라인(13px vs 목업 10.5px) 같은 폰트 크기 차이도 발견했지만, 체크박스·링크처럼 명백한 오차(수치가 가깝고 단일 UI 요소)와 달리 이런 주요 타이포그래피는 모바일 실기기 가독성을 위해 이미 의도적으로 조정됐을 가능성이 높아(목업은 축소된 브라우저 데모 화면이라 절대 px 값이 실기기에 그대로 맞으리라는 보장이 없음) 시각 확인 없이 되돌리는 게 오히려 퇴보일 수 있다고 판단해 이번엔 건드리지 않음. **2026-07-06 테스트 커버리지 보강**: 콜아웃 색상에 이어 "공유하기" 버튼의 accent→metal 그라데이션도 실제 색상 값을 확인하는 테스트가 없었음(버튼 문구·onPressed는 그라데이션이 단색으로 되돌아가도 안 바뀌어서 기존 테스트로는 못 잡음) — `find.ancestor`로 버튼을 감싼 `Container`를 찾아 `decoration.gradient.colors`가 `[AppColors.accent, AppColors.metal]`과 정확히 일치하는지 확인하는 테스트 추가(버튼이 긴 리스트 아래쪽에 있어 세로로 키운 뷰포트 필요). `colors`를 `[accent, accent]`로 일부러 되돌려 테스트가 실제로 실패하는 것까지 확인한 뒤 재적용. **2026-07-07 목업 대조로 발견한 캡션급 텍스트 스타일 불일치 3건 수정**: `.loading-caption` 발견을 계기로 다른 캡션급 요소도 마저 대조 — (1) `_PillarCard`의 "년주" 등 라벨이 목업(`.pillar-card .label`: 9.5px, font-weight 700)과 달리 11px/기본 굵기였음, (2) `_OhaengBarRow`의 퍼센트 텍스트가 목업(`.bar-row .pct`: 10px, font-weight 700)과 달리 12px/기본 굵기였음, (3) `_CategoryCard`의 카테고리 설명 텍스트가 목업(`.cat-card .d`: 10.5px, font-weight 600)과 달리 12px/기본 굵기였음 — 셋 다 목업 값대로 수정. 같은 시각 언어를 복제하는 `share_card.dart`의 `_pillarChip` 라벨·`_balanceRow` 퍼센트 텍스트도 동일하게 맞춤(공유 카드가 이미 대부분 일치했지만 font-weight가 빠져있었음). **2026-07-07 목업 대조로 발견한 그리드 간격 불일치 수정**: 카테고리 카드(연애·재물·건강·성격) 2x2 그리드 간격이 목업(`.cat-grid`: `gap:8px`)과 달리 가로/세로 모두 12px이었음 — 8px로 수정(4기둥 카드 간격은 목업 7px 대비 실제 8px로 1px 차이뿐이라 무시할 만한 수준이라 이번엔 그대로 둠). **2026-07-07 목업 대조로 발견한 섹션 간 여백 불일치 7건 수정**: 그리드 내부 간격에 이어 섹션 전환 여백(margin-bottom류)도 체계적으로 재대조 — 콜아웃→4기둥(목업 `.callout` margin-bottom 14px, 기존 24px), 4기둥→"오행 밸런스" 제목(`.pillars` 14px, 기존 28px), 제목→막대그래프(`.bars h3` margin 8px, 기존 12px), 막대그래프→"오늘 궁금한 것부터" 제목(`.bars` 14px, 기존 28px), 제목→카테고리 그리드(`.cards h3` margin 8px, 기존 12px), 카테고리 그리드→공유 버튼(`.cat-grid` 14px, 기존 28px), 공유 버튼→상세 리포트 링크(`.share-btn` margin-bottom 8px, 기존 12px) 총 7곳을 목업 값 그대로 수정. 헤더급 폰트 크기(22px vs 15.5px 등)는 배율이 비교적 일관적이라 "모바일 가독성을 위한 의도적 확대"로 판단해 그대로 뒀지만, 이 여백들은 배율이 1.5~2배로 들쭉날쭉해 일관된 의도적 스케일링으로 보기 어려워 단순 대조 누락으로 판단. **2026-07-07 목업 대조로 발견한 화면 바깥 패딩 불일치 수정**: 목업(`.result .screen-body`)은 `padding-top:12px` 외엔 기본값(가로 20px, 아래 18px)을 쓰는데 이 화면의 `ListView` padding은 `24/8/24/32`였음 — `20/12/20/18`로 수정(패딩이 줄어드는 방향이라 오버플로우 위험 없음, `flutter test` 전체로 회귀 없음 확인). **2026-07-07 이전 판단을 뒤집은 카드 패딩 수정**: 2026-07-06에 `_PillarCard`/`_CategoryCard`의 패딩 차이(목업 `.pillar-card` 9px 4px·`.cat-card` 10px 11px vs 실제 세로14/가로0·균일14)를 "모바일 터치 영역을 위한 의도적 조정 가능성"으로 보고 모서리 반경만 고치고 패딩은 보류했었는데, 이후 여러 사이클(테두리 두께, `.pill` 패딩, 섹션 여백)에서 비슷한 값 차이들이 실제로는 단순 대조 누락이었음이 반복 확인돼 판단을 뒤집음 — `_PillarCard`는 가로4/세로9, `_CategoryCard`는 가로11/세로10으로 목업 값 그대로 수정(카드 레이아웃에 영향을 주는 변경이라 `flutter test` 전체(시스템 글자 크기 2배 오버플로우 테스트 포함)로 먼저 회귀 없음 확인). **2026-07-07 접근성 격차 발견 및 수정**: `flutter test --coverage`로 `coverage/lcov.info`를 다시 분석해보니 `main.dart`의 4줄(entry point 자체)을 빼면 `lib/` 전체가 사실상 라인 커버리지 100%라 새로운 로직 버그는 못 찾았지만, 같은 파일 안에서 `_PillarCard`(위 두 번째 "4기둥 카드 병합 시맨틱스" 발견 때 이미 처리됨)와 `_CategoryCard`를 나란히 놓고 보니 `_CategoryCard`(연애·재물·건강·성격 2x2 그리드)만 아이콘·제목·설명이 각각 별도 `Text`로 남아 있어 스크린 리더가 세 번 나눠 읽고 있었음(장식용 이모지까지 유니코드 이름으로 읽어 더 혼란스러움) — `_PillarCard`와 똑같은 패턴(`Semantics(label: '$title. $desc', excludeSemantics: true, container: true)`)으로 병합. `deep_dive_result_screen.dart`의 관심사 풀이 카드도 정확히 같은 구조(아이콘+제목+설명 3개 Text)라 같이 발견해 동일하게 수정. 회귀 테스트 2건 추가(각 화면에 병합된 시맨틱 라벨 값 확인) — 수정 전 코드로는 두 테스트 모두 실패하는 것 확인 후 재적용. `flutter analyze`+`flutter test`(199개 전체 통과)+`./tool/check_build.sh`(APK 194,291,122 bytes) 검증 완료. **2026-07-08 테스트 커버리지 보강**: "다시 입력하기" 확인 다이얼로그(`showDialog<bool>()`, `barrierDismissible` 기본값 true)를 지금까지 "취소" 버튼을 직접 누르는 경로만 테스트했을 뿐, 바깥(바리어) 탭이나 안드로이드 뒤로 가기로 닫으면 `pop(false)`가 아니라 인자 없는 `pop()`(null)으로 닫히는 경로는 값으로 확인한 적이 없었던 커버리지 공백을 발견 — `_resetAndReenter`의 가드가 `confirmed != true`라 null도 이미 안전하게 처리되긴 하지만(`confirmed == false`로 바꿔 실제로 테스트가 실패하는 것까지 확인해 이 가드의 정확한 조건이 중요함을 검증), 다이얼로그 안의 `BuildContext`로 얻은 `Navigator`에 인자 없이 `pop()`을 호출해 이 경로를 직접 재현하는 회귀 테스트 추가. `flutter analyze`+`flutter test`(206개 전체 통과)+`./tool/check_build.sh`(APK 194,291,004 bytes) 검증 완료. **2026-07-08 목업 자체가 짚어둔 미검증 지점 발견**: 목업(01-pastel-cute.html) 하단의 "다음으로 다듬을 지점" ①번("시간 모름 체크 시 시주 카드가 어떻게 바뀌는지")을 실제 구현 전체(result_screen/report_screen/share_card)에 대해 다시 점검해보니, result_screen.dart·report_screen.dart는 이미 hour: null로 렌더링해본 테스트가 있는데 같은 시주 표시 로직(`pillars.hour?.label ?? '모름'`, `_pillarChip`의 `stemIndex == null` 분기)을 그대로 복제해 그리는 `share_card.dart`만 지금까지 hour: null로 렌더링해본 적이 한 번도 없었던 비대칭을 발견 — `share_card_test.dart`에 회귀 테스트 추가("모름" 텍스트가 실제로 렌더링되는지 확인). `flutter analyze`+`flutter test`(207개 전체 통과)+`./tool/check_build.sh`(APK 194,291,004 bytes) 검증 완료. **2026-07-08 같은 종류의 비대칭 하나 더 발견**: 우세 오행 콜아웃 박스 배경색(`ohaengSoftColors[dominant]`)·텍스트색(`ohaengTextColors[dominant]`) 분기 로직도 `result_screen.dart`엔 이미 실제 색상 값을 확인하는 테스트가 있는데(위 "2026-07-06 테스트 커버리지 보강" 참고), 같은 분기를 그대로 복제해 그리는 `share_card.dart`만 색상 값 자체를 확인한 적이 한 번도 없었음 — result_screen과 같은 dominant('금')로 맞춰 배경색·텍스트색 둘 다 확인하는 회귀 테스트 추가(색을 고정값으로 되돌려 실제로 실패하는 것까지 확인 후 재적용). `flutter analyze`+`flutter test`(210개 전체 통과)+`./tool/check_build.sh`(APK 194,291,004 bytes) 검증 완료. **2026-07-08 같은 종류의 비대칭 세 번째 발견**: 오행 밸런스 바의 퍼센트 계산(`ohaengCount[ohaeng]! / total * 100`)과 한자 라벨 매핑(`_ohaengHanja`)도 `share_card.dart`가 `result_screen.dart`의 `_OhaengBarRow`와 독립적으로 자기만의 로직을 갖고 있는데, result_screen 쪽엔 이미 실제 계산값(1998-08-15/14시 조합의 {목:2,화:0,토:2,금:3,수:1} → 25%/0%/25%/38%/13%) 검증이 있고 share_card 쪽은 "오행 밸런스"라는 제목 텍스트만 확인했을 뿐 한자·퍼센트 숫자 자체는 한 번도 검증한 적이 없었음 — 같은 값으로 맞춰 한자 5종·퍼센트 4종 전부 확인하는 테스트 추가(`_ohaengHanja` 맵 하나를 일부러 틀리게 바꿔 실제로 실패하는 것까지 확인 후 재적용). `flutter analyze`+`flutter test`(211개 전체 통과)+`./tool/check_build.sh`(APK 194,291,004 bytes) 검증 완료. **2026-07-08 같은 종류의 비대칭 네 번째 발견**: 4기둥 칩(`_pillarChip`)의 실제 한자값(무인/경신/갑자/신미)도 result_screen.dart 쪽은 이미 텍스트로 확인해왔는데 share_card.dart 쪽은 "이름/기둥/오행 밸런스를 렌더링한다" 테스트가 제목류만 확인했을 뿐 실제 기둥 한자는 값으로 확인한 적이 없었음 — 회귀 테스트 추가. `flutter analyze`+`flutter test`(212개 전체 통과)+`./tool/check_build.sh`(APK 194,291,004 bytes) 검증 완료. **2026-07-08 이번엔 대칭적 공백 발견(양쪽 다 미검증)**: `_PillarCard`/`_pillarChip` 둘 다 천간 오행에 따른 글자색 분기(`ohaengTextColors[stemOhaeng(stemIndex)]`)를 갖고 있는데, 콜아웃 색상과 달리 이건 result_screen.dart 쪽도 지금까지 값으로 확인한 적이 없었던(비대칭이 아니라 양쪽 다 빠진) 공백이었음 — 1998-08-15/14시 조합(년주 무인=토, 월주 경신=금, 일주 갑자=목, 시주 신미=금, 전부 천간 기준)으로 두 파일 모두에 색상 검증 추가(각각 색 분기를 일부러 고정값으로 바꿔 실패하는 것까지 확인 후 재적용). `flutter analyze`+`flutter test`(212개 전체 통과, 기존 테스트에 assertion만 추가해 개수는 그대로)+`./tool/check_build.sh`(APK 194,291,004 bytes) 검증 완료. **2026-07-08 `build()`의 3단 폴백 마지막 분기(생성자·라우트 arguments 둘 다 없을 때의 하드코딩된 기본값 1998-08-15 14시) 미검증 발견** — report_screen.dart·deep_dive_input_screen.dart와 완전히 같은 패턴, 자세한 내용은 아래 "상세 리포트 화면" 행 참고. `flutter analyze`+`flutter test`(220개 전체 통과, +3)+`./tool/check_build.sh`(APK 194,291,004 bytes) 검증 완료. **2026-07-08 방어 로직 미검증 발견**: `meta_line.dart`의 `buildMetaLine()`이 출생지 접미사를 붙일지 판단할 때 `info.birthPlace?.trim().isNotEmpty == true`로 null과 "공백뿐인 문자열"을 둘 다 방어하는데(birth_input_screen.dart는 항상 trim 후 비우면 null로 바꿔 넘기지만, `buildMetaLine`은 어떤 `BirthInfo`가 와도 안전해야 하는 공용 함수), 지금까지 null 케이스만 테스트됐을 뿐 "공백뿐인 값이 실제로 null과 동일하게 처리되는지"는 값으로 확인한 적이 없었음 — `meta_line_test.dart`에 `birthPlace: '   '` 케이스 회귀 테스트 추가(가드를 `!= null`만으로 약화시켜 실제로 실패하는 것까지 확인 후 복원). `flutter analyze`+`flutter test`(224개 전체 통과, +1)+`./tool/check_build.sh`(APK 194,291,004 bytes) 검증 완료. **2026-07-08 이름 폴백 로직의 원본 화면 자체가 미검증이었던 것 발견**: report_screen.dart/deep_dive_result_screen.dart도 이 화면과 똑같은 이름 폴백(`name?.trim().isNotEmpty == true ? ... : '회원님'`)을 복제해 갖고 있고 그쪽은 이미 null/공백뿐인 이름 둘 다 값으로 검증돼 있었는데(그중 deep_dive_result_screen_test.dart 주석은 "result_screen_test.dart는 이미 검증해뒀다"고까지 적어뒀음), 정작 이 로직이 처음 만들어진 원본인 이 화면 자체엔 "이름이 있는" 경우만 테스트돼 있었을 뿐 null/공백뿐인 경우의 전용 테스트가 없었던 실제 공백을 발견 — 회귀 테스트 추가하는 과정에서 한 pumpWidget 안에서 두 번째 라우트 arguments가 실제로 반영되지 않는 테스트 함정도 겪음(Flutter가 같은 구조의 위젯 트리를 재사용해버림 — `pumpWidget(SizedBox.shrink())`로 완전히 언마운트 후 재조립해야 함, deep_dive_input_screen_test.dart에서 겪은 것과 같은 종류). `flutter analyze`+`flutter test`(225개 전체 통과, +1)+`./tool/check_build.sh`(APK 194,291,004 bytes) 검증 완료. **2026-07-08 실제 버그 발견 및 수정(Explore 서브에이전트)**: `_resetAndReenter()`가 `BirthInfoStore.clear()`와 `DeepDiveInfoStore.clear()` 두 호출을 하나의 try 블록 안에 같이 넣어뒀었음 — `BirthInfoStore.clear()`가 실패하면(플랫폼 채널 오류 등) catch로 바로 건너뛰어 `DeepDiveInfoStore.clear()`가 아예 호출되지 않아, 바로 위에서 이미 고쳤던 "이전 사람의 MBTI·관심사가 다음 사람에게 그대로 보이는" 데이터 유실 버그가 이 실패 경로에서만 조용히 재발할 수 있는 실제 결함이었음. 각각 독립된 try로 분리해 한쪽이 실패해도 다른 쪽은 그대로 시도되도록 수정 — 실제 플랫폼 채널 오류를 재현하기 위해 `shared_preferences_platform_interface`(`dev_dependencies`에 추가, 기존엔 전이 의존성이었을 뿐)의 `InMemorySharedPreferencesStore`를 상속한 가짜 저장소(`birth_info` 키만 `remove()` 시 예외를 던짐)로 `SharedPreferencesStorePlatform.instance`를 바꿔치기하는 회귀 테스트 추가(원래 코드로 되돌려 테스트가 실제로 실패하는 것까지 확인 후 재적용) — 이 세션에서 실제 플랫폼 실패를 시뮬레이션한 첫 사례. `flutter clean`+`flutter pub get`+`flutter analyze`+`flutter test`(226개 전체 통과, +1)+`./tool/check_build.sh`(APK 194,291,004 bytes) 검증 완료. **2026-07-11 콘텐츠 문구 완전성 공백 발견(Explore 서브에이전트)**: 우세 오행별 4개 영역 풀이(`ohaeng_readings.dart`의 `categoryReadingsByOhaeng`, 5개 오행 × 4개 영역=20가지 조합)의 실제 문구는 지금까지 부분적으로만 값으로 고정돼 있었음 — result_screen_test.dart는 '금' 하나만 4개 전부, report_screen_test.dart는 5개 오행 각각 1개 영역씩만 값으로 확인했을 뿐, 나머지 조합(예: 목의 재물운과 화의 재물운을 통째로 맞바꿔도)은 기존 "오행마다 서로 다른 문구를 반환한다(복사-붙여넣기 오류 방지)" 구조적 유일성 테스트로는 못 잡힘(맞바꿔도 5개 값이 여전히 서로 다르므로 통과) — 실제로 목-화의 재물운 문구를 맞바꿔서 기존 유일성 테스트는 그대로 통과하고 새 테스트만 실패하는 것까지 확인. `ohaeng_readings_test.dart`에 20가지 조합 전체를 값으로 고정하는 테스트 추가. `flutter analyze`+`flutter test`(229개 전체 통과, +1)+`./tool/check_build.sh`(APK 194,291,004 bytes) 검증 완료. **2026-07-11 같은 계열의 공백 네 번째 발견**: 우세 오행 콜아웃 문구(`_ohaengCallout`, 한자·이모지·설명 5개 세트)도 지금까지 '금'(기본 픽스처의 실제 우세 오행) 하나만 값으로 확인됐을 뿐, 나머지 4개(목·화·토·수)는 어떤 birthInfo로도 실제 우세 오행이 되게 만들어 검증한 적이 없었음 — `calculateFourPillars`를 스캔해 각 오행이 우세가 되는 생년월일(1980-01-01=토, 1980-01-15=화, 1980-02-08=금, 1980-02-18=목, 1980-06-01=수)을 찾아 4개 전부 실제 렌더링으로 검증하는 테스트 추가(이 문구는 오행 이름이 문자열 안에 그대로 포함돼 있어 스코핑 없이 값 대조만으로도 오행끼리 문구가 뒤바뀌면 바로 잡힘 — 목-화 문구를 맞바꿔 실제로 실패하는 것까지 확인 후 복원). `flutter analyze`+`flutter test`(233개 전체 통과, +1)+`./tool/check_build.sh`(APK 194,291,004 bytes) 검증 완료. **2026-07-11 같은 계열의 공백 다섯 번째 발견(share_card.dart)**: `_balanceRow`의 한자 라벨(`_ohaengHanja`)·퍼센트 값은 기존에 각각 "화면 어딘가에 존재하는지"만 독립적으로 확인했을 뿐, 특정 한자가 그 오행의 실제 퍼센트와 같은 줄(Row)에 붙어 있는지는 확인한 적이 없었음 — `_ohaengHanja`에서 목·화 한자가 서로 뒤바뀌어도(둘 다 여전히 화면에 하나씩 존재) 기존 assertion들은 그대로 통과. 한자를 감싼 `Row` 안에서 퍼센트 텍스트를 찾아 같은 줄에 있는지 확인하는 assertion 추가(목-화 한자를 맞바꿔 실제로 실패하는 것까지 확인 후 복원). `flutter analyze`+`flutter test`(233개 전체 통과, 기존 테스트에 assertion만 추가)+`./tool/check_build.sh`(APK 194,291,004 bytes) 검증 완료 |
| 상세 리포트 화면 | 🟢 완료 | `project/lib/features/report/` | 상단에 결과 화면과 같은 "{이름}의 상세 리포트" 헤더 + 메타 라인(`buildMetaLine` 재사용)을 표시해 문맥 유지, 명식 8자(천간/지지) breakdown 표(실제 계산값·오행 라벨과 정확히 일치하는지 값으로 검증됨, `_pillarRow`는 "년주. 천간 무, 오행 토. 지지 인, 오행 목." 처럼 한 문장으로 병합된 시맨틱스 제공) + 오행 5종 전체 의미 + 오행별 영역 풀이 전체(결과 화면은 우세 오행만) + 계산 정확도 안내 문단(**2026-07-06 수정**: 절기 근사에 더해 자시 관법·진태양시 미보정 한계도 추가 명시, 테스트로 검증됨. **2026-07-06 추가 발견**: 음력 입력을 양력으로 변환하지 않고 그대로 계산에 쓴다는 네 번째 한계도 같은 문단에 추가 — 아래 "사주 계산 로직" 행 참고). **아직 결제/구독 로직 없이 전부 무료로 노출하는 MVP** (PROGRESS.md 참고). **2026-07-06 안전성 확인(코드 변경 없음)**: result_screen.dart/share_card.dart에서 실제로 겪은 "시스템 글자 크기 2배 확대 시 RenderFlex overflow" 위험을 점검 — `_OhaengMeaningCard`의 40x40 고정 크기 원형 배지처럼 눈여겨볼 만한 요소가 있었으나, `Container`는 `Row`/`Column`과 달리 내용이 넘쳐도 조용히 잘릴 뿐 RenderFlex 예외를 던지지 않아 실제로는 재현되지 않음을 확인, 회귀 방지용 테스트로 고정. **2026-07-07 추가(사용자 요청)**: "MBTI·관심사로 심층 분석 받기 →" 버튼을 사주 결과 화면에서 이 화면 맨 아래("지금은 모든 내용을 무료로..." 안내 박스 다음)로 옮겨왔다 — 상세 리포트까지 다 본 사람이 자연스럽게 더 깊은 분석으로 이어가도록 함(`AppRoutes.deepDiveInput`으로 `info`를 그대로 전달, 스타일은 결과 화면에 있던 것과 동일하게 유지). **2026-07-07 접근성 격차 발견 및 수정**: 같은 파일의 `_PillarBreakdownTable._pillarRow`는 이미 병합 시맨틱스가 있는데, 바로 위 "2026-07-06 안전성 확인" 메모에서 언급된 그 `_OhaengMeaningCard`(오행 5종 의미 카드, 배지 한자+"오행 · 의미" 제목+설명)만 세 위젯이 각각 별도 Text/Container라 스크린 리더가 세 번 나눠 읽고 있었음(`result_screen.dart`의 `_CategoryCard`에서 먼저 발견한 것과 같은 패턴, 위 "사주 결과 화면" 행 참고) — `_pillarRow`와 같은 `Semantics(label: '$ohaeng · ${title}. ${desc}', excludeSemantics: true, container: true)`로 병합. 회귀 테스트 추가(수정 전 코드로 실패 확인 후 재적용). `flutter analyze`+`flutter test`(200개 전체 통과)+`./tool/check_build.sh`(APK 194,291,122 bytes) 검증 완료. **2026-07-08 테스트 커버리지 보강**: 같은 `buildMetaLine(info)`를 재사용하는 result_screen.dart/share_text.dart/meta_line.dart 자체는 이미 `isLunar: true`로 렌더링·조립해본 테스트가 있는데, 이 화면만 `isLunar: true`로 렌더링해본 적이 한 번도 없었던 비대칭을 발견(share_card.dart의 `hour: null` 공백과 같은 종류 — 위 "사주 결과 화면" 행 참고) — 회귀 테스트 추가("1998.08.15 · 오후 2시生 · 음력"이 그대로 표시되는지 확인). `flutter analyze`+`flutter test`(208개 전체 통과)+`./tool/check_build.sh`(APK 194,291,004 bytes) 검증 완료. **2026-07-08 대조로 발견한 공백**: `_charCell`(천간/지지 각 칸)이 result_screen `_PillarCard`/share_card `_pillarChip`과 같은 `ohaengTextColors` 글자색 분기를 갖고, 거기다 칸 배경색(`ohaengSoftColors`)까지 오행별로 물들이는데, 기존 테스트는 한자·"천간 · 오행" 라벨 문자열만 확인했을 뿐 이 두 색 값 자체(테스트 파일에 `AppColors` import조차 없었음)는 한 번도 검증한 적이 없었음 — 1998-08-15/14시 조합(년주 무=토/인=목, 월주 경=금/신=금, 일주 갑=목/자=수, 시주 신=금/미=토)으로 텍스트색+배경색 둘 다 값 고정 회귀 테스트 추가(색 분기를 고정값 `AppColors.ink`로 일부러 바꿔 테스트가 실제로 실패하는 것까지 확인 후 복원). `flutter analyze`+`flutter test`(212개 전체 통과, 기존 테스트에 assertion만 추가)+`./tool/check_build.sh`(APK 194,291,004 bytes) 검증 완료. **2026-07-08 같은 종류의 공백 추가 발견**: `_OhaengMeaningCard`의 40x40 원형 배지(木/火/土/金/水 한자, 태어난 정보와 무관하게 항상 5개 전부 렌더링)도 `ohaengSoftColors` 배경·`ohaengTextColors` 글자색을 쓰는데, 지금까지 시맨틱스(병합 라벨)만 검증됐을 뿐 색 값 자체는 확인한 적이 없었음 — 5개 배지 전부의 배경·글자색을 값으로 고정하는 회귀 테스트 추가(색 분기를 고정값으로 바꿔 실패하는 것까지 확인 후 복원). `flutter analyze`+`flutter test`(216개 전체 통과, +1)+`./tool/check_build.sh`(APK 194,291,004 bytes) 검증 완료. **2026-07-08 세 화면(result_screen·report_screen·deep_dive_input_screen) 공통 폴백 공백 발견**: 세 화면 모두 `build()`에서 `widget.birthInfo ?? (ModalRoute.of(context)?.settings.arguments as BirthInfo?) ?? BirthInfo(1998-08-15, 14시)`라는 완전히 동일한 3단 폴백을 쓰는데, 지금까지 어떤 테스트도 마지막 분기(생성자·라우트 arguments 둘 다 없을 때의 하드코딩된 기본값)를 실제로 타본 적이 없었음 — 실제 앱 흐름에서는 도달하지 않아야 정상이지만(calculating_screen.dart/결과 화면이 항상 arguments를 넘김), 라우트 배선이 실수로 깨지면 크래시 대신 조용히 엉뚱한 기본값(1998-08-15 14시)을 자기 결과인 것처럼 보여줄 수 있는 위험. 세 화면 모두에 `const XScreen()`(생성자 인자·라우트 arguments 둘 다 없음)으로 pumpWidget해 기본값이 그대로 표시(또는 다음 화면까지 전달)되는지 확인하는 테스트 추가(기본값을 일부러 다른 날짜로 바꿔 테스트가 실제로 실패하는 것까지 확인 후 복원). `flutter analyze`+`flutter test`(220개 전체 통과, +3)+`./tool/check_build.sh`(APK 194,291,004 bytes) 검증 완료. **2026-07-08 더 큰 공백 발견(존재 자체가 미검증)**: `_AllReadingsSection`("오행별 오늘의 풀이 모음", 5개 오행 × 4개 영역=20줄 실제 풀이 문구 + 제목 5개 글자색)은 지금까지 어떤 테스트에서도 언급된 적이 없었음 — `categoryReadingsFor()`가 알 수 없는 키에 조용히 '토'로 폴백하는 구조라, 루프 변수가 잘못 전달돼도 화면이 안 깨지고 그냥 틀린 오행 내용을 보여줄 수 있어 실제 문구 값까지 대조해야 잡히는 종류의 회귀. 섹션 제목·5개 오행 제목 색상·20줄 중 5줄(오행별 1개씩) 실제 문구를 값으로 고정하는 테스트 추가(루프를 일부러 전부 '토'로 바꿔 테스트가 실제로 실패하는 것까지 확인 후 복원). `flutter analyze`+`flutter test`(217개 전체 통과, +1)+`./tool/check_build.sh`(APK 194,291,004 bytes) 검증 완료. **2026-07-08 안내 문구 최신화 공백 발견**: "계산 정확도 안내" 문단이 절기·자시·진태양시·음력 네 가지는 안내하는데, 2026-07-07에 새로 문서화된 다섯 번째 정확도 이슈(한국의 역사적 서머타임 1948~1960·1987~1988년 미반영)는 그날 계산 로직의 산술 버그만 고쳤을 뿐 안내 문구엔 반영하는 걸 놓쳤음(온보딩 다이얼로그도 동일, 위 "온보딩 화면" 행 참고) — 서머타임 한계 안내 문장 추가, 회귀 테스트 추가(문구 제거해 실제로 실패하는 것까지 확인 후 복원). `flutter analyze`+`flutter test`(222개 전체 통과, 기존 테스트에 assertion만 추가)+`./tool/check_build.sh`(APK 194,291,004 bytes) 검증 완료. **2026-07-08 이름 폴백 공백 발견** — "이름이 없으면" 테스트가 null 경우만 확인했을 뿐 공백뿐인 이름은 확인한 적이 없었음(자세한 내용은 위 "사주 결과 화면" 행 참고), 회귀 테스트 추가(가드 약화시켜 실패 확인 후 복원). `flutter analyze`+`flutter test`(225개 전체 통과, +1)+`./tool/check_build.sh`(APK 194,291,004 bytes) 검증 완료. **2026-07-11 콘텐츠 문구 완전성 공백 발견 및 테스트 작성 실수 정정**: `_OhaengMeaningCard`의 설명 문구("새싹이 자라나는 이미지" 등)가 기존엔 '목'만 병합 시맨틱 라벨로 전체 확인됐고 나머지 4개(화·토·금·수)는 제목 접두어만 확인했을 뿐 설명은 단 한 번도 값으로 확인된 적이 없었음(오행별 영역 풀이·직장운·MBTI 코멘트에서 반복 발견된 것과 같은 종류) — 처음엔 `find.text(...)`로 "문구가 화면 어딘가에 존재하는지"만 확인하는 테스트를 짰다가, 화-토의 설명을 통째로 맞바꿔도(둘 다 여전히 화면 어딘가엔 존재하므로) 통과해버리는 것을 실제로 겪어 테스트 자체가 잘못됐음을 발견 — `_OhaengMeaningCard`가 이미 제공하는 "$ohaeng · 제목. 설명" 병합 시맨틱 라벨 전체를 정확히 대조하는 방식으로 다시 작성해, 같은 맞바꾸기로 이번엔 실제로 실패하는 것까지 확인 후 복원. `flutter analyze`+`flutter test`(232개 전체 통과, +1)+`./tool/check_build.sh`(APK 194,291,004 bytes) 검증 완료 |
| 심층 분석(MBTI·관심사) | 🟢 완료 | `project/lib/features/deep_dive/` | 결과 화면의 "MBTI·관심사로 심층 분석 받기 →"에서 진입(신규 라우트 `AppRoutes.deepDiveInput`). **2026-07-07부터는 이 진입점이 사주 결과 화면이 아니라 상세 리포트 화면 맨 아래에 있다**(사용자 요청, 아래 "상세 리포트 화면" 행 참고). `deep_dive_input_screen.dart` — 관심사(연애·재물·직장·건강, 기본 전체 선택) 다중 선택 칩 + "MBTI를 알고 있어요" 체크(기본 꺼짐, birth_input의 "시간 모름" 패턴과 동일)로 켜면 4개 MBTI 축(E/I·S/N·T/F·J/P)을 `PastelToggleRow`(각각 `semanticLabel`로 그룹 라벨 부여)로 입력 — 네 축 전부 실제로 바꿔도 코드가 정확히 반영되는지, 관심사 칩을 껐다 다시 켜는 재선택까지 커버리지 100%로 검증됨. **2026-07-05 수정**: 관심사 칩(`_InterestChip`)도 `PastelToggleRow`와 같은 `Semantics(button:, selected:, onTap:, excludeSemantics: true)` 패턴을 쓰는데, 지금까지 이 화면의 모든 테스트는 `tester.tap()`(InkWell 직접 히트테스트)만 써서 `onTap:` 선언이 빠지는 회귀(과거 `PastelToggleRow`에서 실제로 겪은 것과 같은 종류)를 못 잡고 있었음 — `SemanticsAction.tap`을 직접 실행해 선택이 토글되는지 확인하는 회귀 테스트 추가, 실제로 `onTap:`을 제거해 테스트가 실패하는 것까지 확인 후 원복함. "심층 분석 보기" → `deep_dive_result_screen.dart`로 직접 push(데이터 2개를 넘겨야 해서 named route 대신 `result_screen.dart`의 "다시 입력하기"와 같은 방식으로 `MaterialPageRoute` 직접 사용). **1단계(조합형 규칙 기반)** — 오행×MBTI(16)×관심사를 전부 곱하면 손으로 못 쓸 조합(80+)이 나와서, ① 관심사는 기존 `ohaeng_readings.dart`의 오행별 카테고리 풀이를 재사용(연애·재물·건강)하거나 신규 콘텐츠(직장, `deep_dive_readings.dart`의 `_careerReadingByOhaeng`)를 붙이고, ② MBTI는 오행과 무관하게 16유형별 독립 코멘트 한 줄만 얹는 방식으로 조합 폭발을 피함. 우세 오행 선택은 `FourPillars.dominantOhaeng`(원래 `result_screen.dart` 안에 있던 동률 처리 로직을 여러 화면이 공유하도록 이동)를 그대로 재사용. **2단계(실제 AI 연동)는 별도 결정 대기**(API 키/비용/개인정보 전송 동의 — PROGRESS.md 참고). 입력값은 `core/storage/deep_dive_info_store.dart`의 `DeepDiveInfoStore`(`BirthInfoStore`와 동일 패턴)로 SharedPreferences에 저장돼, 화면을 다시 열면 이전 선택이 그대로 반영됨(첫 방문 null과 "관심사 전부 해제 후 저장"을 구분해 의도치 않게 기본값으로 되돌아가지 않도록 처리, 테스트로 검증됨). 헤더 아래 메타 라인(`buildMetaLine` 재사용, 날짜·시간·양음력·성별·출생지)도 실제 값 그대로 렌더링되는지 검증됨. **2026-07-06 보강**: `deep_dive_result_screen.dart`가 지금까지 `birthHour`를 아는 경우만 테스트됐음(전부 `hour: 14`) — birth_input/result/report 화면은 이미 "태어난 시간을 몰라요" 경로를 검증해뒀는데 심층 분석만 빠져 있던 커버리지 공백. 같은 1998-08-15 생일이라도 시주 유무에 따라 우세 오행이 실제로 '금'(8글자 기준)→'목'(6글자 기준)으로 달라진다는 것까지 값으로 확인하는 테스트 추가. **2026-07-06 관례 확인**: 새 결함이 아니라 기존에 이미 성립하던 동작(체크박스를 껐다 켜도 그 사이 골랐던 MBTI 축 선택이 기본값으로 리셋되지 않고 유지됨 — birth_input의 "태어난 시간을 몰라요" 체크박스가 시간 값을 유지하는 것과 같은 관례)을 지금까지 테스트로 못박은 적이 없었던 것을 발견 — I·N으로 바꾼 뒤 체크 해제→재체크해도 코드가 정확히 "INTJ"(기본값 E·S로 안 돌아감)로 나오는 것까지 값으로 확인하는 회귀 테스트 추가. **2026-07-06 버그 수정**: birth_input과 같은 이유로 `_saveAndContinue()`도 "심층 분석 보기"를 빠르게 두 번 누르면 `DeepDiveResultScreen`이 `Navigator.push()`로 중복 push되는 실제 버그를 재현·확인 — 같은 `_isSubmitting` 플래그로 수정. 이 화면은 `tester.tap()`으로는 재현이 안 돼(await 횟수가 적어 두 번째 tap 전에 첫 호출이 이미 끝나버림) `onPressed`를 직접 두 번 동기 호출하는 기법으로 바꾸고 나서야 확실히 재현·검증됨. **2026-07-06 테스트 커버리지 보강**: `deep_dive_result_screen.dart`의 헤더도 `result_screen.dart`와 정확히 같은 폴백 로직(`birthInfo.name?.trim().isNotEmpty == true ? ... : '회원님'`)을 쓰는데, 지금까지 "이름이 있는" 경우만 테스트돼 있었고 이름이 없거나 공백뿐일 때 "회원님"으로 표시되는지는 확인한 적이 없었던 비대칭을 발견 — result_screen_test.dart에 이미 있던 것과 같은 검증을 이 화면에도 추가(공백만 있는 이름으로 "회원님의 심층 분석 ✨" 표시 확인). 위젯 테스트 8개(`test/features/deep_dive/deep_dive_result_screen_test.dart`). **2026-07-06 엔드투엔드 커버리지 보강**: `deep_dive_info_store_test.dart`에는 "관심사를 전부 꺼서 저장해도(빈 Set) — 첫 방문(null)과 구분된다"는 저장소 단위 테스트가 이미 있었지만, `DeepDiveInfoStore`를 직접 호출한 것일 뿐 실제 화면에서 칩 4개를 전부 탭으로 꺼서 제출하는 UI 경로로는 한 번도 검증된 적이 없었음을 발견(기존 저장 테스트는 재물운 하나는 남겨둔 채였음) — `deep_dive_input_screen_test.dart`에 관심사 4개를 전부 해제→제출→결과 화면의 빈 안내 문구 확인→화면을 다시 열어도 전체 선택으로 안 되돌아가는지까지 잇는 엔드투엔드 테스트 추가. 작성 중 새로 발견한 테스트 함정: 같은 `MaterialApp(home: DeepDiveInputScreen(...))`를 이미 `DeepDiveResultScreen`이 push된 상태 위에 그대로 다시 `pumpWidget`하면 Navigator가 기존 push 상태를 유지해버려 입력 화면이 안 보임 — `pumpWidget(const SizedBox.shrink())`로 완전히 언마운트한 뒤 다시 pumpWidget해야 함(다른 화면의 유사 테스트를 작성할 때도 참고할 만한 패턴). **2026-07-06 버그 수정**: birth_input_screen.dart와 정확히 같은 원인의 실제 버그를 발견 — `_saveAndContinue()`의 더블탭 가드 `_isSubmitting`도 `true`로 바꾸는 지점만 있고 다시 `false`로 되돌리는 지점이 없어, "심층 분석 보기" 제출 후 결과 화면에서 뒤로가기로 이 화면에 돌아오면 버튼이 영구적으로 먹통이 되는 버그였음. `push()`가 반환하는 Future가 완료되는 시점(뒤로가기로 돌아왔을 때)에 맞춰 플래그를 되돌리도록 수정 — 수정을 일부러 되돌려 새 회귀 테스트("한 번 제출한 뒤 뒤로가기로 돌아와도...")가 실제로 실패하는 것까지 확인한 뒤 재적용. **2026-07-06 안전성 확인(코드 변경 없음)**: result_screen.dart/share_card.dart에서 실제로 겪은 "시스템 글자 크기 2배 확대 시 RenderFlex overflow" 위험을 점검 — 관심사 칩은 `Wrap`(내용에 맞춰 줄바꿈), MBTI 토글은 `PastelToggleRow`(고정 높이 없음)라 실제로는 재현되지 않음을 확인, 회귀 방지용 테스트로 고정. **2026-07-06 목업 대조 수정**: `_InterestChip`도 `PastelToggleRow`와 같은 이유로 선택 상태가 accent+흰 글자(브랜드 CTA 버튼 조합)를 잘못 쓰고 있었음 — accentSoft+accentText(테두리는 accent 유지)로 수정해 "공용 위젯"/"공통 테마/디자인 토큰" 행과 같은 방식으로 텍스트 대비 문제 해결, `BorderRadius.circular(999)`도 목업 `.pill` 기준 12px로 수정. 색상/반경 변경은 시맨틱스에 영향 없어 기존 탭 액션·selected 플래그 테스트 전부 그대로 통과. **2026-07-07 목업 대조로 발견한 테두리 두께 불일치 수정**: `PastelPillButton`/`PastelToggleRow`와 같은 이유(목업 `.pill`은 1.5px 테두리)로 `_InterestChip`의 `Border.all`에도 `width: 1.5`를 추가(위 "공용 위젯" 행 참고). **2026-07-07 목업 대조로 발견한 안쪽 여백 불일치 수정**: 같은 이유(목업 `.pill`은 `padding:9px 14px`)로 `_InterestChip`의 padding도 가로 16/세로 10에서 가로 14/세로 9로 수정(위 "공용 위젯" 행 참고). **2026-07-06 목업 대조로 발견한 스타일 불일치 수정**: "MBTI를 알고 있어요" 체크박스 문구도 birth_input의 "태어난 시간을 몰라요"와 같은 이유(목업 `.check-row`는 12px)로 `fontSize: 12`로 수정. 위젯 테스트 12개(`test/features/deep_dive/deep_dive_input_screen_test.dart`). **2026-07-07 문서 정확도 수정**: `deep_dive_input_screen.dart` 최상단 doc-comment가 "기존 birth_input 화면의 칩/체크박스 패턴을 재사용"이라고 설명하고 있었는데, MBTI 질문이 이 화면에서 빠지면서(같은 날 앞선 변경) 이 화면엔 체크박스가 더 이상 없어 부정확해진 걸 발견 — 이 화면엔 이제 커스텀 칩(`_InterestChip`)만 있다고 정정. **2026-07-07 변경(사용자 요청)**: MBTI 질문(체크박스+4개 축 토글)을 이 화면에서 완전히 제거하고 `birth_input_screen.dart`로 옮겼다(위 "생년월일시 정보 입력 화면" 행 참고) — 이 화면은 이제 관심사만 고른다. 대신 `initState()`의 `_loadSaved()`가 `DeepDiveInfoStore`에서 불러온 `mbti`를 화면에 보여주지 않고 `_mbti` 필드에만 조용히 담아뒀다가, 제출 시 관심사와 함께 그대로 다시 저장해 심층 분석 결과 화면에 이어지도록 한다(이 값을 잃어버리면 birth_input에서 입력한 MBTI가 통째로 사라지는 회귀가 되므로, 저장된 MBTI가 있으면 화면엔 안 보여도 결과에 그대로 반영되는지 확인하는 테스트 추가) — 결과 화면의 "MBTI·관심사로 심층 분석 받기" 진입점도 상세 리포트 화면으로 옮겨졌다(아래 "사주 결과 화면"/"상세 리포트 화면" 행 참고). **2026-07-07 접근성 격차 발견 및 수정**: `deep_dive_result_screen.dart`의 관심사 풀이 카드(아이콘+제목+설명)가 `result_screen.dart`의 `_CategoryCard`와 똑같이 스크린 리더에 세 번 나눠 읽히는 문제가 있어(자세한 내용은 "사주 결과 화면" 행 참고) 같은 방식(`Semantics(label: '$title. $desc', excludeSemantics: true, container: true)`)으로 병합, 회귀 테스트 추가. **2026-07-08 테스트 커버리지 보강**: birth_input·onboarding·report·result·share_card·deep_dive_input 6개 화면은 전부 "시스템 글자 크기 2배 확대 시 RenderFlex overflow 없음" 회귀 테스트가 있는데 `deep_dive_result_screen_test.dart`만 이 테스트가 빠져 있던 비대칭을 `grep`으로 발견 — MBTI+관심사 4개 전부 선택한 최대 콘텐츠 케이스로 2배 배율 렌더링 테스트 추가(관심사 카드가 `Row(icon + Expanded(Column))`를 고정 높이 없는 `ListView` 안에 두는 구조라 실제로는 재현되지 않음을 확인, 코드 변경 없이 회귀 방지용으로 고정). `flutter analyze`+`flutter test`(201개 전체 통과)+`./tool/check_build.sh`(APK 194,291,122 bytes) 검증 완료. **2026-07-08 버그 수정 2건(Explore 서브에이전트가 발견)**: (1) `deep_dive_readings.dart`의 `readingFor()`가 `Interest.categoryTitle`(이 파일)과 `categoryReadingsByOhaeng`의 제목(`ohaeng_readings.dart`)이라는, 서로 다른 파일의 문자열 리터럴이 항상 일치한다고 가정하고 `firstWhere()`를 폴백 없이 쓰고 있었음 — 같은 파일의 다른 조회는 전부 기본값 폴백이 있는데 이것만 없어, 향후 둘 중 하나만 바뀌면(예: "건강운"→"건강 운" 오타) 이 화면 전체가 `StateError`로 크래시했을 잠재적 버그였음. 일치하는 항목이 없으면 `categories.first`로 폴백하도록 수정, 두 문자열이 실제로 어긋나면 CI에서 먼저 잡히도록 하는 일치성 테스트도 추가(런타임 폴백을 직접 테스트하기보다 애초에 어긋나지 않는지 확인하는 방식 — `Interest`가 컴파일 타임 enum이라 폴백 분기 자체를 강제로 타게 만들 수는 없었음). (2) 위 "생년월일시 정보 입력 화면" 행 참고 — `birth_input_screen.dart` 재제출 시 관심사가 조용히 전체 선택으로 초기화되던 버그를 이 스토어 쪽에서도 함께 수정. **2026-07-08 테스트 커버리지 보강**: `deep_dive_result_screen.dart`도 report_screen.dart와 정확히 같은 패턴(`buildMetaLine(birthInfo)` 직접 호출)인데, `isLunar: true`로 렌더링해본 적이 한 번도 없었던 같은 종류의 비대칭을 발견(위 "상세 리포트 화면" 행 참고) — 회귀 테스트 추가("1998.08.15 · 오후 2시生 · 음력"이 그대로 표시되는지 확인). `flutter analyze`+`flutter test`(209개 전체 통과)+`./tool/check_build.sh`(APK 194,291,004 bytes) 검증 완료. **2026-07-08 대조로 발견한 공백**: `_InterestChip`은 `PastelToggleRow`(아래 "공용 위젯" 행 참고)와 완전히 같은 accentSoft+accentText 선택 색 조합을 독립적으로 재구현하는데, 이 화면 쪽 테스트는 지금까지 selected 시맨틱스만 확인했을 뿐 실제 배경·테두리·글자색 값 자체는 검증한 적이 없었음(테스트 파일에 `AppColors` import조차 없었음) — 관심사 하나를 꺼서 선택/비선택 대조군을 만들고 두 상태 모두 색 값 고정 테스트 추가(색 분기를 고정값으로 바꿔 테스트가 실제로 실패하는 것까지 확인 후 복원). `flutter analyze`+`flutter test`(214개 전체 통과, 아래 "공용 위젯" 행 픽스와 함께 +2)+`./tool/check_build.sh`(APK 194,291,004 bytes) 검증 완료. **2026-07-08 `build()`의 3단 폴백 마지막 분기(생성자·라우트 arguments 둘 다 없을 때의 하드코딩된 기본값 1998-08-15 14시) 미검증 발견** — result_screen.dart·report_screen.dart와 완전히 같은 패턴이지만 이 화면은 birthInfo를 자기 화면에 표시하지 않고 "심층 분석 보기" 제출 시 다음 화면에 넘기기만 해서 지금까지 이 폴백이 실제로 타는지 확인한 적이 없었음 — `const DeepDiveInputScreen()`으로 pumpWidget 후 제출까지 이어가 다음 화면의 메타 라인이 하드코딩된 기본값("1998.08.15 · 오후 2시生 · 양력")과 일치하는지 확인하는 테스트 추가(기본값을 다른 날짜로 바꿔 실패하는 것까지 확인 후 복원). `flutter analyze`+`flutter test`(220개 전체 통과, +3)+`./tool/check_build.sh`(APK 194,291,004 bytes) 검증 완료. **2026-07-08 순서 보장 미검증 발견(Explore 서브에이전트)**: `deep_dive_result_screen.dart`의 관심사 카드 목록이 `deepDiveInfo.interests`(Set, 삽입 순서 보장 안 됨)를 직접 순회하지 않고 `for (final interest in Interest.values) if (...contains(interest))`로 항상 `Interest.values` 선언 순서(연애·재물·직장·건강)를 따르도록 의도적으로 짜여 있는데, 지금까지 이 화면 순서 자체를 확인한 테스트가 없었음(기존 유일한 2개 이상 선택 테스트는 `{Interest.career, Interest.love}`로 넣어뒀는데도 `findsOneWidget`/`findsNothing`만 확인해 순서는 안 봄) — 만약 이 루프가 실수로 `for (final interest in deepDiveInfo.interests)`(Set 직접 순회)로 퇴보하면, 기존 assert는 전부 그대로 통과한 채 카드 순서만 사용자가 관심사를 고른 순서에 따라 조용히 뒤바뀌는(예: 재물운을 먼저 골랐으면 재물운이 위로) 실사용자 체감 회귀가 될 뻔했음. `tester.getTopLeft`로 두 카드의 세로 위치를 비교하는 assertion 추가(실제로 루프를 Set 직접 순회로 바꿔 테스트가 실패하는 것까지 확인 후 복원). `flutter analyze`+`flutter test`(222개 전체 통과, 기존 테스트에 assertion만 추가)+`./tool/check_build.sh`(APK 194,291,004 bytes) 검증 완료. **2026-07-08 접근성 회귀 발견 및 수정**: result_screen.dart의 `_CategoryCard`/`_OhaengMeaningCard`는 이미 2026-07-07에 "장식용 이모지까지 스크린 리더가 유니코드 이름으로 읽어 혼란스럽다"는 이유로 Semantics 라벨에서 이모지를 뺐는데(시각적 텍스트에는 그대로 유지), 정작 관심사 목록의 "원본"인 `_InterestChip`만 라벨에 이모지("💘 연애운")를 그대로 포함하고 있어서 지금까지 이 비대칭이 발견된 적이 없었음 — 라벨을 `interest.categoryTitle`만으로 수정(시각적 Text는 이모지 그대로 유지), 회귀 테스트 추가(이모지를 다시 넣어 실제로 실패하는 것까지 확인 후 복원). `flutter analyze`+`flutter test`(228개 전체 통과, +1)+`./tool/check_build.sh`(APK 194,291,004 bytes) 검증 완료. **2026-07-11 콘텐츠 문구 완전성 공백 추가 발견**: `deep_dive_readings.dart`의 `_careerReadingByOhaeng`(직장운 5개 오행 문구)도 위 "사주 결과 화면" 행에서 발견한 `ohaeng_readings.dart`의 문제(2026-07-08)와 완전히 같은 종류 — "5개 전부 비어있지 않고 서로 다르다"는 구조적 테스트만 있었을 뿐, 오행끼리 문구가 통째로 뒤바뀌어도 잡히지 않는 상태로 지금까지 단 한 곳에서도 실제 문구 값이 확인된 적이 없었음(ohaeng_readings.dart보다도 더 심함 — 그쪽은 부분적으로라도 값 검증이 있었는데 여긴 전혀 없었음). 5가지 문구 전체를 값으로 고정하는 테스트 추가(목-화 문구를 맞바꿔 기존 구조적 테스트는 통과하고 새 테스트만 실패하는 것까지 확인 후 복원). `flutter analyze`+`flutter test`(230개 전체 통과, +1)+`./tool/check_build.sh`(APK 194,291,004 bytes) 검증 완료. **2026-07-11 같은 종류의 공백 세 번째 발견**: `mbtiComments`(MBTI 16유형 코멘트)도 지금까지 "16개 전부 서로 다르다"는 구조적 테스트와 "INTJ는 mbtiComments['INTJ']와 같다"(production 코드의 같은 맵을 양쪽에서 다시 읽는 것이라 실제 문구가 틀렸는지 못 잡음)만 있었을 뿐, 16개 전체를 하드코딩된 문자열과 직접 대조한 적이 없었음 — INTJ-INTP 코멘트를 맞바꿔 기존 두 테스트 모두 그대로 통과하고 새 테스트만 실패하는 것까지 확인 후 복원. `flutter analyze`+`flutter test`(231개 전체 통과, +1)+`./tool/check_build.sh`(APK 194,291,004 bytes) 검증 완료. **2026-07-11 실제 경쟁 상태(race condition) 버그 발견 및 수정(Explore 서브에이전트)**: `initState()`가 `_loadSaved()`를 기다리지 않는데(fire-and-forget), 이미 저장된 관심사가 있는 상태(재방문)에서 그 비동기 로드가 끝나기 전에 사용자가 칩을 탭하면 `_toggleInterest()`의 `setState` 직후 뒤늦게 도착하는 `_loadSaved()`의 `setState`(저장된 값으로 무조건 덮어씀)가 방금 누른 탭을 조용히 되돌리는 실제 버그였음 — 사용자가 한 번이라도 직접 상호작용했으면 그 뒤 로드가 끝나도 더 이상 덮어쓰지 않도록 `_userInteracted` 플래그로 막음. 테스트로 재현하는 과정에서, 목(mock) SharedPreferences는 실제 지연이 없어 `pumpWidget()` 자체가 이미 로드를 다 끝내버려 경쟁이 재현 안 되는 것을 발견 — `getAll()`이 수동으로 완료시키는 `Completer`를 기다리는 가짜 스토어로 바꿔치기해 로드 완료 시점을 직접 통제하는 방식으로 결정적으로 재현(가드를 제거해 실제로 실패하는 것까지 확인 후 복원). `flutter analyze`+`flutter test`(234개 전체 통과, +1)+`./tool/check_build.sh`(APK 194,291,004 bytes) 검증 완료 |
| 십신(十神) 콘텐츠 확장 (미착수) | ⚪ 미착수 | (아직 없음 — 만든다면 `project/lib/features/deep_dive/` 또는 별도 `project/lib/features/ten_gods/`) | **2026-07-06 리서치로 발견한 콘텐츠 갭(docs/research/01_사주팔자_기초개념 참고)**: `project/lib/` 전체에 십신 관련 로직이 전혀 없음 — 오행 분포와 함께 명리학의 양대 해석 축인데 오행만 다루는 중. 웹 검색으로 "10개 그대로 노출 대신 5그룹(비겁/식상/재성/관성/인성)으로 단순화 + 실생활 문장" 실제 사례를 찾았고, 일간(day master) 기준 기계적 계산(오행 상생상극 + 음양 비교)이라 새 라이브러리·AI 연동 없이도 심층 분석 MBTI처럼 1단계 규칙 기반으로 구현 가능해 보임. 시장 1위 포스텔러도 캐주얼 메인 앱엔 안 보여주고 전문가용 서브 앱에서만 노출하는 걸 보면, 우리도 "심층 분석" 확장 화면 패턴이 잘 맞아 보임. **다만 자동화 루프가 임의로 구현하지 않는다** — 지장간(支藏干) 포함 여부·용신(用神) 판단법(억부법/조후법)처럼 십신도 학파마다 세부 판정이 갈릴 여지가 있고(연구 문서의 "해석 단계의 학파 편차" 절 참고), 신규 화면·콘텐츠·잠재적 결제 연계까지 걸린 제품 결정이라 `accent` 브랜드 컬러·정밀 절기 계산·AI 해석 연동·결제 로직과 같은 급의 사람 결정 대기 항목으로 취급한다. 향후 개발 모드 사이클은 이 행을 "미착수 항목"으로 자동 선택해 구현을 시작하지 말고, 사용자가 명시적으로 진행을 지시할 때까지 그대로 둘 것 |
| 사주 계산 로직 | 🟡 진행 중 | `project/lib/core/saju/` | `ganzhi.dart`+`four_pillars.dart`로 4주(년/월/일/시) 60갑자 + 오행 집계 구현 완료, 결과 화면에 실제 연결됨. 시주 계산의 전통 시진 경계(23/0시=자시, 1/2시=축시, 11/12시=오시 등), 입춘(2/3 vs 2/4) 경계값, 월주(오호둔년기월법) 실제 계산값, `ohaengCount`(오행 집계)의 8글자 분포 자체를 각각 직접 검증하는 테스트 있음(`test/core/saju/four_pillars_test.dart`) — 4주(년/월/일/시) + 오행 집계 전부 값 자체가 테스트로 잠겨 있음. **단, 절기 근사 계산이라 정확도 한계 있음(PROGRESS.md 참고, 출시 전 만세력 대조 검증 필요)** → 정밀 검증 전까지 🟡. **2026-07-06 리서치로 추가 확인**: 자시(子時) 관법은 3가지(`midnight`/`splitJasi`/`jasi`) 중 가장 단순한 `midnight`(롤오버 없음)을 씀 — 전통적으로 더 흔한 `splitJasi`와는 23시대 출생자의 시주가 달라질 수 있음(docs/research/04_입력정보_요구사항, 07_오픈소스_계산로직 참고). 진태양시(출생지 경도·균시차 보정)는 전혀 반영하지 않음 — `BirthInfo.birthPlace`가 계산에 전달되지 않음, 서울 기준 약 -14분~-48분의 보정 오차가 있어 시진 경계 부근 출생자는 시주가 바뀔 수 있음(docs/research/07_오픈소스_계산로직 참고). 두 항목 모두 `four_pillars.dart`의 doc-comment와 온보딩/상세 리포트 화면의 사용자 안내 문구에 반영됨. 구현 여부(포팅)는 절기 근사와 마찬가지로 사람 결정 대기. **2026-07-06 네 번째 정확도 이슈 발견**: `calculateFourPillars()`는 `isLunar` 파라미터 자체를 받지 않고 `birthDate`를 무조건 양력으로 취급한다 — `BirthInfo.isLunar`는 화면 표시(메타 라인 "음력" 라벨)에만 쓰일 뿐, 사용자가 음력으로 입력해도 그 날짜를 양력인 것처럼 그대로 계산에 사용한다. 앞의 세 항목(절기 근사·자시 관법·진태양시)은 경계 근처에서만 며칠~몇십 분 어긋나는 정도지만, 이 항목은 음력으로 입력한 모든 사용자의 네 기둥이 (변환 없이 엉뚱한 날짜로 계산되어) 통째로 틀릴 수 있어 영향 범위가 훨씬 크다. 정밀 구현에는 만세력 데이터 기반 음양력 변환(예: 위 `manseryeok`/`lunar-javascript` 계열)이 필요해 다른 세 항목과 마찬가지로 사람 결정 대기 — `four_pillars.dart`의 doc-comment, 온보딩/상세 리포트 안내 문구에 반영하고 `result_screen_test.dart`에 "isLunar: true여도 isLunar: false와 4기둥이 완전히 동일하다"는 현재 동작을 값으로 고정하는 회귀 테스트 추가. **2026-07-07 다섯 번째 정확도 이슈 발견(리서치)**: 한국은 표준시가 역사적으로 4번 바뀌었고, 서머타임(일광절약시간제)도 1948~1960년·1987~1988년 두 시기에 시행됨(국가기록원·한국민족문화대백과사전·사주포럼 세 곳에서 교차 확인) — 서머타임 기간엔 시계가 실제보다 1시간 빨랐는데 우리 앱은 이를 전혀 반영하지 않음. 기존 AST 미보정(최대 -48분)보다 큰 ±60분 오차이고, 1987~1988년생은 현재 핵심 이용자 연령대와 겹쳐 영향받는 실사용자가 있을 수 있음. 절기 정밀 계산과 무관한(단순 날짜 조회 문제) 독립적인 사람 결정 대기 항목으로 추가 — `manseryeok`의 `time/korea-timezone.ts`(약 50줄, 표준시·서머타임 이력 조회 테이블)가 그대로 포팅 가능한 참고 구현이며, 원광만세력 같은 실제 앱은 한국 출생자에겐 사용자 입력 없이 자동 보정하는 것이 관행임을 확인(자세한 내용은 docs/research/07_오픈소스_계산로직 참고). 다섯 이슈(절기·자시·AST·음력·서머타임) 전체를 영향크기·난이도·절기 계산과의 독립 여부 기준으로 정리한 종합표도 같은 문서에 있음. **2026-07-07 계산 로직 자체의 실제 버그 2건 발견 및 수정(위의 5가지 "사람 결정 대기" 정확도 이슈와는 성격이 다름 — 이건 이미 채택한 간이 계산 방식 안에서도 내부적으로 앞뒤가 안 맞던 구현 버그라 자동으로 수정함)**: (1) `daysSinceEpoch` 계산이 로컬(non-UTC) `DateTime.difference()`를 써서, 두 날짜 사이에 서머타임 오프셋 변경이 끼면(바로 위 다섯 번째 이슈에서 다룬 1948~1960·1987~1988년 서머타임 기간) 실제 달력 일수보다 하루 적게 나와 일주·시주가 통째로 하루씩 밀리는 것을 확인(`TZ=Asia/Seoul dart`로 실측: 1987-08-15/1950-08-15 생일이 `TZ=UTC`로 실행했을 때와 다른 일주가 나옴) — `DateTime.utc()`로 두 날짜를 정규화해 서머타임과 무관하게 항상 정확한 날짜 차이가 나오도록 수정, `TZ=Asia/Seoul`·`TZ=UTC` 양쪽에서 동일한 결과가 나오는 것으로 교차 검증. (2) 월지(月支) 계산이 그레고리력 월(`date.month`)만 보고 결정돼, 년주는 입춘(2/4 근사) 이전이면 전년도로 롤백하는데 월지는 2/1~2/3에도 "2월→인월"로 계산돼 년주·월주가 서로 모순되는 날짜가 있었음(실측: 2024-02-03의 월주가 갑인/인월로 나왔는데, 입춘 전이라 아직 인월이 시작되지 않았으니 1월 15일과 같은 축월/을축이어야 정답) — 년주와 같은 `date.day < 4` 판단을 월지에도 적용해 2/1~2/3을 1월과 동일하게 취급하도록 수정. 두 버그 모두 `four_pillars.dart`에 수정 근거 doc-comment로 남기고, `four_pillars_test.dart`에 회귀 테스트 추가(서머타임 기간 일주 3건, 입춘 이전 월지 2건) — 기존 11개 테스트 케이스는 전부 이 두 버그의 사각지대(서머타임 기간 밖 날짜, `.year.label`만 검증하고 `.month`는 검증 안 함)에 있어 수정 전후로 값이 안 바뀌었음을 확인. `flutter analyze`+`flutter test`(197개 전체 통과)+`./tool/check_build.sh`(APK 194,291,122 bytes) 검증 완료. 5가지 "사람 결정 대기" 정확도 이슈(절기 근사·자시 관법·진태양시·음력·서머타임 미반영 자체)는 이 수정과 무관하게 그대로 남아 있어 행 상태는 여전히 🟡. **2026-07-08 테스트 커버리지 보강**: `FourPillars.dominantOhaeng`의 동률 처리(`reduce((a,b) => a.value >= b.value ? a : b)`, 먼저 나온 쪽이 이김)를 검증하는 기존 테스트는 "1위(금)는 유일하고 그 아래(목·토)만 동률"인 경우만 확인해, 정작 동률 규칙이 실제로 의미를 갖는 "1위 자리 자체가 동률"인 경우는 값으로 확인한 적이 없었던 커버리지 공백을 발견 — `dart`로 실제 생년월일을 탐색해 1990-01-12/14시(년주 기사·월주 정축·일주 정미·시주 정미, 화·토가 정확히 4개씩 동률 공동 1위이고 목·금·수는 0인 깔끔한 사례)를 찾아 목화토금수 순서상 화가 선택되는지 값으로 고정하는 테스트 추가. `flutter analyze`+`flutter test`(205개 전체 통과)+`./tool/check_build.sh`(APK 194,291,004 bytes) 검증 완료 |
| 공용 위젯 | 🟢 완료 | `project/lib/shared/widgets/` | `PastelPillButton`, `PastelToggleRow<T>`, `PastelCard` — birth_input/result 화면의 중복 스타일을 공용화. 둘 다 `Semantics(button:, selected/enabled:, onTap:, excludeSemantics: true)`로 선택/비활성 상태와 탭 액션을 스크린 리더에 정확히 전달. `PastelToggleRow`는 옵션 `semanticLabel`로 목업의 `aria-label` 그룹 라벨과 동등한 그룹 전체 라벨(`Semantics(container: true, label:)`)도 지원 — birth_input의 양력/음력·성별 토글에서 사용. `PastelToggleRow`에 `SemanticsAction.tap`을 직접 실행해 `onChanged`가 호출되는지 확인하는 회귀 테스트 추가 — 기존 테스트는 `matchesSemantics`의 `hasTapAction`이나 `tester.tap()`(실제 히트테스트, InkWell을 직접 타서 Semantics의 `onTap:` 누락을 못 잡음)만 확인했었는데, `onTap:` 선언을 실수로 빠뜨려도 이 두 검증은 여전히 통과해버리는 것을 직접 재현해 확인함 — 스크린 리더가 실제로 보내는 액션 경로 자체를 검증해야 이 회귀(과거 `PastelPillButton`/`PastelToggleRow`에서 실제로 겪은 것과 같은 종류)를 잡을 수 있다. **2026-07-05 감사**: 같은 `Semantics(excludeSemantics: true, onTap:)` 패턴을 쓰는 인터랙티브 위젯 전체(`PastelToggleRow`, 심층 분석의 `_InterestChip`, `PastelPillButton`)를 전수 점검 — `PastelPillButton`에도 동일한 `SemanticsAction.tap` 직접 실행 회귀 테스트를 추가하고, `onTap:` 제거 시 기존 `matchesSemantics(hasTapAction:)` 테스트와 새 테스트 둘 다 실패하는 것까지 확인 후 원복함(이 위젯은 기존에도 `hasTapAction` 플래그 검증이 있어 부분적으로는 이미 보호돼 있었지만, 플래그 존재 여부와 실제 콜백 실행은 별개라 새 테스트로 실제 실행까지 확인). 3개 위젯 모두 이 회귀 패턴에 대한 실제 액션 실행 테스트를 갖추는 것으로 감사 완료. **2026-07-06 목업 대조로 발견한 실제 시각적 불일치 2건 수정**: (1) `PastelToggleRow`의 선택된 상태가 목업(`.pill.is-active`)과 달리 진한 `accent` 배경+흰(`accentInk`) 글자를 써서 브랜드 CTA 버튼(`.btn-primary`)과 같은 조합을 잘못 재사용하고 있었음 — 실제 목업은 옅은 `accentSoft` 배경+진한 `accentText` 글자를 쓰고(테두리만 `accent` 유지), `accentText`는 이미 `accentSoft` 위에서 WCAG AA 4.5:1을 통과하도록 설계돼 있어(`app_colors_contrast_test.dart`로 검증됨) 목업대로 고치면서 "공통 테마/디자인 토큰" 행에 있던 선택된 토글의 텍스트 대비 미달 문제도 자연히 해결됨(테두리 자체의 비텍스트 대비 미달은 목업도 그대로 accent를 써서 남아있음). (2) `PastelPillButton`/`PastelToggleRow` 둘 다 `BorderRadius.circular(999)`(완전히 둥근 알약 모양)를 썼는데, 목업의 실제 CSS(`.pill`)는 12px 모서리 반경만 씀 — 12로 수정. 두 위젯 모두 색상/반경 자체는 시맨틱스와 무관해 기존 접근성 테스트(탭 액션·selected 플래그)는 전부 그대로 통과함(회귀 없음 확인). **2026-07-07 목업 대조로 발견한 테두리 두께 불일치 수정**: 목업의 `.pill`은 `border:1.5px solid`인데, `PastelPillButton`/`PastelToggleRow`(및 아래 "심층 분석" 행의 `_InterestChip`) 셋 다 `Border.all(color:)`만 쓰고 `width`를 지정하지 않아 Flutter 기본값인 1px이었음 — 셋 다 1.5로 수정(`PastelCard`는 목업 `.pillar-card`/`.cat-card`가 실제로 1px이라 그대로 둠, `test/shared/widgets/pastel_card_test.dart`의 기존 테두리 검증과도 일치). **2026-07-07 목업 대조로 발견한 안쪽 여백 불일치 수정**: 목업 `.pill`은 `padding:9px 14px`인데 `PastelPillButton`(가로 20/세로 14)·`PastelToggleRow`(가로 20/세로 12) 둘 다 이보다 여유가 많았음 — 가로 14/세로 9로 수정(`flutter test` 전체로 회귀 없음 확인, `_InterestChip`도 같은 이유로 아래 "심층 분석" 행 참고). **2026-07-07 검토 후 보류(코드 변경 없음)**: `PastelPillButton`/`PastelToggleRow`/`_InterestChip` 셋 다 이제 `.pill` 모양 값(반경 12, 테두리 1.5, 패딩 14/9)이 리터럴로 동일하게 3곳에 중복돼 있어 공유 상수(예: `AppDimens`)로 묶는 방안을 검토했으나, 이 저장소의 명시적 지침("Three similar lines is better than a premature abstraction")에 따라 아직 이 정도 중복(3곳, 리터럴 값 4개)은 새 추상화를 도입할 근거로 부족하다고 판단해 보류 — 향후 같은 값이 4번째 위치에도 나타나거나 실제로 드리프트가 발생하면 그때 재고려. **2026-07-07 목업 대조로 발견한 카드 모서리 반경 불일치 수정**: `PastelCard`(4기둥 카드·카테고리 카드·심층 분석 카드·상세 리포트 카드가 전부 이 기본값을 그대로 씀)의 기본 `borderRadius`가 16px이었는데, 목업의 실제 카드 클래스(`.pillar-card`, `.cat-card`)는 둘 다 13px을 씀 — 지금까지 버튼/토글의 999px 알약 모양 오차는 여러 차례 잡혔지만, 이 공용 카드 컴포넌트의 기본 반경 자체는 한 번도 목업과 대조된 적이 없었다. 기본값을 13으로 수정(`test/shared/widgets/pastel_card_test.dart`의 기본값 검증 테스트도 16→13으로 갱신). 카드별 패딩(`.pillar-card`는 9px 4px, `.cat-card`는 10px 11px로 서로 다르고 실제 구현의 14px 균일 패딩과도 다름)은 헤더/메타 라인 폰트 크기와 마찬가지로 실제 모바일 터치 영역·가독성을 위한 의도적 조정일 가능성이 있어 이번엔 반경만 수정하고 그대로 둠(위 "판단" 사례와 같은 이유). **2026-07-08 대조로 발견한 공백**: `PastelToggleRow`가 2026-07-06에 accentSoft+accentText 조합으로 고쳐진 뒤로, 그 근거인 실제 색 값 자체(배경·테두리·글자색)는 이 위젯 테스트에서도(그리고 같은 조합을 재구현한 심층 분석 `_InterestChip`에서도, 위 "심층 분석" 행 참고) 한 번도 값으로 확인된 적이 없었음 — 지금까지는 selected 시맨틱스 플래그만 검증돼 있었다. 선택/비선택 두 상태 모두 배경(`accentSoft`/`bgCard`)·테두리 색·너비(`accent`/`border`, 1.5px)·글자색(`accentText`/`ink`) 값을 고정하는 회귀 테스트 추가(색 분기를 고정값으로 바꿔 테스트가 실제로 실패하는 것까지 확인 후 복원). `flutter analyze`+`flutter test`(214개 전체 통과, `_InterestChip` 픽스와 함께 +2)+`./tool/check_build.sh`(APK 194,291,004 bytes) 검증 완료. **2026-07-08 같은 종류의 공백 추가 발견**: `PastelPillButton`도 활성/비활성 배경색(`onTap == null ? border.withValues(alpha: 0.4) : bgCard`)을 갖는데, 기존 테스트 파일엔 `AppColors` import조차 없이 시맨틱스(enabled 플래그)만 검증돼 있었음 — 활성/비활성 두 상태를 각각 pumpWidget해 배경색 값을 고정하는 회귀 테스트 추가(색 분기를 고정값으로 바꿔 실패하는 것까지 확인 후 복원). `flutter analyze`+`flutter test`(215개 전체 통과, +1)+`./tool/check_build.sh`(APK 194,291,004 bytes) 검증 완료 |
| 빌드 검증 스크립트 | 🟢 완료 | `project/tool/check_build.sh` | 표준 빌드 체크 절차(pub get → analyze → test → build apk --debug → 산출물 존재/크기 확인)를 한 번에 실행하는 스크립트. `CLAUDE.md`의 "빌드/린트 체크" 섹션 및 오버나이트 루프 프롬프트와 절차가 동일. 실패 시 즉시 중단(`set -e`)하고 산출물이 없거나 1MB 미만이면 별도로 실패 처리 |
| 앱 아이콘 | 🟢 완료 | `project/test/tool/generate_app_icon.dart`, `project/assets/icon/icon.png` | 파스텔 큐트 톤(초승달+오행 5색 알갱이) 아이콘. SVG→PNG 변환 도구 없이 `RenderRepaintBoundary.toImage()`로 직접 캡처해 소스 PNG를 생성(주의: `Icon`/텍스트 글리프는 `flutter test`가 빈 박스로 치환하므로 `CustomPainter`로 순수 도형만 그림). `flutter_launcher_icons`(dev_dependency, pubspec.yaml에 설정)로 Android/iOS/macOS 아이콘 일괄 생성. 아이콘을 바꾸려면 스크립트 수정 후 `flutter test test/tool/generate_app_icon.dart` → `dart run flutter_launcher_icons` 순서로 재실행 |

상태 범례: 🟢 완료 · 🟡 진행 중/부분 완료 · ⚪ 미착수

## 참고 자료 (원자료, 수정 지양)

| 자료 | 경로 |
|---|---|
| 사주팔자 기초개념 | `docs/research/01_사주팔자_기초개념/` |
| 경쟁앱 분석 | `docs/research/02_경쟁앱_분석/` |
| UI/UX 트렌드 | `docs/research/03_UI_UX_레퍼런스/` |
| 입력정보 요구사항 | `docs/research/04_입력정보_요구사항/` |
| 결과화면 사례 | `docs/research/05_결과화면_사례/` |
| 소셜미디어 레퍼런스 | `docs/research/06_소셜미디어_레퍼런스/` |
| 오픈소스 계산로직 | `docs/research/07_오픈소스_계산로직/` |
| UI/UX 목업 4종 | `docs/mockups/` |
