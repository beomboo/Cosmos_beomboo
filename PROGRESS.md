# 진행 로그

> **2026-07-05부로 사용자 요청에 따라 이 파일은 더 이상 갱신하지 않는다.** 아래 내용은 그 시점까지의 기록이며 참고용으로만 남겨둔다. 이후 작업 상태는 [PROJECT_ROUTER.md](PROJECT_ROUTER.md)만으로 추적한다. 빌드/테스트 관련 알려진 이슈는 [CLAUDE.md](CLAUDE.md) 참고.

## 현재 상태 요약

### ✅ 완료
- 앱 골격: 온보딩 → 생년월일시 입력 → 계산 중 → 결과 → 상세 리포트 (라우팅 포함)
- 사주 계산 로직(4주 60갑자 + 오행 집계) — 절기 근사치 기반(정확도 한계는 화면에도 안내 문구로 노출)
- 생년월일시 저장(SharedPreferences) — 재실행 시 결과 화면 바로 표시, "다시 입력하기"로 초기화 가능. 저장/조회 실패해도 앱 진행에 지장 없도록 방어 코드 적용
- 결과 화면 카테고리 풀이(연애·재물·건강·성격) — 오행별 규칙 기반 텍스트
- 공유 기능 — 9:16 이미지 카드 캡처 + 텍스트, 캡처 실패 시 텍스트 전용 자동 폴백
- 공용 위젯(PastelPillButton/ToggleRow/Card) 추출, 접근성 시맨틱스(선택/비활성/버튼 라벨) 적용
- 색상 대비(WCAG) 일부 수정(inkSoft), 한국어 로케일 적용(datePicker/timePicker)
- OS 홈 화면 앱 이름 통일("사주랑"), macOS 플랫폼 추가
- 오버나이트 자동화 루프 + 빌드 오류(stale dex) 자동 대응 규칙
- 앱 아이콘 교체(파스텔 큐트 톤 — 초승달 + 오행 5색 알갱이, `flutter_launcher_icons`로 Android/iOS/macOS 일괄 적용)
- 오행색 텍스트 WCAG 대비 수정(`ohaengTextColors` 추가 — 배경/아이콘용 `ohaengColors`는 그대로 두고 텍스트 전용 진한 변형만 추가하는 방식으로, 브랜드 색상 자체는 건드리지 않음)
- 빌드 체크 절차 강화(`flutter pub get` 선행 체크 + 산출물 실제 존재/크기 검증) + `project/tool/check_build.sh`로 표준 절차 스크립트화
- 온보딩 화면 목업 정합화(헤드라인/설명 문구 실제 반영, "어떻게 계산되나요?" 설명 다이얼로그 추가)
- 계산 중 화면 로딩 문구 3종 순환(목업 STEP 3, 1.8초 간격, reduce-motion 존중)
- 계산 중 화면 궤도 애니메이션도 reduce-motion 존중(로딩 문구와 일관되게)
- 온보딩→입력→계산 중→결과→상세 리포트까지 실제 라우트로 잇는 전체 여정(end-to-end) 테스트 추가
- 저장된 값 → "다시 입력하기" → 새 값 재제출까지 실제 라우트로 잇는 회귀 방지 테스트 추가(이전 값이 새 값으로 완전히 교체되는지 확인)
- 공유 카드(ShareCard) 레이아웃 오버플로 수정(이름 20자+출생지 30자 최대 길이 조합 시 실제로 넘쳤던 것을 실측으로 확인·수정)
- **`project/` 전체가 이제 git에 커밋됨(2026-07-05, 사용자 요청)** — 그동안 로컬 파일로만 존재하던 Flutter 앱 코드 전체를 기능 단위 15개 커밋(운영 문서 → Dart MCP 설정 → 스캐폴드 → 테마 → 공용 위젯 → 계산 로직 → 저장소 → 라우팅 → 화면들(온보딩/입력/계산 중/결과/리포트) → 앱 진입점 → 아이콘)으로 나눠 커밋 완료. git 히스토리상 백업/복구 지점이 없던 긴급 이슈 해소
- 4기둥 카드(결과 화면)·명식 breakdown 행(상세 리포트)에 스크린 리더용 병합 시맨틱스 추가 — 값과 라벨이 따로 읽혀 맥락이 끊기던 문제 수정
- 결과 화면 "공유하기" 버튼의 실제 탭 동작(이미지 캡처 → `SharePlus.instance.share()` → 실패 시 스낵바)을 실제로 탭해서 검증하는 테스트 추가 — 그동안 "싱글턴이라 자동 테스트 불가"로 판단했던 게 재검토 결과 실제로는 테스트 가능했음(아래 타임라인 참고)
- `PastelToggleRow`에 `semanticLabel`(그룹 라벨) 옵션 추가 — 양력/음력·성별 토글에 목업의 `aria-label` 그룹 라벨과 동등한 스크린 리더 그룹 안내 적용
- 정오(12시) 경계값 테스트 추가 — 12시간제 표기("오후 12시")와 시주 지지 계산(오시) 양쪽 다 검증(아래 타임라인 참고)
- **MBTI·관심사 기반 심층 분석 1단계(사용자 요청, 2026-07-05)** — 결과 화면 → "MBTI·관심사로 심층 분석 받기" → 관심사(연애/재물/직장/건강) 다중 선택 + MBTI(선택, 4축 토글) 입력 → 우세 오행 기준 풀이 + MBTI 코멘트를 보여주는 심층 분석 화면 신규 구현. 조합 폭발(오행5×MBTI16×관심사)을 피하려고 관심사는 기존 오행별 카테고리 풀이 재사용(+직장운만 신규), MBTI는 오행과 무관한 16유형 독립 코멘트로 분리하는 "조합형 규칙 기반" 방식 채택 — 실제 AI 연동(2단계)은 API 비용/개인정보 동의가 걸려 있어 별도 사람 결정 대기로 남겨둠(아래 참고)
- **심층 분석 입력값(MBTI·관심사) 영속 저장** — `DeepDiveInfoStore`(`BirthInfoStore`와 동일 패턴)로 SharedPreferences에 저장, 화면을 다시 열면 이전 선택이 그대로 반영됨(첫 방문 null과 "관심사를 전부 꺼서 저장"을 구분해 의도치 않게 기본값으로 되돌아가지 않도록 처리)
- 테스트 145개 (`flutter analyze`/`flutter test`/Android 빌드로 매 사이클 검증)

### 🟡 진행 중 / 사람 결정 대기
- **macOS/iOS 로컬 빌드 실패 — 근본 원인 확정**: `com.apple.provenance` 코드사이닝 오류의 실제 원인은 Claude Code 세션이 아니라 **이 Mac의 iCloud Drive "데스크탑 및 도큐먼트 폴더" 동기화**였음. 프로젝트가 `~/Desktop` 아래 있어 iCloud가 빌드 산출물까지 동기화하려다 저장공간 초과(quota exceeded)로 실패하는 것을 `brctl status`로 직접 확인함. 사용자가 시스템 설정에서 해당 동기화를 끄기로 결정(진행 중) — **꺼졌다는 확인을 받으면 잔여 메타데이터 정리 후 macOS 빌드 재검증 필요**
- **브랜드 컬러 WCAG 대비 — 오행색 텍스트는 해결, `accent` 버튼의 흰 텍스트는 여전히 사람 결정 대기**: 오행색(wood/fire/earth/metal/water)을 텍스트로 쓰던 7곳(`share_card.dart`, `report_screen.dart`, `result_screen.dart`)을 실측했더니 실은 metal/water도 4.5:1 미달이었고(기존엔 wood/fire/earth만 알려져 있었음), 심지어 오행 소프트 배경(`ohaengSoftColors`) 위에서는 다섯 색 전부 3.8~4.1:1로 더 크게 미달이었던 것도 새로 확인함 — `ohaengTextColors`(각 색을 ink 쪽으로 34~49% 블렌드한 텍스트 전용 진한 버전, `inkSoft`와 동일한 기법)를 추가해 텍스트로 쓰이는 7곳만 교체했고, 배경/아이콘/바 차트 채우기로 쓰이는 원래 `ohaengColors`는 전혀 건드리지 않았으므로 브랜드 색상 자체(목업의 고정 팔레트)는 그대로 유지됨. **다만 `accent`(#FF6B8A) 배경 위 흰 텍스트(주요 버튼들의 CTA 텍스트, 대비 2.72:1)는 여전히 미해결** — 이건 오행색과 달리 텍스트 전용 변형을 추가하는 걸로 해결이 안 되고, `accent` 배경 자체를 어둡게 하거나 버튼 글자색을 흰색이 아닌 다른 색으로 바꿔야 하는데 둘 다 앱 전체에서 가장 눈에 띄는 CTA 버튼의 실제 브랜드 룩을 바꾸는 일이라 계속 사람 결정 대기로 남겨둠
- **사주 계산 정확도 정밀화**(절기 실측 계산) — 외부 절기 데이터 포팅 또는 태양 위치 계산이 필요해 규모가 크고 검증이 까다로움. 신중히 접근 필요
- **심층 분석 2단계(실제 AI 해석 연동)** — 1단계(조합형 규칙 기반)는 완료했지만, 진짜 개인화된 문단을 LLM으로 생성하려면 API 키·비용, 지금은 완전 오프라인인 앱에 네트워크 의존성 추가, 생년월일시+MBTI+관심사 같은 개인정보를 외부로 보내는 것에 대한 동의 문구, 실패 시 폴백까지 결정해야 해서 사람 결정 대기로 남겨둠 — 기존 "카테고리 풀이 실제 AI 해석 연동" 항목과 동일한 종류의 결정

### ⬜ 예정 / 후보
- 상세 리포트 유료화/결제 로직 (현재는 전부 무료 MVP)

---

## 타임라인 (최신순, 한 줄 요약)

- **2026-07-05 (오버나이트 루프 재개 후 8회차)**: ⬜ "예정/후보"에 남은 유일한 항목(상세 리포트 유료화/결제 로직)이 사람 결정 대기 목록에 있어 건드릴 수 없어, 기존에 반복해온 "새 화면의 검증 빈틈 찾기" 방식을 이어서 `deep_dive_result_screen_test.dart`를 다시 훑어봄 — result_screen_test.dart/report_screen_test.dart는 `buildMetaLine`이 조립하는 메타 라인(날짜·시간·양음력·성별·출생지)이 화면에 실제로 렌더링되는지 값으로 검증해뒀는데, 같은 `buildMetaLine`을 재사용하는 심층 분석 결과 화면만 지금까지 "이름" 표시 여부만 확인했을 뿐 메타 라인 자체는 한 번도 값으로 검증한 적이 없었다는 빈틈을 발견 — `buildMetaLine` 자체는 순수 함수 단위 테스트로 이미 잠겨 있지만, 실제로 이 화면의 `Text` 위젯에 그 값이 그대로 전달되는지는 별개 확인이 필요했음. 성별·출생지까지 포함한 `BirthInfo`로 화면을 렌더링해 "1998.08.15 · 오후 2시生 · 양력 · 여성 · 서울특별시"가 정확히 보이는지 확인하는 테스트 추가 — 실행해보니 예상대로 정확히 통과(버그 없었음, 순수 검증 강화). 테스트 1개 추가 — 총 145개, `flutter analyze` 클린, `./tool/check_build.sh` 전체 통과(APK 194MB). Dart MCP는 이번 사이클에도 여전히 "⏸ Pending approval"로 표시됨(사용자가 별도 세션에서 승인했다고 확인했던 것과 무관하게, 이 자동화 세션 관점에서는 계속 대기 상태로 보임 — 지난 사이클에 남긴 메모와 동일한 관찰).
- **2026-07-05 (오버나이트 루프 재개 후 7회차)**: ⚪ 항목이 없어 PROGRESS.md의 ⬜ "예정/후보" 중 사람 결정이 필요하지 않은 항목(심층 분석 입력값 영속 저장 — 결제 로직은 사람 결정 대기 목록에 있어 제외)을 진행. `BirthInfoStore`와 완전히 동일한 패턴으로 `core/storage/deep_dive_info_store.dart`를 추가해 MBTI·관심사 선택을 SharedPreferences에 저장 — 관심사는 `Set<Interest>`를 이름 목록(`List<String>`)으로, MBTI는 4개 축 이름을 각각 저장하고 넷 다 있어야 복원(하나라도 없으면 "모름"으로 간주). 설계에서 주의한 점: `load()`가 "저장된 적이 한 번도 없음(null)"과 "저장은 됐지만 관심사를 전부 꺼서 비어 있음"을 구분하지 못하면, 사용자가 의도적으로 전부 해제했던 선택이 다음 방문 때 기본값(전체 선택)으로 되돌아가 버리는 실제 버그가 될 뻔했음 — 이 경계 케이스를 테스트로 명시적으로 잠갔다. `deep_dive_input_screen.dart`에 `initState()`에서 저장된 값을 비동기로 불러와 반영하는 로직과 제출 시 저장하는 로직을 추가(둘 다 저장/조회 실패해도 화면 진행은 막지 않도록 방어 — 기존 `BirthInfoStore` 사용처와 동일한 원칙). 이 과정에서 이 화면의 기존 위젯 테스트들이 `SharedPreferences.setMockInitialValues({})` 없이 작성돼 있어 `initState`의 새 비동기 로드와 충돌할 뻔한 걸 미리 발견해 `setUp`에 추가. 테스트 8개 추가(스토어 단위 테스트 6개 — 경계 케이스 포함, 화면 사전 채움/제출 시 저장 테스트 2개) — 총 144개, `flutter analyze` 클린, 신규 코드 커버리지 100%, `./tool/check_build.sh` 전체 통과(APK 194MB). Dart MCP는 사용자가 지난 대화에서 별도 세션으로 승인했다고 확인했지만, 이 사이클에서 `claude mcp list`로 다시 보니 "⏸ Pending approval"로 나타남 — 승인이 세션마다 별도로 유지되는 것으로 보이며, 이 자동화 사이클 자체는 그 승인과 무관하게 기존 CLI 절차로 정상 진행됨.
- **2026-07-05 (오버나이트 루프 재개 후 6회차)**: 지난 두 사이클이 심층 분석(MBTI·관심사) 기능의 개별 화면 커버리지를 닫았던 흐름을 이어서, `test/widget_test.dart`의 "실제 라우트 전체 여정" 패턴(온보딩→상세 리포트 여정 테스트, 재입력 여정 테스트)이 아직 심층 분석 경로는 다루지 않는다는 빈틈을 발견 — 지금까지 심층 분석 관련 테스트는 전부 스텁 라우트(`onGenerateRoute` 커스텀)나 화면 직접 생성으로만 검증했을 뿐, `CosmosSajuApp`의 실제 `AppRoutes.routes` 배선을 그대로 타고 결과 화면 → 심층 분석 입력 → 심층 분석 결과까지 이어서 통과한 적은 없었다 — 각 구간이 개별로 통과해도 실제 네임드 라우트 배선이나 `MaterialPageRoute` 직접 push 인자 전달이 깨지는 회귀는 이런 전체 여정 테스트가 아니면 못 잡는다. 1998-08-15/14시(우세 오행 '금'으로 이미 검증된 조합)로 실제 결과 화면부터 "MBTI·관심사로 심층 분석 받기 →" 버튼을 실제로 탭해 심층 분석 입력 화면까지 도달하고, 기본값(관심사 전체 선택·MBTI 미입력) 그대로 제출해 심층 분석 결과 화면에서 4개 관심사 전부 우세 오행 기준 실제 풀이 문구가 보이고 MBTI 코멘트는 없는지 확인하는 테스트 추가 — 실행해보니 예상대로 정확히 통과(버그 없었음, 순수 검증 강화). 테스트 1개 추가 — 총 136개, `flutter analyze` 클린, `./tool/check_build.sh` 전체 통과(APK 194MB). Dart MCP는 여전히 승인 대기.
- **2026-07-05 (오버나이트 루프 재개 후 5회차)**: ⚪ 항목이 없어, 직전 사이클에 사용자 요청으로 막 추가된 심층 분석(MBTI·관심사) 기능이 아직 이 오버나이트 루프 특유의 검증 사이클(커버리지 확인 등)을 한 번도 거치지 않았다는 점에 착안 — `flutter test --coverage`로 재보니 `deep_dive_input_screen.dart`가 93.9%(4줄 누락)로 새 코드 중 가장 낮았음. 누락된 줄을 직접 대조해보니 ①관심사 칩을 다시 탭해 재선택하는 분기(`_toggleInterest`의 "추가" 쪽 — 기존 테스트는 항상 전체 선택 상태에서 끄는 것만 확인했지 껐다가 다시 켜는 흐름은 없었음), ②MBTI 축 4개 중 S/N 하나만 바꿔봤을 뿐 나머지 세 축(E/I·T/F·J/P)의 `onChanged` 콜백 자체는 한 번도 실제로 발동된 적이 없었다는 것을 발견 — 총합 코드("ENTJ")는 맞았어도 그건 우연히 기본값이 나머지 세 축과 일치했을 뿐, 그 콜백들이 실제로 동작하는지는 검증되지 않은 상태였다. 네 축을 전부 반대로 뒤집어 "INFP"가 정확히 나오는지 확인하는 테스트와, 관심사 칩을 껐다 다시 켜는 테스트를 추가 — 실행해보니 둘 다 예상대로 정확히 통과(버그 없었음), `deep_dive_input_screen.dart` 커버리지 100% 달성. 테스트 2개 추가 — 총 135개, `flutter analyze` 클린, `./tool/check_build.sh` 전체 통과(APK 194MB). Dart MCP는 여전히 승인 대기.
- **2026-07-05 (사용자 요청 — MBTI·관심사 심층 분석 1단계)**: 사용자가 "MBTI, 관심사(재테크·연애·직장·건강 등)를 입력받아 사주 결과를 심층적으로 제공하고 싶다"고 요청 — 먼저 분석을 제공(오행5×MBTI16×관심사를 전부 곱하면 수백 조합이 나와 손으로 쓴 문구로는 감당 안 됨, 조합형 규칙 기반 vs 실제 AI 연동 두 갈래, 후자는 이미 문서화된 "AI 해석 연동" 사람 결정 대기 항목과 동일한 종류의 결정이라는 점을 먼저 짚음), 사용자가 1단계(조합형 규칙 기반)부터 진행하기로 확정. 구현: `lib/features/deep_dive/`에 신규 모듈 추가 — `deep_dive_info.dart`(MBTI 4축 enum + `Mbti.code` 조합, `Interest` enum(연애·재물·직장·건강), `DeepDiveInfo`), `deep_dive_readings.dart`(관심사는 `ohaeng_readings.dart`의 기존 카테고리 재사용 + 직장운만 신규 5줄, MBTI는 오행과 무관한 16유형 독립 코멘트로 조합 폭발 회피), `deep_dive_input_screen.dart`(관심사 다중 선택 칩 기본 전체 선택 + "MBTI를 알고 있어요" 체크로 축 토글 노출 — birth_input의 "시간 모름" 패턴 재사용), `deep_dive_result_screen.dart`(우세 오행 기준 선택 관심사 풀이 + MBTI 코멘트). 리팩터링: `result_screen.dart` 안에 있던 우세 오행 선택 reduce 로직(동률 처리 포함, 이미 테스트로 잠긴 로직)을 `FourPillars.dominantOhaeng` getter로 옮겨 결과 화면과 심층 분석 화면이 동일 로직을 공유하게 함(중복 구현 방지). 라우팅: `AppRoutes.deepDiveInput` 신규 등록, 결과 화면에 "MBTI·관심사로 심층 분석 받기 →" 링크 추가(입력→결과 화면 전환은 데이터 2개를 넘겨야 해서 `result_screen.dart`의 "다시 입력하기"와 같은 방식으로 named route 대신 `MaterialPageRoute` 직접 push). 테스트 20개 추가(콘텐츠 순수 함수 6개 — 16유형 전부 서로 다른 코멘트 검증 포함, 입력 화면 6개, 결과 화면 5개, 라우터 1개, 결과 화면 신규 버튼 네비게이션 1개, `dominantOhaeng` 리팩터링 검증 1개) — 총 133개, `flutter analyze` 클린, `./tool/check_build.sh` 전체 통과(APK 194MB). MBTI·관심사 입력값은 세션 내에서만 유지되고 저장하지 않음(v1 의도적 범위 제한, ⬜ 항목 참고). 실제 AI 연동(2단계)은 여전히 사람 결정 대기.

- **2026-07-05 (오버나이트 루프 재개 후 4회차)**: `meta_line_test.dart`를 다시 보다가, 자정(hour: 0 → "오전 12시")은 이미 경계값 테스트가 있는데 12시간제 변환 공식의 또 다른 경계인 정오(hour: 12)는 한 번도 검증한 적이 없었다는 걸 발견 — `buildMetaLine`의 공식(`hour % 12 == 0 ? 12 : hour % 12`로 "12시" 결정, `hour < 12`로 오전/오후 결정)이 정오에서는 "12 % 12 == 0"이라 자정과 똑같이 "12시"가 나오지만 `hour < 12`가 false가 되어 "오후"로 갈라지는 지점이라, 직접 계산해보기 전엔 자명하지 않았다. birth_input_screen.dart의 `_formattedTime`(picker 버튼 라벨)도 같은 패턴(`hourOfPeriod == 0 ? 12 : hourOfPeriod`)을 쓰고 있어 코드로 정합성을 확인했지만, 이건 State 내부 private getter라 실제 위젯으로 정오를 선택해 검증하려면 Material timePicker 다이얼을 조작해야 해서(복잡도 대비 낮은 가치) 건드리지 않고, 공유되는 순수 함수 `buildMetaLine`(result/report/share 3곳이 재사용) 쪽에만 테스트를 추가했다 — 이 함수 하나만 잠가도 사용자가 보는 모든 화면에 적용됨. 같은 김에 `four_pillars_test.dart`의 시진 경계 테스트도 재검토해, 자시/축시/인시/해시는 있는데 자시의 정반대 위치인 **오시(정오가 낀 경계)**도 빠져있던 걸 발견해 같이 추가(11시/12시 모두 오시인지 확인) — 12시간제 표기의 정오 분기와는 완전히 별개인 오행/지지 계산 공식이 정오를 올바르게 묶는지 별도로 검증. 테스트 2개 추가 — 총 113개, `flutter analyze` 클린, `./tool/check_build.sh` 전체 통과(APK 194MB). Dart MCP는 여전히 승인 대기.
- **2026-07-05 (오버나이트 루프 재개 후 3회차)**: 지금까지 결과/리포트 화면의 명식 표시부만 접근성 관점에서 훑었던 걸 이어서, birth_input 화면을 목업(docs/mockups/01-pastel-cute.html STEP 2)과 다시 대조해봄 — 카피(라벨/문구)는 거의 완벽히 일치했지만, 목업의 양력/음력·성별 토글이 각각 `role="group" aria-label="양력 또는 음력"`/`aria-label="성별 선택"`을 갖고 있는 반면 실제 `PastelToggleRow` 구현은 개별 버튼(예: "여성, 선택됨, 버튼")만 시맨틱스가 있고 그룹 자체를 아우르는 라벨이 없었다는 걸 발견 — 특히 "태어난 날짜" 바로 아래 있는 양력/음력 토글은 전용 `_FieldLabel`조차 없어서(성별은 그나마 위에 "성별" 텍스트가 있음), 스크린 리더가 순서대로 안 읽고 화면을 훑다가 이 버튼으로 곧장 이동하면 "양력/음력"이 뭘 고르는 건지 전혀 알 수 없는 실제 접근성 결함이었다. `PastelToggleRow`에 옵션 파라미터 `semanticLabel`을 추가해 `Semantics(container: true, label: ...)`로 그룹 전체를 감싸되(개별 버튼의 selected 시맨틱스는 그대로 유지되도록 `excludeSemantics`는 안 씀), birth_input의 두 토글에 각각 목업과 똑같은 문구("양력 또는 음력", "성별 선택")를 적용. `semanticLabel`을 안 주면 기존과 완전히 동일하게 동작하도록 옵션으로 설계해 기존 호출부(다른 화면은 없음)에 영향 없음. `find.bySemanticsLabel()`로 그룹 라벨 존재/부재를 검증하는 테스트 2개 추가 — 총 111개, `flutter analyze` 클린, `./tool/check_build.sh` 전체 통과(APK 194MB). Dart MCP는 여전히 승인 대기.
- **2026-07-05 (오버나이트 루프 재개 후 2회차)**: 지난 사이클에서 추가한 결과/리포트 화면 4기둥 시맨틱스가 정말 필요했는지, 그리고 오행 밸런스 바(`_OhaengBarRow`)·카테고리 카드(`_CategoryCard`) 등 다른 정보 표시부도 같은 문제가 있는지 재확인하려고 `tester.binding.pipelineOwner.semanticsOwner!.rootSemanticsNode!.toStringDeep()`로 결과 화면의 시맨틱스 트리 전체를 직접 덤프해봄 — 오행 밸런스 바는 Flutter의 `LinearProgressIndicator`가 이미 `role: progressBar`+`value`+`label("목\n25%")`을 자동으로 제공하고 있었고, 카테고리 카드도 감싸는 Semantics가 없는데도 형제 Text들이 자동으로 하나의 노드("💘\n연애운\n...")로 병합되고 있어 추가 조치가 불필요함을 확인(지난 사이클의 4기둥 카드 수정은 여전히 유효 — 순서를 "값→라벨"에서 "라벨→값"으로 바로잡은 부분이 핵심이었음). 이어서 `flutter test --coverage`로 커버리지를 다시 재보니 `result_screen.dart`가 87.1%(누락 17줄, 전부 `_handleShare` 내부)로 가장 낮았는데, 기존엔 "`SharePlus.instance`가 싱글턴이라 자동 테스트 범위 밖(수동 검증만 가능)"이라고 여러 사이클에 걸쳐 판단해왔던 것을 재검토 — 실제로 위젯 테스트 환경에서 `share_plus`의 플랫폼 채널에는 목(mock) 핸들러가 등록돼 있지 않아 `SharePlus.instance.share()` 호출이 `MissingPluginException`을 던진다는 걸 임시 스크립트로 직접 확인함. 이건 정확히 우리가 대비하려던 "공유 시트 자체 실패" 상황과 동일해서, 실제로 "공유하기" 버튼을 탭해 이미지 캡처(`RenderRepaintBoundary.toImage()`, `tester.runAsync()`로 감쌈)부터 공유 실패 시 스낵바까지 이어지는 실제 사용자 흐름을 처음으로 테스트로 재현·고정함(뷰포트를 세로로 키워야 버튼을 찾을 수 있었음, 기존 CLAUDE.md 함정과 동일한 이유). 이 테스트 하나로 `result_screen.dart` 커버리지가 87.1%→98.5%로 오르고 지금까지 손대지 않고 남겨뒀던 "수동 검증만 가능" 항목 하나가 실제로는 자동 검증 가능했다는 걸 밝힘(남은 미커버 2줄은 이미지 캡처 자체가 실패하는 더 깊은 분기라 트리거가 까다로워 남겨둠 — 위험도 낮은 잔여 항목으로 판단). 테스트 1개 추가 — 총 109개, `flutter analyze` 클린, `./tool/check_build.sh` 전체 통과(APK 194MB). Dart MCP는 여전히 승인 대기.
- **2026-07-05 (오버나이트 루프 재개 후 1회차)**: 사용자 요청으로 `project/` 전체와 루트 운영 문서를 기능 단위 15개 커밋으로 나눠 처음으로 git에 커밋 완료(대화 중 진행, 이번 사이클에서 `PROGRESS.md` 상태에 반영) — 지난 며칠간 "긴급"으로 표시했던 git 미커밋 이슈가 실제로 해소됨. 이어서 이번 자동 사이클에서는 결과 화면(`result_screen.dart`의 `_PillarCard`)과 상세 리포트 화면(`report_screen.dart`의 `_pillarRow`/`_charCell`)의 4기둥 명식 표시를 다시 점검하다가, 지금까지 접근성(시맨틱스) 검증이 버튼/토글류에만 있었고 이 두 화면의 핵심 콘텐츠인 명식 카드/breakdown 표에는 전혀 없었다는 걸 발견 — 시각적으로는 "년주" 아래 "무인"처럼 라벨과 값이 한 카드 안에 묶여 보이지만, `Semantics` 래핑이 없어 스크린 리더는 가지 텍스트 노드를 각각 따로 읽어 어느 기둥의 값인지 맥락 없이 들리는 실제 접근성 결함이었다(결과 화면 카드는 심지어 시각 순서상 값을 라벨보다 먼저 읽게 됨). `Semantics(label:, excludeSemantics: true)`로 각 카드/행을 감싸 "년주 무인", "년주. 천간 무, 오행 토. 지지 인, 오행 목." 같은 하나의 문장으로 병합했는데, 첫 시도에서 `container: true`를 빠뜨려 한 Row/Column 안의 형제 카드·행들의 시맨틱스가 전부 하나로 뭉쳐지는(`"년주 무인\n월주 경신\n..."`) 회귀를 테스트로 바로 잡아내 `container: true`를 추가해 각 카드/행이 독립된 노드가 되도록 수정. 시맨틱스 테스트 4개 추가(결과 화면 2개, 상세 리포트 2개, 시간 모름 케이스 포함) — 총 108개, `flutter analyze` 클린, `./tool/check_build.sh` 전체 통과(APK 194MB). Dart MCP는 여전히 승인 대기.
- **2026-07-05 14:16**: 오버나이트 자동화 루프 종료. 원래 예약 조건이 "2026-07-04 07:30 KST가 되면 이번 실행을 마지막으로 종료"였는데, cron 작업 자체는 그 시각을 넘긴 뒤에도(2026-07-05까지) 계속 30분 간격으로 돌고 있었던 것을 이번 턴에 발견 — 각 사이클이 스스로 시각을 확인해 종료 조건을 판단하는 구조가 아니었던 탓. 실제 시각(2026-07-05 14:16 KST)이 예정 종료 시점을 이미 하루 넘게 지난 것을 확인하고 `CronDelete`로 반복 작업을 즉시 취소함(더 이상 재실행되지 않음). Dart MCP는 이 시점까지도 여전히 사용자 승인 대기 상태(`claude mcp list` 기준) — 승인하려면 사용자가 대화형으로 `claude`를 실행해야 함. 코드/테스트 변경 없음(테스트 104개, `PROJECT_ROUTER.md` 상태 최신).
- **2026-07-05**: Dart MCP는 여전히 승인 대기. 지난 며칠간 4주·오행 분포·퍼센트까지 값으로 잠갔는데, 정작 그 값들로 "어떤 오행이 우세한지" 골라서 사용자가 실제로 읽는 콜아웃 문구·카테고리 풀이(연애·재물·건강·성격) 4장을 결정하는 로직(`result_screen.dart`의 `ohaengCount.entries.reduce((a,b) => a.value >= b.value ? a : b)`) 자체는 여태 한 번도 실제 값으로 검증된 적이 없었다는 걸 발견 — `share_text_test.dart`/`share_card_test.dart`는 dominant를 그냥 '목'으로 하드코딩해서 넘겼을 뿐, 이 reduce 선택 로직 자체를 거친 적이 없었음. 이건 사용자가 실제로 읽는 핵심 콘텐츠를 결정하는 로직이라 지금까지 검증한 것 중 가장 중요할 수 있는 부분. 1998-08-15/14시 조합({목:2,화:0,토:2,금:3,수:1})은 금이 유일한 최댓값이고 목·토가 동률(2)이라 tie-break도 같이 확인되는 좋은 케이스라, 콜아웃 문구("金(금) 기운이 강한 타입이에요...")와 금의 카테고리 풀이 4장이 정확히 나오는지 검증하는 테스트 추가 — 실행해보니 정확히 예상대로 통과(버그 없었음). 총 104개, `flutter analyze` 클린, `./tool/check_build.sh` 전체 통과
- **2026-07-05**: Dart MCP는 여전히 승인 대기. 지난 사이클에 결과 화면 본문의 4기둥 한자·퍼센트를 값으로 검증했던 것과 똑같은 빈틈이 `report_screen.dart`에도 있었다 — 상세 리포트 화면은 명식을 천간/지지로 나눠 더 자세히 보여주는데도(결과 화면은 "무인"처럼 합쳐서 보여줌) 지금까지 "년주"/"월주" 라벨만 확인했을 뿐 실제 글자·오행 라벨은 검증한 적이 없었음. 같은 1998-08-15/14시 조합(무인·경신·갑자·신미)으로 천간/지지 8글자와 각각의 "천간·지지 · 오행" 라벨을 전부 값으로 확인하는 테스트 추가 — 이 과정에서 시주의 천간 신(辛, 금)과 월주의 지지 신(申, 금)이 한글로는 똑같이 "신"이고 심지어 오행까지 둘 다 금이라 "천간 · 금" 문구도 2번 나타난다는 걸 미리 손으로 따져보고 `findsNWidgets(2)`로 정확히 반영 — 실행해보니 손으로 계산한 중복 개수가 전부 맞아떨어짐(버그 아님). 총 103개, `flutter analyze` 클린, `./tool/check_build.sh` 전체 통과
- **2026-07-05**: Dart MCP는 여전히 승인 대기. 지난 사이클에 "계산값 → 공유 텍스트"까지는 정확한 값으로 이어봤는데, 정작 사용자가 제일 먼저 보는 **결과 화면 본문 자체**(4기둥 카드의 한자, 오행 밸런스 바의 %)는 지금까지 "년주"/"오행 밸런스" 같은 라벨 문구만 확인했을 뿐 실제 계산값이 반영됐는지 확인한 적이 없었다는 걸 발견 — `result_screen_test.dart`가 결과 화면을 가장 많이 다루는 파일인데도 이 부분만 비어 있었음. 이미 검증해둔 1998-08-15/14시 조합(무인·경신·갑자·신미, {목:2,화:0,토:2,금:3,수:1}→25%/0%/25%/38%/13%)을 그대로 재사용해 화면 본문에서 정확히 그 한자와 퍼센트가 보이는지 확인하는 테스트 추가 — 공유 카드/텍스트뿐 아니라 실제 화면 본문까지 계산값 검증이 완결됨. 총 102개, `flutter analyze` 클린, `./tool/check_build.sh` 전체 통과
- **2026-07-05**: Dart MCP는 여전히 승인 대기. 지난 사이클에 `ohaengCount`의 정확한 분포({목:2,화:0,토:2,금:3,수:1})를 처음으로 값으로 검증해둔 김에, 그 값이 실제로 사용자에게 보이는 형태(공유 텍스트의 "오행 밸런스: 목 25% · ..." 줄)까지 정확히 이어지는지도 확인한 적이 없다는 걸 발견 — 기존 테스트는 "퍼센트 합이 100 근처"만 확인했을 뿐 각 숫자 자체는 안 봤음. `dart run`으로 Dart의 실제 반올림 동작(`(37.5).round()`가 파이썬의 은행원 반올림이 아니라 38로 올라가는 것, `.5`는 항상 양수 쪽으로 올림)을 먼저 확인한 뒤, 1998-08-15/14시 조합에서 정확히 "목 25% · 화 0% · 토 25% · 금 38%(3/8=37.5→38) · 수 13%(1/8=12.5→13)"가 나오는지 문자열 그대로 비교하는 테스트 추가 — 총 101개, `flutter analyze` 클린, `./tool/check_build.sh` 전체 통과. 계산 로직 관련 검증(4주 값·오행 분포·공유 텍스트 반영)은 이제 충분히 두터워진 것 같아 다음 사이클엔 다른 영역을 살펴볼 것
- **2026-07-05**: Dart MCP는 여전히 승인 대기. 4주(년/월/일/시)를 전부 값으로 잠근 김에 `ohaengCount`(오행 집계)를 다시 보니, 지금까지는 총 개수(6개/8개)만 확인했을 뿐 실제 분포(목/화/토/금/수 각각 몇 개)가 8글자와 정확히 일치하는지는 한 번도 검증한 적이 없었다는 걸 발견 — 총합만 맞고 분포가 틀리는(한 글자를 두 번 세고 다른 글자를 빠뜨리는) 버그는 총합 검증만으로는 절대 못 잡는데, 이 값이 바로 결과 화면 오행 밸런스 바 차트·공유 텍스트에 그대로 노출되는 값이라 실질적 위험이 있었음. 기존 테스트에서 이미 쓰던 1998-08-15/14시 조합의 4주(무인·경신·갑자·신미)를 파이썬으로 미리 계산해 기대 분포({목:2, 화:0, 토:2, 금:3, 수:1})를 구하고, 실제 코드 결과와 정확히 일치하는지 확인하는 테스트 추가 — 버그는 아니었지만 실사용자가 보는 숫자를 직접 검증하는 첫 테스트. 총 100개, `flutter analyze` 클린, `./tool/check_build.sh` 전체 통과
- **2026-07-05**: Dart MCP는 여전히 승인 대기. 지난 사이클에 시주(時柱) 경계값·입춘 경계를 테스트로 잠갔던 김에 `four_pillars_test.dart` 전체를 다시 훑어보니, 년주·일주·시주는 각각 검증이 있는데 **월주(月柱)만 실제 계산값을 직접 확인하는 테스트가 하나도 없었다**는 걸 발견 — 4주 중 유일하게 한 번도 값 자체를 테스트받지 않은 기둥이었음. 고전 공식(오호둔년기월법 — "갑기지년병작수, 을경지세무위두"로 시작해 인월부터 월지가 바뀔 때마다 월간이 하나씩 밀리는 규칙)을 파이썬으로 미리 계산해보고, 그 값과 실제 `calculateFourPillars()` 결과가 일치하는지 3개 케이스로 확인: 갑자년(1984) 인월(2월)=병인, 을축년(1985) 인월=무인(년간이 바뀌면 인월의 월간도 규칙대로 바뀌는지), 갑자년 신월(8월)=임신(인월이 아닌 다른 달에서도 공식이 맞는지). 셋 다 실제 코드 결과와 정확히 일치 — 버그는 아니었지만 사주 계산의 네 기둥 중 유일하게 미검증 상태였던 걸 닫음. 테스트 3개 추가 — 총 99개, `flutter analyze` 클린, `./tool/check_build.sh` 전체 통과
- **2026-07-05**: Dart MCP는 여전히 승인 대기. `four_pillars_test.dart`/`ganzhi_test.dart`를 다시 보다가, 시주(時柱) 계산의 실제 경계값(전통 시진 경계 — 23시/0시가 같은 자시, 1시/2시가 같은 축시 등)과 입춘(2/4) 경계 자체(2/3 vs 2/4)를 지금까지 한 번도 직접 테스트한 적이 없었다는 걸 발견 — 코드(`((hour+1)~/2)%12`, `date.day < 4`)는 파이썬으로 직접 계산해 미리 검증한 대로 정확했지만(버그는 아니었음), 이런 경계 조건은 향후 계산 로직을 건드릴 때 가장 먼저 깨지기 쉬운 지점이라 명시적인 회귀 테스트로 남겨두는 게 값어치가 있다고 판단. 시주 경계 4개(23/0시=자시, 1/2시=축시, 3시=인시, 21/22시=해시) + 입춘 경계(2/3=전년도, 2/4=해당연도) 테스트 6개 추가 — 총 96개, `flutter analyze` 클린, `./tool/check_build.sh` 전체 통과
- **2026-07-05**: Dart MCP는 여전히 승인 대기(사용자 상호작용 불가한 사이클이라 이번에도 못 씀). 지난 사이클의 "실측으로 극단값 검증" 흐름을 이어서, birth_input의 datePicker/timePicker가 지금까지 "확인"으로 확정하는 경로만 테스트돼 있고 실제 사용자가 자주 하는 "취소"(picker가 null 반환) 경로는 한 번도 검증한 적이 없었다는 걸 발견 — 코드 자체(`if (picked != null) setState(...)`)는 이미 올바르게 방어돼 있었지만 실제로 취소를 눌러도 원래 값이 안 깨지는지 테스트로 확인된 적은 없었음. datePicker/timePicker 각각 "취소" 테스트 2개 추가 — 둘 다 그대로 통과(회귀는 아니었지만 실제로 검증된 적 없던 코드 경로를 확인해 안전망으로 남김). 총 91개, `flutter analyze` 클린, `./tool/check_build.sh` 전체 통과
- **2026-07-05**: Dart MCP는 여전히 승인 대기라(이번 사이클도 사용자 상호작용 불가) 기존 CLI로 계속 진행. 최근 두 사이클이 "실제 라우트 전체 여정" 테스트를 계속 확장하던 흐름에서, 이번엔 그 값들의 극단(이름 20자·출생지 30자 — birth_input의 실제 입력 제한 최대치)을 공유 카드(`share_card.dart`)에 넣어보는 실측 테스트를 만들어봤다가, `RenderFlex overflowed by 17 pixels` 오류를 실제로 재현함 — `share_card.dart`가 폭 360×높이 640 고정 레이아웃인데, 이름/메타 라인 `Text`에 줄 수 제한이 없어서 긴 이름+출생지 조합이면 실제로 카드 높이를 넘길 수 있었던 진짜 결함이었다. 이 카드는 `RepaintBoundary.toImage()`로 그대로 캡처해서 공유하는 이미지라, 실사용자가 이름/출생지를 길게 입력하면 노란/검정 줄무늬가 찍히거나(디버그) 내용이 잘린(릴리즈) 공유 이미지가 나갈 뻔한 것. 이름 `Text`는 `maxLines: 2`, 메타 라인은 `maxLines: 1`로 제한하고 둘 다 `overflow: TextOverflow.ellipsis`를 추가해 고정 높이를 절대 넘지 않도록 수정 — 다시 같은 극단값으로 테스트해 overflow가 사라진 것 확인. `share_card_test.dart`에 이 극단값 조합을 검증하는 회귀 테스트 1개로 정식 추가 — 총 89개, `flutter analyze` 클린, `./tool/check_build.sh` 전체 통과
- **2026-07-05 (사용자 요청으로 Dart MCP 서버 추가)**: 사용자 요청으로 Dart SDK 내장 `dart mcp-server`를 프로젝트 스코프 MCP 서버로 등록(`claude mcp add -s project dart -- dart mcp-server`, 저장소 루트 `.mcp.json`에 기록됨). 새로 추가된 프로젝트 MCP 서버는 보안상 사용자 승인이 필요한데(`⏸ Pending approval`), 이 오버나이트 사이클은 사용자가 상호작용할 수 없는 자동 실행이라 승인이 안 된 상태 그대로였음 — 이번 사이클은 이 서버 없이 기존 CLI 절차(flutter analyze/test/build)로 계속 진행. **사용자가 다음에 `claude`를 대화형으로 실행해 승인하면, 그다음 사이클부터 Dart MCP 도구를 실제로 써볼 수 있을 것**. 이어서 지난 사이클 방식(실제 라우트 전체 여정 테스트)을 한 단계 더 확장 — 저장된 값으로 시작해 "다시 입력하기"로 실제 재입력 화면까지 갔다가 새 값을 제출하면 이전 값이 완전히 새 값으로 교체되는지 실제 라우트로 검증하는 테스트 추가(스텁 기반 result_screen_test.dart의 "다시 입력하기" 테스트와 달리 처음부터 끝까지 실제 라우트/실제 재제출까지 확인). 처음엔 테스트에서 직접 만든 `BirthInfo`에 `gender`를 안 넣어서(실제 UI 제출과 달리 기본값이 없음) 메타 라인에 "· 여성"이 없다는 걸 놓쳐 실패했는데 바로 수정 — 테스트 1개 추가, 총 88개, `flutter analyze` 클린, `./tool/check_build.sh` 전체 통과
- **2026-07-05**: 지난 사이클에 추가한 전체 여정 테스트가 결과 화면에서 멈춰 있던 걸 발견 — 마지막 구간(결과 → "상세 리포트 보기" → 실제 ReportScreen)만 실제 라우트로 안 이어져 있었음. `ReportScreen`은 반복 애니메이션이 없어 이 구간은 다시 `pumpAndSettle()`을 안전하게 쓸 수 있어서, 같은 테스트에 이어붙여 상세 리포트 화면까지 실제로 도달해 헤더("회원님의 상세 리포트")와 메타 라인이 입력한 생년월일시 그대로인지 확인하도록 확장(테스트 개수는 그대로 87개, 새 테스트를 추가한 게 아니라 기존 여정 테스트를 한 구간 더 늘림). `flutter analyze` 클린, `./tool/check_build.sh` 전체 통과
- **2026-07-05**: 세 번 연속 "새 결함 없음" 사이클 뒤라 다른 종류의 가치를 찾아봄 — 지금까지의 테스트가 각 화면을 스텁 라우트나 개별 위젯 생성으로만 검증했을 뿐, `CosmosSajuApp`의 진짜 라우트 배선(`AppRoutes.routes`)을 그대로 타고 온보딩→입력→계산 중→결과까지 한 번에 이어서 통과한 적이 없었다는 빈틈을 발견 — 이러면 각 구간은 개별적으로 다 통과해도 라우트 배선 자체가 잘못되거나 화면 간 인자 전달이 깨지는 회귀는 못 잡는다. `test/widget_test.dart`에 실제 라우트로 전 구간을 잇는 테스트를 추가 — "시작하기" 탭 → 기본값 그대로 제출 → 계산 중 화면 확인 → 3초 흘려보내기 → 결과 화면에 입력했던 날짜/시간/양력이 실제로 반영됐는지 확인. 계산 중 화면부터는 궤도 애니메이션이 무한 반복이라 `pumpAndSettle()`을 쓸 수 없어 수동 `pump()`로 전환(기존에 이미 알고 있던 함정). 처음엔 메타 라인에 "· 여성"이 빠진 문자열을 기대해서 실패했는데, birth_input의 성별 기본값(여성)이 실제로 제출된다는 걸 깜빡했던 것 — 바로 수정. 테스트 1개 추가 — 총 87개, `flutter analyze` 클린, `./tool/check_build.sh` 전체 통과
- **2026-07-05**: iCloud 동기화발 중복 파일이 새로 생겼는지 재확인(없음), 오늘로 세 번째 "새 결함을 못 찾은" 사이클이라 다른 각도로 `analysis_options.yaml`에 `flutter_lints` 기본보다 훨씬 엄격한 규칙(discarded_futures, avoid_redundant_argument_values, cancel_subscriptions 등)을 임시로 켜서 `flutter analyze`를 돌려봄 — 실제 결함은 없었고, 나온 19개 전부 스타일 취향 수준(테스트에서 기본값과 같은 인자를 명시한 것, 네비게이션/다이얼로그 Future를 async 없이 fire-and-forget으로 호출하는 것 — 이 코드베이스 전체에 이미 일관되게 쓰이는 패턴이라 지금 와서 바꾸면 오히려 불필요한 대규모 변경이 됨)이라 실제로 고칠 가치가 없다고 판단, `analysis_options.yaml`은 원래대로 되돌림(실험이었을 뿐 실제 변경 아님). 코드 변경 없음 — `flutter analyze` 클린, 86개 테스트 통과, `./tool/check_build.sh` 전체 통과. **이 시점에서 종합 평가**: PROJECT_ROUTER.md의 미착수(⚪) 항목은 이제 없고, 남은 🟡 2건(계산 정밀화, accent 버튼 대비)은 사람 결정이 필요해서 못 건드리는 것이지 AI가 할 수 있는 일이 남아서가 아니며, ⬜ 2건(AI 해석 연동, 결제)은 명백히 큰 스코프라 보류 중 — 지난 여러 사이클 동안 code review, 커버리지 분석, 목업 대조, initState 안티패턴 전수조사, 컨트롤러 dispose 감사, 의존성 최신성, 카피 교정, 엄격 린트 실험까지 다양한 각도로 훑었지만 이번엔 새 소재가 안 나옴 — 현재 스코프(사람 결정 대기 항목 제외) 안에서는 실질적으로 성숙한 상태로 보임
- **2026-07-05**: 지난 사이클에서 겪은 "`initState()` 안 `MediaQuery.of(context)` 예외"가 다른 곳에도 숨어있을지 확인하려고 `lib/` 전체에서 `initState`를 오버라이드하는 위젯을 찾아봤는데 `calculating_screen.dart` 하나뿐이라(이미 고침) 추가로 고칠 곳은 없었음. 같은 김에 인접한 것들도 점검: 컨트롤러(`AnimationController`/`TextEditingController`) dispose 누락 여부(전부 정상 해제됨), `flutter pub outdated`로 직접 의존성 최신 여부(direct/dev dependencies 전부 최신, transitive만 사소하게 뒤처짐 — 손댈 필요 없음), `ohaeng_readings.dart`의 오행별 카피 5종을 다시 정독(오탈자·중복 없음, 이미 깔끔함). 전부 정상이라 이번 사이클은 코드 변경 없이 검증만 진행 — `flutter analyze` 클린, 86개 테스트 그대로 통과, `./tool/check_build.sh` 전체 통과
- **2026-07-05**: 지난 사이클에 로딩 문구 순환에만 reduce-motion(동작 줄이기) 처리를 넣고, 훨씬 더 눈에 띄는 궤도 회전 애니메이션(달 주위를 도는 🌿⭐💫)은 그대로 계속 도는 채로 남겨뒀던 일관성 문제를 발견해 마저 고침 — `MediaQuery.of(context).disableAnimations`가 켜져 있으면 `_orbitController.repeat()`를 아예 안 부르도록(정적으로 멈춰 보이게) 수정. 처음엔 이 체크를 `initState()`에 그대로 넣었는데, 실행해보니 "dependOnInheritedWidgetOfExactType<MediaQuery>() ... was called before initState() completed" 예외로 이 화면을 쓰는 기존 테스트가 전부 깨지는 걸 실제로 겪음 — `initState()` 안에서는 `MediaQuery.of(context)`를 쓸 수 없다는 걸 몰랐던 것. `didChangeDependencies()`로 옮기고 `bool` 플래그로 한 번만 실행되게 가드해서 해결(향후 재발 방지로 CLAUDE.md에도 기록). reduce-motion 시 궤도가 실제로 안 움직이는지 검증하는 테스트 1개 추가(1초 전후 이모지 위치가 동일한지 비교) — 총 86개, `flutter analyze` 클린, `./tool/check_build.sh` 전체 통과
- **2026-07-05**: 지난 사이클 방식(목업 대조)을 이어서 목업의 나머지 부분(STEP 4 결과 화면, CSS 전체, 스크립트 섹션)까지 훑었지만 이번엔 실제로 고칠 만한 새 항목을 못 찾음 — 목업 스크립트에 있던 인터랙션은 pill 토글(이미 `PastelToggleRow`로 구현됨)과 로딩 문구 순환(지난 사이클에 구현함) 둘뿐이었고 그 외엔 없었음. 유일하게 눈에 띈 차이는 온보딩의 마스코트(목업은 방울 모양 블롭 + 그라디언트 + 눈 두 개 + 좌우로 흔들리는 반짝임 애니메이션인데, 구현은 단순 원+아이콘)인데, 실제로 구현을 시도해보니 반복 애니메이션(`AnimationController.repeat()`)이 추가되면 기존 온보딩 테스트들이 쓰는 `pumpAndSettle()`이(계산 중 화면에서 이미 겪었던 것과 같은 이유로) 절대 안 끝나서, 네비게이션/다이얼로그 테스트를 전부 수동 `pump()` 방식으로 다시 짜야 하는 부담이 생김 — 순수 장식(기능·접근성 영향 없음)에 비해 리스크가 안 맞다고 판단해 착수하지 않고 되돌림(파일 변경 없음). `flutter analyze` 클린, 85개 테스트 그대로 통과, `./tool/check_build.sh` 전체 통과 — 코드 변경 없는 검증 사이클
- **2026-07-05**: 지난 사이클에서 목업 대조로 온보딩 화면 갭을 찾았던 방식을 이어가서, 목업 자체의 footer 안내문("다음으로 다듬을 지점")에 적힌 4가지 항목을 하나씩 다시 점검함 — ①시간 모름 시 시주 카드(이미 구현·테스트됨), ③공유 이미지 9:16 레이아웃(이미 구현됨), ④무료/유료 경계선(이미 ⬜ 항목으로 분리해둔 결제 로직과 동일, 큰 스코프라 보류 유지)은 전부 처리돼 있었지만, ②"로딩 문구 3종 순환 타이밍"만 실제로 구현이 안 돼 있었음 — `calculating_screen.dart`는 지금까지 "사주팔자를 계산하고 있어요..." 한 문구만 3초 내내 고정으로 보여주고 있었고, 목업 JS(`msgs` 배열 + 1800ms `setInterval`)가 정의한 나머지 두 문구("오행 기운을 분석하는 중...", "당신만의 이야기를 준비하고 있어요...")는 구현에 전혀 반영이 안 됐던 것. `Timer.periodic(1800ms)`으로 문구를 순환시키되, 목업이 `prefers-reduced-motion`을 존중하는 것과 동일하게 `MediaQuery.of(context).disableAnimations`일 때는 순환을 건너뛰도록 구현. 실제 네비게이션까지 3초뿐이라 1.8초 간격 순환으로는 3개 중 2개 문구만 보게 되는 게 정상 동작(목업은 데모 페이지라 순환이 무한 반복되지만, 우리 앱은 3초 뒤 실제로 다음 화면으로 넘어가므로 자연스러운 차이). 로딩 문구 순환을 검증하는 테스트 1개 추가(1.8초 뒤 두 번째 문구로 바뀌는지 확인) — 총 85개, `flutter analyze` 클린, `./tool/check_build.sh` 전체 통과
- **2026-07-05**: 지난 사이클엔 코드 리뷰로 새 소재를 못 찾았어서, 이번엔 `docs/mockups/01-pastel-cute.html`을 다시 처음부터 훑어 실제 구현과 대조하는 방식으로 접근 — STEP 1(온보딩) 섹션에서 실제 카피가 완전히 다르다는 걸 발견했다. 목업은 "사주랑"을 작은 워드마크로 두고 그 아래 진짜 헤드라인("내 안의 오행, 3분이면 알 수 있어요")과 설명 문구를 배치하는데, 구현은 "사주랑"을 큰 헤드라인처럼 쓰고 목업에 없는 문구("생년월일시로 알아보는...")를 쓰고 있었음. 또 목업엔 "시작하기" 아래 "어떻게 계산되나요?"라는 보조 링크가 있는데 구현엔 아예 없었음 — 온보딩 화면의 헤드라인/설명 문구를 목업과 맞추고, "어떻게 계산되나요?" 텍스트 버튼을 추가해 탭하면 `report_screen.dart`의 기존 정확도 안내 문구를 재사용한 짧은 설명 다이얼로그(60갑자 변환 방식 + 절기 근사 한계)를 띄우게 구현. 워드마크를 다시 그리다가 목업 CSS가 이 텍스트에 `--app-accent`를 그대로 쓴다는 걸 확인했는데, 이게 지난 사이클에 고친 오행색과 똑같이 WCAG AA 미달(2.64:1)이라는 걸 바로 알아채고 그 자리에서 같은 기법(ink 쪽으로 48% 블렌드)으로 `AppColors.accentText`를 새로 추가해 대신 사용 — 하마터면 지난 사이클에 고친 것과 똑같은 종류의 결함을 새로 만들 뻔한 걸 스스로 잡음. 전용 테스트 파일이 없던 온보딩 화면에 `test/features/onboarding_screen_test.dart`를 새로 만들어 헤드라인/워드마크 표시, "시작하기" 이동, "어떻게 계산되나요?" 다이얼로그 열기/닫기까지 검증(3개) + `accentText` 대비 회귀 테스트 1개 추가 — 총 84개, `flutter analyze` 클린, `./tool/check_build.sh` 전체 통과
- **2026-07-05**: 이번 사이클은 새 코드 변경 없이 검증/점검만 진행 — `lib/` 전체 19개 파일을 순서대로 다시 훑어봤고(대부분 지난 여러 사이클에서 이미 리뷰함), `flutter test --coverage`로 커버리지도 재확인함. 눈에 띈 두 가지를 확인했지만 둘 다 실제 결함은 아니었음: ① `onboarding_screen.dart`는 전용 테스트 파일(`onboarding_screen_test.dart`)이 없어 처음엔 빈틈처럼 보였지만, `widget_test.dart`의 앱 레벨 테스트만으로 이미 13/13줄(100%) 커버되고 있어 실제로는 문제 없음(파일명 대칭을 맞추려고 중복 테스트 파일을 새로 만들진 않음). ② `app_theme.dart`의 `ElevatedButtonThemeData`가 `disabledBackgroundColor`/`disabledForegroundColor`를 지정하지 않아 버튼이 비활성화되면 활성 상태와 똑같은 진한 핑크 배경으로 보일 수 있는 이론적 위험을 발견했지만, 앱 전체에서 실제로 `ElevatedButton`을 쓰는 3곳(birth_input 제출, 결과 화면 공유, 온보딩 시작하기) 전부 `onPressed`가 항상 non-null이라 지금은 재현되지 않는 잠재 이슈일 뿐이라 손대지 않음(존재하지 않는 시나리오를 위한 선제적 변경은 지양). 커버리지 수치는 기존과 동일하게 알려진 3곳만 남음(`result_screen.dart`의 `SharePlus` 실패 경로, `pastel_toggle_row.dart`의 시맨틱스 전용 tap 1줄, `main.dart` 부트스트랩) — 전부 이전 사이클에서 이미 검토·수용한 항목. `flutter analyze` 클린, 80개 테스트 통과, `./tool/check_build.sh` 전체 통과
- **2026-07-05**: `result_screen.dart`의 `_handleShare()`를 다시 살펴보다가, 공유 카드 이미지 캡처 실패는 이미 try/catch로 텍스트 전용 폴백을 하고 있지만 정작 마지막 단계인 `SharePlus.instance.share()` 호출 자체는 아무 예외 처리 없이 그대로 호출되고 있는 걸 발견 — 플랫폼 채널 오류 등으로 이게 실패하면 "공유하기" 버튼을 눌러도 아무 반응이 없는 것처럼 보이고 사용자에게 어떤 피드백도 없는 실제 UX 결함이었음. try/catch로 감싸고 실패 시 스낵바("공유하는 중 문제가 발생했어요...")를 띄우도록 수정 — 저장소 read/write 실패를 이미 같은 방식으로 방어하고 있던 기존 패턴과 통일. 구현 중 `context.mounted`로 async-gap 가드를 했더니 `use_build_context_synchronously` 린트가 걸려서(State 메서드 안에서는 `context.mounted`보다 `mounted`를 쓰라는 제안), `mounted`로 바꿔 analyze를 다시 클린하게 만듦. `SharePlus.instance`가 싱글턴이라 이 실패 경로 자체는 기존 판단대로 자동 테스트 범위 밖(수동 검증만 가능) — 테스트 개수는 그대로 80개, `flutter analyze` 클린, `./tool/check_build.sh` 전체 통과
- **2026-07-05**: 코드 정리 — `result_screen.dart`의 `_ohaengCallout` 맵(우세 오행별 한자/설명 문구)이 오행색(`AppColors.wood` 등)을 세 번째 튜플 원소로 갖고 있었는데, 실제로는 `callout.$3`을 참조하는 코드가 어디에도 없어(캡션/공유 텍스트는 전부 `.$1`/`.$2`만 씀) 이 색상값이 `result_screen.dart` → `_handleShare` → `share_text.dart`(`buildShareText`)까지 세 파일과 6개 테스트 케이스에 걸쳐 아무 의미 없이 전달만 되고 있던 죽은 데이터였던 걸 발견 — 튜플 타입을 `(String, String, Color)`에서 `(String, String)`으로 줄여 제거. 그 결과 `share_text.dart`에서 `Color` 타입이 더 이상 필요 없어져 `package:flutter/material.dart` import 자체도 제거됐고, `share_text_test.dart`의 6개 테스트에서도 항상 `Colors.green`을 의미 없이 넘기고 있던 걸 지우면서 같은 이유로 `material.dart` import를 제거. 순수 리팩터링(동작 변화 없음) — 80개 테스트 그대로 통과, `flutter analyze` 클린, `./tool/check_build.sh` 전체 통과(APK 178MB)
- **2026-07-05**: git 미커밋 이슈(사람 결정 대기 항목)에 대비해 실제로 초기 커밋 시 스테이징될 파일을 `git ls-files --others --exclude-standard`로 미리 점검 — `android/ios/macos` 각각에 이미 있는 플랫폼별 `.gitignore`가 `local.properties`/`Pods/`/`Generated.xcconfig`/keystore 등을 정상적으로 제외하고 있어 커밋 대상 자체는 깨끗함을 확인(처음엔 이게 빠져 있는 줄 알고 루트 `project/.gitignore`에 중복 규칙을 추가했다가, 재확인 후 불필요한 중복이라는 걸 깨닫고 되돌림 — 스스로 발견한 오판을 바로 수정). 대신 iCloud 동기화 충돌로 생긴 " 2"~" 6" 중복 파일 15개(기존 gitignore 패턴이 정식 파일명만 매칭해서 안 걸리는 것들)를 새로 발견해 삭제 — 타임스탬프가 최근(오늘 새벽)이라 iCloud 동기화 문제가 아직 해결되지 않고 계속 파일을 오염시키고 있다는 걸 재확인. 코드 변경은 없었지만(정리 작업뿐) 강화된 표준 절차(`./tool/check_build.sh`)로 전체 재검증 — 80개 테스트, `flutter analyze` 클린, APK 빌드 성공
- **2026-07-05**: 사용자 요청으로 빌드 체크 절차를 강화 — 기존엔 flutter analyze/test/build apk만 확인했는데, (1) 새 패키지를 추가 안 한 사이클에도 매번 `flutter pub get`을 먼저 실행해 의존성/락파일 드리프트를 조기에 잡고, (2) `flutter build apk --debug`가 exit 0이어도 `app-debug.apk` 파일이 실제로 존재하고 1MB 이상인지까지 확인하도록 CLAUDE.md와 오버나이트 루프 프롬프트(cron `6e7737fe`, 기존 `9b904245` 대체)를 갱신. 이 환경엔 연결된 Android 에뮬레이터/기기가 없다는 것도 `flutter devices`로 직접 확인해 문서화(설치 후 실행까지의 런타임 검증은 불가, 산출물 검증까지만 가능). 이 표준 절차를 매번 프롬프트로 반복 서술하지 않고 한 번에 실행할 수 있도록 `project/tool/check_build.sh`를 작성 — 파일 누락/삭제 시 실제로 실패(exit 1)를 반환하는지 직접 시뮬레이션해 검증한 뒤, 전체 절차를 실행해 통과 확인(80개 테스트, apk 155MB). `flutter analyze` 클린
- **2026-07-05**: 여러 사이클째 "사람 결정 대기"로 미뤄뒀던 오행색 텍스트 WCAG 대비 미달 문제를 실제로 계산해보니, 기존에 알려진 wood/fire/earth뿐 아니라 metal/water도 bg/bgCard 위에서 4.5:1 미달(3.4~3.9)이었고, 실제 렌더링되는 `ohaengSoftColors` 배경 위에서는 다섯 색 전부 더 크게 미달(3.8~4.1)이라는 걸 python으로 정밀 계산해 확인. PROGRESS.md가 이미 제시해뒀던 옵션 ②("텍스트 전용 진한 변형색 추가")를 채택 — `inkSoft`를 고칠 때 썼던 것과 같은 "ink 쪽으로 블렌드" 기법으로 `ohaengTextColors`(34~49% 블렌드, 세 가지 배경 조건 전부 4.5:1 이상되게 계산)를 추가하고, 텍스트로만 쓰이던 7곳(`share_card.dart` 2곳, `report_screen.dart` 3곳, `result_screen.dart` 2곳)을 여기로 교체. 배경/아이콘/바 차트로 쓰이는 원래 `ohaengColors`/`ohaengSoftColors`는 전혀 손대지 않아 목업이 고정한 브랜드 팔레트 자체는 그대로 유지됨(순수 추가라 되돌리기도 쉬움). `accent` 배경 위 흰 CTA 텍스트(대비 2.72:1)는 브랜드 버튼 자체의 색을 바꿔야 해서 이번에도 사람 결정 대기로 남겨둠 — WCAG 대비 테스트 파일에 다섯 색 전부 세 배경 조건 검증하는 테스트 추가(1개, 배경별로 5×3=15 assertion) — 총 80개, `flutter analyze` 클린, Android 디버그 빌드 성공
- **2026-07-05**: 상세 리포트 화면(`report_screen.dart`)이 결과 화면과 달리 이름/생년월일 등 아무 문맥 정보 없이 바로 "명식 한 글자씩 뜯어보기"로 시작해, 리포트를 보다가 이게 누구의 사주인지 알 수 없다는 일관성 문제를 발견 — 목업엔 리포트 화면 상세 디자인이 없어(문서에도 "무료/유료 경계선 구체화"가 "다음에 다듬을 지점"으로만 적혀 있음) 직접 설계 판단이 필요했음. `result_screen.dart`에 있던 메타 라인 조립 로직(`_metaLine`, private 메서드)을 `share_text.dart`를 만들 때와 같은 방식으로 `meta_line.dart`(`buildMetaLine`, 순수 함수)로 추출해 두 화면이 같은 문구를 재사용하게 하고, report_screen 상단에 결과 화면과 같은 스타일의 "{이름}의 상세 리포트" 헤더 + 메타 라인을 추가. 구현 중 처음엔 `'$displayName님의 상세 리포트'`로 썼다가, 이름이 없을 때 이미 "님"이 포함된 기본값 "회원님"과 합쳐져 "회원님님의"라는 이중 존칭 실수를 스스로 발견해 결과 화면과 동일하게 `'$displayName의 상세 리포트'`(님 없이)로 수정. 테스트 5개 추가(`buildMetaLine` 단위 테스트 3개 + report_screen 헤더 위젯 테스트 2개) — 총 79개, `flutter analyze` 클린, Android 디버그 빌드 성공
- **2026-07-05**: birth_input의 이름/출생지 `TextField`가 `hintText`만 쓰고 있어서, 실제로 위젯 테스트(`tester.getSemantics`)로 확인해보니 필드가 비어 있을 때 스크린 리더가 그 예시 문구("예: 민지")를 필드의 이름(semantics label)으로 그대로 읽어버리고, 정작 위에 시각적으로만 보이는 "이름 (선택)" `_FieldLabel`은 이 TextField와 시맨틱으로 전혀 연결돼 있지 않다는 걸 실측으로 확인 — 스크린 리더 사용자는 이 필드가 뭘 입력하는 칸인지 알 수 없는 실제 접근성 결함이었음. `excludeSemantics` 없는 `Semantics(label: '이름 (선택)')`/`Semantics(label: '태어난 곳 (선택)')`로 필드를 감싸면 시각적 변화 없이 "이름 (선택)\n예: 민지"처럼 병합되는 걸 임시 테스트로 먼저 검증한 뒤 적용(버튼류에 쓰던 `excludeSemantics: true` 패턴과 달리, 텍스트 필드는 감싸기만 해도 자동 병합되는 걸 이번에 확인 — 향후 참고). 테스트 1개 추가(병합된 label에 필드 용도와 hint가 모두 포함되는지 검증) — 총 74개, `flutter analyze` 클린, Android 디버그 빌드 성공
- **2026-07-05**: 지난 사이클에서 PROJECT_ROUTER.md에 "앱 아이콘" 행을 추가하며 실수로 표 중간에 빈 줄을 넣어 마크다운 표가 깨져 있던 걸(표가 두 조각으로 쪼개짐) 발견해 수정. 이어서 코드 리뷰로 birth_input의 이름/출생지 `TextField`에 글자 수 제한이 없다는 걸 발견 — `share_card.dart`가 폭 360 고정 레이아웃이라 아주 긴 이름을 넣으면 헤더 텍스트가 카드 세로 길이를 넘겨 겹치거나 잘릴 수 있는 실제 결함이었음. 이름 20자/출생지 30자로 `maxLength` 제한을 추가(카운터 텍스트는 파스텔 큐트 톤과 안 어울려 `counterText: ''`로 숨김). 테스트 1개 추가(25자 입력 시 20자까지만 반영되는지 검증) — 총 73개, `flutter analyze` 클린, Android 디버그 빌드 성공
- **2026-07-04**: "앱 아이콘을 파스텔 큐트 톤으로 교체" 항목을 이전 사이클엔 "SVG→PNG 변환 도구가 없어 보류"로 남겨뒀었는데, `share_card.dart`가 이미 쓰던 `RenderRepaintBoundary.toImage()` 캡처 기법을 그대로 재사용하면 변환 도구 없이도 Flutter 위젯을 직접 그려 PNG를 뽑을 수 있다는 걸 깨닫고 착수. 처음엔 `Icon(Icons.auto_awesome_rounded)`로 시도했으나 `flutter test`가 결정론적 렌더링을 위해 모든 폰트 글리프(아이콘 폰트 포함)를 빈 사각형으로 치환한다는 걸 실제 캡처 결과로 확인 — `CustomPainter`로 순수 도형만 그리는 방식(초승달 + 오행 5색 알갱이, `calculating_screen.dart`의 궤도 모티프 재구성)으로 전환해 해결. 첫 시도에서 `boundary.toImage()`/`toByteData()`를 testWidgets의 기본 FakeAsync 존 안에서 직접 await해 10분 타임아웃으로 행(hang)이 발생했는데, `tester.runAsync()`로 감싸야 한다는 걸 발견해 수정(신규 트러블슈팅 팁, CLAUDE.md에도 기록 필요). `flutter_launcher_icons`(dev_dependency)로 Android/iOS/macOS 아이콘 일괄 생성, `remove_alpha_ios: true`로 앱스토어 알파채널 경고까지 해소. `flutter analyze`/`flutter test`(72개, 회귀 없음)/Android 디버그 빌드 모두 통과. 같은 사이클에서 저장소 루트 `git status`를 처음 확인하다 `project/` 전체가 지금까지 한 번도 커밋된 적 없다는 것과 iCloud 동기화발로 보이는 중복 파일 3개(`.flutter-plugins-dependencies 2`/`3`, `flutter_lldbinit 2`, 전부 자동 생성 빌드 메타데이터라 안전하게 삭제)를 발견 — 위 "사람 결정 대기" 항목에 긴급으로 기록함
- **2026-07-04**: birth_input의 "성별" 토글이 `birthPlace`와 똑같은 패턴의 버그를 가지고 있던 것을 발견 — 화면엔 토글 상태(`_gender`)가 있었지만 실제로는 `BirthInfo`에 전혀 전달되지 않고 버려지고 있었음. 공개 `enum Gender { female, male }`을 `BirthInfo`에 추가하고 `BirthInfoStore` 저장/조회, `result_screen`의 메타 라인과 `share_text.dart`의 공유 텍스트(출생지보다 앞에 표시)까지 실제로 연결. 테스트 6개 추가(저장소 왕복 2개, birth_input 제출 2개, 결과 화면 1개, 공유 텍스트 1개) — 총 72개, `flutter analyze` 클린, Android 디버그 빌드 성공. iOS 시뮬레이터 빌드는 기존에 원인이 확인된 iCloud 동기화발 CodeSign 오류로 여전히 실패(신규 이슈 아님, 사용자의 iCloud 동기화 해제 확인 대기 중)
- **2026-07-04**: birth_input의 "태어난 곳 (선택)" 필드가 입력만 받고 실제로는 어디에도 쓰이지 않고 완전히 버려지는 실제 문제를 발견 — `BirthInfo`에 `birthPlace` 필드 추가(`name`과 동일한 패턴), `BirthInfoStore` 저장/조회에 포함, `result_screen`의 메타 라인과 `share_text.dart`의 공유 텍스트에 실제로 표시되도록 연결(계산에는 아직 안 씀 — 향후 진태양시 보정 등에 쓸 수 있음). 테스트 5개 추가
- **2026-07-04**: 순수 커버리지 대신 실질적 개선으로 전환(직전 사이클 방향대로) — 결과 화면의 "다시 입력하기" 아이콘이 확인 절차 없이 바로 저장된 정보를 지우고 화면을 이동하던 실제 UX 안전성 문제를 발견해 확인 다이얼로그("다시 입력할까요? / 취소·다시 입력하기") 추가. 취소 시 아무 것도 안 지워지는지 검증하는 테스트 포함 2개 추가
- **2026-07-04**: 커버리지 재측정 결과 대부분 파일이 100%에 도달(핵심 로직/화면 전부). 남은 건 `main.dart`(부트스트랩, 관례상 제외), `result_screen.dart`의 실제 `SharePlus.instance.share()` 호출(싱글턴이라 의도적으로 테스트 안 함), `pastel_toggle_row.dart`의 `Semantics.onTap` 1줄(스크린 리더 액션 전용 경로) 뿐. 마지막 줄을 `SemanticsOwner.performAction()`으로 직접 발동시켜 검증하는 테스트를 시도했으나, deprecated된 `pipelineOwner` API 전환 과정에서 동작이 불안정해(신뢰할 수 없는 결과) 추가하지 않고 되돌림 — `matchesSemantics(hasTapAction: true)` 검증으로 이미 배선은 확인되며, 실제 액션 디스패치는 Flutter 프레임워크 책임 영역이라 판단. **이 시점부터 순수 커버리지 추가보다 남은 사람 결정 대기 항목이나 새로 발견되는 실질적 개선을 우선할 것**
- **2026-07-04**: 커버리지 점검 이어서 — 온보딩 "시작하기" 탭, birth_input의 실제 datePicker/timePicker "확인" 흐름, 성별 토글 탭이 지금까지 한 번도 실행되지 않았던 것을 발견해 테스트 4개 추가(각 pill을 실제로 열고 확인 버튼까지 눌러 검증). 이 김에 `birth_input_screen_test.dart`의 `buildApp()`에도 한국어 로케일 델리게이트를 추가해 "확인" 버튼 텍스트를 실제로 찾을 수 있게 함
- **2026-07-04**: 공유 텍스트 조립 로직(`_shareText`)이 `ResultScreen`의 private 메서드라 테스트 불가능했던 것을 `share_text.dart`(`buildShareText`, `ShareCard`와 같은 패턴)로 추출해 테스트 가능하게 만듦 — 4개 테스트 추가(내용 포함 여부, 시주 없음/있음, 음력 표시, 오행 퍼센트 합계). `_handleShare`의 실제 `SharePlus.instance.share()` 호출 자체는 여전히 테스트 범위 밖(싱글턴이라 기존 판단 유지)
- **2026-07-04**: 커버리지 점검 이어서 진행 — `ganzhi.dart`(간지 상수/오행 매핑/`GanzhiPillar`)에 전용 단위 테스트가 없던 것을 발견해 `test/core/saju/ganzhi_test.dart` 7개 추가(오행 매핑, 60갑자 순환(%10/%12), `toString()` 등). `main.dart`의 `main()` 함수 자체(부트스트랩 코드)는 낮은 커버리지가 남아있지만 이건 관례적으로 테스트 대상에서 제외(단순 배선 코드)로 판단하고 넘어감
- **2026-07-04**: `flutter test --coverage`로 실제 커버리지를 측정해 객관적으로 빈틈을 찾음 — `router.dart`가 20%로 가장 낮았고, 지금까지 화면 테스트가 전부 화면을 직접 생성해서만 검증했지 `AppRoutes.routes` 맵을 통한 실제 이름 기반 네비게이션은 한 번도 거친 적이 없었음. `test/app/router_test.dart` 4개 추가로 4개 경로 전부(birthInput/calculating/result/report) 실제 라우팅 검증
- **2026-07-04**: `test/widget_test.dart`에 앱 진입점 레벨 테스트 추가 — 최근 추가한 핵심 기능(저장된 정보가 있으면 온보딩 건너뛰고 결과 화면 바로 표시)이 지금까지 저장소/화면 단위로만 검증됐고 `CosmosSajuApp` 전체 조합으로는 검증되지 않던 빈틈을 발견해 보강
- **2026-07-04**: `project/README.md`가 `flutter create` 기본 영문 템플릿 그대로 남아 "모든 마크다운은 한국어" 규칙을 위반하고 있던 것을 발견해 실제 프로젝트 설명(한국어)으로 교체. 저장소 전체 `.md` 파일 재점검 완료(나머지는 이미 한국어 — iOS 에셋 카탈로그의 자동 생성 README 1개만 예외로 남김, 모든 Flutter 프로젝트에 동일하게 있는 보일러플레이트라 손대지 않음)
- **2026-07-04**: 저장소(`BirthInfoStore`) 관련 3곳(`main()` 시작 시 로드, birth_input 제출 시 저장, "다시 입력하기" 삭제)에 try/catch 추가 — 저장 실패 시 앱이 아예 안 열리거나 사용자가 화면 전환을 못 하는 심각한 회귀를 방지(저장은 "다음에 편의를 위한" 부가 기능일 뿐, 실패해도 현재 세션 진행은 막지 않게 함)
- **2026-07-04**: `PastelCard` 위젯 테스트 3개 추가(렌더링/기본 스타일/커스텀 padding·radius). macOS 빌드 실패 원인을 iCloud Desktop 동기화 저장공간 초과로 확정, 사용자가 동기화 끄는 중
- **2026-07-04**: PROGRESS.md를 요약 중심으로 재구성, 상세 트러블슈팅 팁은 CLAUDE.md로 이관
- **2026-07-04**: 사용자 요청으로 macOS 빌드 오류 조사 — SDK 캐시의 quarantine/provenance 속성 제거(승인받고 진행)했으나 근본 해결은 안 됨, 사용자 터미널에서 재현 확인 요청
- **2026-07-04**: `MaterialApp`에 `ko_KR` 로케일 적용 — datePicker/timePicker가 영어로 뜨던 버그 수정, 테스트로 검증
- **2026-07-04**: 접근성 2차 — 색상 대비 실측(WCAG), `inkSoft` 조정, `PastelPillButton` 시맨틱스 추가 중 `excludeSemantics`가 탭 액션을 지우는 회귀 발견·수정
- **2026-07-04**: 접근성 1차 — `PastelToggleRow` 선택 상태 시맨틱스, 체크박스 터치 영역 확대(`CheckboxListTile`), 온보딩 장식 아이콘 `ExcludeSemantics`
- **2026-07-04**: 계산 정확도 한계를 결과/리포트 화면에 안내 문구로 노출 (기존엔 코드 주석에만 있었음)
- **2026-07-04**: 사용자 요청으로 오버나이트 루프에 "빌드 오류 체크(stale dex 자동 재시도)" 단계 추가
- **2026-07-04**: 상세 리포트 화면 신규 구현 (명식 8자 breakdown, 오행 5종 전체 의미/풀이, 무료 MVP 안내)
- **2026-07-04**: 생년월일시 저장(SharedPreferences) — 재실행 시 결과 화면 바로 열기, "다시 입력하기" 추가
- **2026-07-04**: 사용자 요청으로 macOS 플랫폼 추가 (당시엔 빌드 실패 원인 미상, 이후 조사로 이어짐)
- **2026-07-04**: 공유를 텍스트 전용 → 9:16 이미지 카드(`ShareCard`) 캡처 + 텍스트로 업그레이드, 실패 시 텍스트 폴백
- **2026-07-04**: OS 홈 화면 앱 이름을 "사주랑"으로 통일 (Android/iOS)
- **2026-07-04**: 이름 입력 필드 추가(선택), 공유 버튼을 실제 OS 공유 시트(`share_plus`)로 연결
- **2026-07-04**: birth_input·calculating 화면 위젯 테스트 보강
- **2026-07-04**: 카테고리 풀이를 오행별 규칙 기반 텍스트로 개선 (기존엔 고정 placeholder)
- **2026-07-04**: 공용 위젯(PastelPillButton/ToggleRow/Card) 추출 — **PROJECT_ROUTER.md 전 항목이 처음으로 🟢/의도적 🟡 완료, 1차 기능 구현 완료**
- **2026-07-04**: 사주 결과 화면 구현 + birth_input→calculating→result 데이터 연결
- **2026-07-04**: 사주 계산 로직(4주 60갑자 + 오행 집계) 구현, 정확도 한계 문서화
- **2026-07-04 (오버나이트 루프 재시작)**: 승인 프롬프트로 밤사이 루프가 멈췄던 원인 진단(`permissions.defaultMode`) 해결, 시간 제한 없이 재등록. 명식 계산 중 화면 구현
- **2026-07-04 09:18 KST**: 오버나이트 루프 07:30 종료 조건 도달로 정지(1차 종료)
- **2026-07-03 23:35 KST**: 라우팅 + 생년월일시 입력 화면 구현 (오버나이트 루프 2회차)
- **2026-07-03 23:0x KST**: 테마 토큰 + 온보딩 화면 구현, iOS 시뮬레이터 빌드 환경 제약 최초 발견 (오버나이트 루프 1회차)
- **2026-07-03 22:5x KST**: 초기 셋업 — `project/` 폴더에 Flutter 프로젝트 생성, CLAUDE.md/PROJECT_ROUTER.md/PROGRESS.md 작성
