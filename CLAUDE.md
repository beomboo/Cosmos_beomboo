# COSMOS_BEOMBOO — 작업 규칙

사주팔자(사주/명리) 서비스를 위한 Flutter 앱 프로젝트다. Flutter 프로젝트 본체는 저장소 루트가 아니라 **`project/` 폴더 안**에 위치한다 (`project/pubspec.yaml`, `project/lib/` 등). 저장소 루트에는 문서(`docs/`, `CLAUDE.md`, `PROJECT_ROUTER.md`, `COSMOS_LOOP_JOB.md`)와 설정(`config/`)만 둔다.

## 필수 규칙

1. **경로 탐색은 반드시 [PROJECT_ROUTER.md](PROJECT_ROUTER.md)부터 시작한다.**
   새 기능을 추가하거나 기존 기능을 찾을 때, 먼저 `PROJECT_ROUTER.md`에서 해당 기능의 경로를 확인하고 그 경로로 이동해 작업한다. 새 기능을 추가했거나 기존 코드의 위치나 파일명이 수정되었다면 `PROJECT_ROUTER.md`에도 반드시 항목을 추가/수정한다. **`PROJECT_ROUTER.md`는 Flutter 프로젝트(`project/`)에 구현된 비즈니스 로직의 조직도이자 순수 네비게이션 문서다(2026-07-13, 사용자 요청) — 사이클별 발견/수정 이력을 누적 기록하는 곳이 아니다.** 표의 각 행은 "기능 | 경로 | 상태 | 한 줄 설명"만 유지하고, 날짜별 상세 서술을 덧붙이지 않는다(과거 이력이 필요하면 `git log -p PROJECT_ROUTER.md`로 확인). 항목을 갱신할 때는 그 행의 설명을 현재 상태에 맞게 **교체**하되, 이전 이력을 아래에 계속 덧붙이지 않는다.

2. **모든 마크다운 문서는 한국어로 작성한다.** (`*.md` 전부 해당. 코드 주석/커밋 메시지도 한국어 우선)

3. **작업 범위는 `COSMOS_BEOMBOO` 폴더 내부로 한정한다.** 이 디렉터리 밖의 파일을 읽거나 쓰지 않는다. (예: `~/.claude`, 다른 프로젝트 디렉터리 등 접근 금지)

4. **UI/UX는 반드시 [docs/mockups/01-pastel-cute.html](docs/mockups/01-pastel-cute.html)(파스텔 큐트) 목업을 기준으로 개발한다.** 신규 화면/컴포넌트를 만들거나 기존 화면을 고칠 때, 이 목업의 컬러 토큰(`app_colors.dart`에 이미 반영됨)·레이아웃·카피 톤에서 벗어나지 않는지 먼저 대조한다.

5. **사용자가 "푸시"를 요청하면, 그 시점까지 쌓인 변경사항을 기능 단위로 나눠 각각 커밋한 뒤에 푸시한다(2026-07-05, 사용자 요청).** 한 번에 통짜로 커밋하지 않고, 변경된 파일들을 화면/모듈/문서 등 논리적 단위로 묶어 커밋 메시지도 그 단위에 맞게 한국어로 작성한다(이전에 `project/` 전체를 15개 커밋으로 나눴던 방식 참고). 커밋 자체는 이 규칙에 따라 자동으로 진행하되, 푸시는 "푸시해달라"는 명시적 요청이 있을 때만 한다 — 커밋과 푸시를 요청 없이 먼저 하지 않는다는 기존 원칙과 배치되지 않는다.

6. **규모 있는 작업(신규 기능·화면 추가, 여러 파일에 걸친 수정 등 `PROJECT_ROUTER.md` 경로 확인이 실제로 의미 있는 작업)은 아래 처리 순서를 따른다(2026-07-13, 사용자 요청 · `/grill-me`로 합의).** 오타 수정, 색상 값 하나 변경처럼 사소한 작업에는 이 절차를 적용하지 않고 직접 처리한다. 각 단계는 메인 세션(사람과 직접 대화하는 세션)이 중개한다 — 서브 에이전트끼리 `Agent` 도구로 서로를 직접 호출하지 않는다.
   1. `saju-planner`로 요구사항을 분석하고 `PROJECT_ROUTER.md` 경로를 확인한다. 게이트 항목(accent 브랜드 컬러, 정밀 절기 계산, 십신·용신·대운/세운·십이운성·신살·공망·궁합 콘텐츠, AI 해석 연동, 결제/구독 — 2026-07-13, 사용자 요청으로 리서치가 찾아낸 신규 콘텐츠 갭 6종 추가)에 해당한다고 판단되면 그 자리에서 프로세스를 멈추고 사용자 승인을 먼저 구한다 — 승인 전에는 `saju-developer`를 호출하지 않는다.
   2. 게이트 항목이 아니면 `saju-developer`로 구현하고 표준 빌드 검증(아래 "빌드/린트 체크" 참고)까지 수행한다.
   3. `saju-tester`로 신규/변경된 로직에 대한 테스트를 보강한다 — 생략 없이 항상 거치는 단계다.
   4. `saju-tester`의 검증에서 실패가 나오면 그 내용을 가지고 `saju-developer`를 한 번 더 호출해 수정을 시도한다. 그래도 실패하면 더 반복하지 않고 사용자에게 실패 내용을 그대로 보고한다.
   - `saju-researcher`는 이 프로세스에 포함되지 않는다 — 오버나이트 자동화 루프(스케줄러)가 `COSMOS_LOOP_JOB.md`를 검토·숙지해 리서치 보강 모드(KST 00:00~06:00)에 진입할 때만 투입된다.

7. **사주 계산 결과 자체는 절대 변동 없이 정확해야 한다(2026-07-18, 사용자 요청).** 사주(명식)는 생년월일시가 같으면 언제·어떤 상황에서도 동일하게 나오는 고정된 데이터다 — 이 원문 계산(연/월/일/시주, 오행 분포 등)에 로테이션·시드·무작위 요소를 넣어 매번 다른 값을 보여주는 방식은 쓰지 않는다(과거에 이미 한 번 거부됨: "매번 다른 결과가 나오기보단 사주명리학에 근거하여 철저하고 정확한 결과가 나와야 합니다"). 반면 그 정확한 명식을 **어떻게 풀이하는지**(카피, 캐릭터화, 강조하는 포인트 등)는 해석 주체마다 다를 수 있는 영역이므로, 우리 앱만의 풀이 스타일은 여기서 자유롭게 차별화한다 — 다만 그 풀이는 항상 정확한 원문 계산 위에 얹는 것이지, 계산 자체를 흔드는 방식이어서는 안 된다.

8. **목업(`docs/mockups/*.html`) 시안 작업 중 사람 판단이 필요해 보이는 세부 사항(레이아웃 순서, 카피 문구, 우선순위 등)은 매번 되묻지 않고 합리적으로 판단해 바로 반영한다(2026-07-18, 사용자 요청).** 목업은 논의용 초안이라 되돌리기 쉽고, 실제 구현(`project/`)으로 넘어갈 때는 규칙 6번의 승인 절차를 그대로 거치므로 여기서 미리 막을 필요가 없다. 단, 결제/구독처럼 게이트 항목 자체에 해당하는 결정(예: 상세 리포트의 무료/유료 경계선)은 목업에서도 실제 값을 확정하지 않고 "🔒 미승인" 개념 시안으로만 표시한다 — 이 예외는 규칙 6번의 게이트 목록과 일치시키기 위함이다.

## 빌드/린트 체크

`project/` 디렉터리에서 실행한다 (`cd project && ...`). 표준 절차(의존성 정합성 확인 → 린트 → 테스트 → 빌드 → 산출물 확인) 전체를 한 번에 실행하려면 `./tool/check_build.sh`를 쓴다(각 단계를 순서대로 실행하고, 산출물 존재/크기 확인까지 포함하며, 실패 시 즉시 중단하고 비정상 종료 코드를 반환한다). 각 단계의 세부 사항은 스크립트 내 주석 참고.
